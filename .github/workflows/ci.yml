name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: [main]

# A newer push to the same ref cancels the in-flight run.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  backend:
    name: Backend (pytest, Python 3.12)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          # Pinned to the production runtime so the test signal matches prod.
          python-version: "3.12"
          cache: pip
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt -r requirements-dev.txt
      - name: Run tests
        env:
          ENVIRONMENT: test
          MOCK_MODE: "true"
        run: python -m pytest -q

  frontend:
    name: Frontend (typecheck + build)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v7
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - name: Typecheck
        run: npm run typecheck
      - name: Logic harnesses (type-stripped unit tests)
        # Pure-logic unit harnesses over greeting + leaderboard math + the first-login
        # order; no server/browser. The onboarding/tour pair pins the order a new student
        # meets the gates in — without them here, a reorder regresses silently (neither
        # suite was reachable from CI or scripts/start-harness.sh before).
        run: |
          node --experimental-strip-types tests/greeting_assert.mjs
          node --experimental-strip-types tests/tutor_greeting_assert.mjs
          node --experimental-strip-types tests/leaderboard_logic.mjs
          node --experimental-strip-types tests/onboarding_order_test.mjs
          node --experimental-strip-types tests/tour_engine_test.mjs
          node --experimental-strip-types tests/display_name_assert.mjs
      - name: Production build
        run: npm run build

      - name: Install Chromium for the browser harnesses
        run: npx playwright install --with-deps chromium

      - name: Visual assert harnesses (aurora + station + rotate gate)
        # The only gate that drives the real built app in a browser. Hand-run only until now,
        # so they rotted unnoticed for weeks: a pinned rail silently covered the flashcards
        # exit, and asserts stayed pinned to changed copy and to /admin + /supervisor routes
        # that had been deleted. Nothing else catches that class of regression.
        # SKIP_BUILD=1 reuses the build above; the script serves the standalone bundle,
        # warms the dynamic routes, then asserts.
        working-directory: ${{ github.workspace }}
        run: SKIP_BUILD=1 bash scripts/start-harness.sh all


  audit:
    name: Supply-chain audit
    runs-on: ubuntu-latest
    # Advisory, not a hard gate — Dependabot opens the remediation PRs. Surfaces
    # new CVEs without blocking unrelated work on unfixable transitives.
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: pip-audit
        run: |
          python -m pip install --upgrade pip pip-audit
          pip-audit -r requirements.txt || true
      - uses: actions/setup-node@v7
        with:
          node-version: "24"
      - name: npm audit (production deps)
        working-directory: frontend
        run: npm audit --omit=dev || true
