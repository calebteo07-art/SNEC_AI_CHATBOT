"use client";
/* Placeholder — Task 8 lands the real Overview. Exists so the route resolves and the
   tree builds between tasks. */
export function Overview() {
  return <p className="cs-note">Overview</p>;
}
