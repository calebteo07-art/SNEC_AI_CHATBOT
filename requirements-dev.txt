# EyeBot — development & CI tooling (NOT installed in production).
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=8.0.0
pytest-asyncio>=0.23.0

# Supply-chain auditing (run in CI; see .github/workflows/ci.yml)
pip-audit>=2.10.1

# Lockfile generation for reproducible builds (see scripts/lock-deps.sh)
pip-tools>=7.4.0
