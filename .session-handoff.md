# SESSION HANDOFF
> Black-box snapshot for a cross-account resume. Written 2026-06-14 (de-AI overhaul COMPLETE) — HEAD `6a2fed3`.
> To resume in the new session: run `/handoff-resume` (or paste this whole file and say
> "resume from this handoff").

## 1. NEXT ACTION (read this first)
**The de-AI overhaul is COMPLETE and shipped.** All three features now serve pre-authored,
KB-grounded, per-user-no-repeat content with NO runtime Gemini for generation (grading stays
AI per spec, except check-in which is MCQ/deterministic). Nothing is pending.

If the user says "continue" or wants more: the natural remaining (optional, user-gated) item
is a LIVE prod smoke-test of the new pickers + no-repeat in the running app (Cases topic
picker, Flashcards topic+difficulty picker, daily check-in MCQ) — this needs real login
credentials the agent does not have, so confirm with the user / have them drive it. Otherwise
possible polish: a bit of CSS for the flashcard picker (currently uses an inline-styled wrapper
+ reused `aurora-topic-*` classes), or expanding content volumes later. No pending question.

## 2. SESSION GOAL
De-AI the three learning features (check-in, cases, flashcards): replace runtime Gemini
generation with large, pre-authored, **KB-grounded**, role-filtered static pools served
deterministically with per-user no-repeat. Grading stays AI (case scoring/debrief, flashcard
free-text check, tutor, patient roleplay); check-in became MCQ (deterministic). Part of the
IELA 2026 award push. **STATUS: COMPLETE.** Check-in = 50 MCQ/role; cases = 151 curated;
flashcards = 300 static cards (30 sets/role). All shipped to main; tests green; no runtime
Gemini in any generation path.

## 3. DECISIONS LOCKED (do NOT re-litigate)
- **Every item relates ONLY to its role's field.** OT = ophthalmic investigations/imaging.
  **OA ≈ PSA** (almost same course) → they SHARE one pool (CLINICAL); OT separate. This is
  implemented as pools, not per-role files, for flashcards + check-in.
- **All content KB-grounded** ("backed by the database, not random"); tag `source` where the
  schema has it. Grounding source = `workflows/ophthalmology_kb.md` (469-line Module 1) + the
  90-doc/630-chunk Supabase KB (Module 1+2 PDFs + 8 OSCE docs).
- **Volumes:** check-in = 50 MCQ/role (easiest difficulty, 4-option single-best, auto-submit,
  deterministic grading — DONE). cases = 50/role = 10 topic-sets × 5, medium difficulty,
  realistic Singaporean/Singlish patient voice, every case uses a checklist/OSCE — DONE (151).
  flashcards = **30 sets/role (USER CHANGED from 50), = 15 topics × 2 difficulties (easy +
  medium) × 5 cards = 150/role**, selectable by **topic AND difficulty**.
- **Short and neutral language** in flashcards; jargon only when needed.
- Standing rules: **auto commit + push after each completed unit** (one topic/set per commit);
  **INLINE execution, no subagents**; don't overwrite workflows without permission; medical
  accuracy is critical (author in reviewable batches; additive commits to main are fine).
- Cases serve curated files (no AI gen); grading STAYS AI everywhere except check-in.

## 4. OPEN QUESTIONS (awaiting user)
None blocking. User delegated ordering ("you decide", "continue"). After flashcards content is
complete → task #22 (verify + ship the whole de-AI overhaul).

## 5. ACTIVE SKILL / WORKFLOW
none. Plain build-and-commit loop (the /handoff and /handoff-resume slash commands are in use
for cross-account continuity).

## 6. TASK LIST SNAPSHOT
- #10–#19 [completed] — AURORA redesign phases 0–9 (shipped earlier; do NOT redo).
- #20 [completed] — Cases → 50/role topic-sets, no-repeat rotation, drop AI gen. (151 cases.)
- #21 [completed] — Flashcards → 150/role as 30 sets (15 topics × easy/medium) + topic &
  difficulty picker. (DONE: 300 distinct cards, all 30 sets/role at 5/5, picker shipped.)
- #22 [completed] — Verify + ship de-AI overhaul. (tests/cases 10 + tests/api 48 pass [2
  pre-existing admin fails unrelated], frontend typecheck clean, generation paths AI-free,
  all shipped to main. Optional remaining: live prod smoke-test of pickers — user-gated.)

## 7. WORK PRODUCT SO FAR (flashcards — the live work)
**Foundation built + shipped (commit `304e4d4`), app stays working.** Content authored = 4 of
30 sets per pool (CLINICAL: ocular_emergencies, triage; OT: oct_macula, hvf) — each easy+medium
×5 = **40 cards total**. `set_card_counts('OA')` shows those 4 CLINICAL sets at 5, rest 0;
`set_card_counts('OT')` shows oct_macula+hvf at 5, rest 0. PSA == OA (shared pool).

**Remaining to author (each = 5 easy + 5 medium):**
- CLINICAL (OA+PSA) topics left (13): red_eye, history_taking, distance_va, near_vision,
  pinhole, iop_nct, eye_drops, pupil_dilation, colour_vision, amsler_macula, fall_risk,
  perioperative, abbreviations.
- OT topics left (13): oct_rnfl, gvf, ascan_biometry, optical_biometry, endothelial, asoct,
  flare, corneal_topography, pam, hrt, orthoptics, dayward_theatre, auto_refraction.
(Exact keys + display labels are in `tools/flashcards/flashcard_sets.py` → FLASHCARD_TOPICS.)

**Card shape:** `{"front": "...", "back": "..."}` inside
`FLASHCARDS[pool][topic_key]["easy"|"medium"]` (a list of 5). The serving layer adds
topic_tag/difficulty — do NOT add those in the card dict. Easy = single-fact recall; medium =
applied/scenario. Style: short, neutral, KB-grounded (see authored examples in the file).

This session also COMPLETED the entire cases library (49 new cases): OA Visual Acuity + Fall
Risk + Perioperative + History + Colour; ALL of OT (8 sets); ALL of PSA (8 sets). 151 case
JSONs in `cases/`, all 30 topic-sets ≥5, tests/cases 10 passing.

## 8. KEY FILES & REPO FACTS
Flashcards (THE LIVE WORK):
- `tools/flashcards/flashcard_sets.py` — taxonomy: FLASHCARD_TOPICS (15 CLINICAL + 15 OT,
  ordered (key,label)), DIFFICULTIES=["easy","medium"], pool_for_role (OT→"OT" else
  "CLINICAL"), make_set_key/split_set_key ("topic__difficulty"), sets_for, label_for.
- `tools/flashcards/static_cards.py` — `FLASHCARDS[pool][topic_key][difficulty]` = list of
  {front,back}; helpers get_set_cards(role,topic,diff), get_all_cards(role),
  set_card_counts(role). EDIT THIS FILE to add content. (Old flat STATIC_FLASHCARDS removed;
  nothing else imports it.)
- `tools/api/routers/student.py` — `GET /api/flashcards/topics` (30 sets + per-set total/
  completed) and `GET /api/flashcards/generate?set_key=topic__difficulty` (serves one set with
  per-user no-repeat via pick_next_unseen; no-arg = rotate whole pool). `/api/flashcards/check`
  = AI grading (KEEP). Uses `get_served_static_fronts`/`insert_cards` (Supabase, source='static').
- `frontend/src/hooks/useFlashcards.ts` — useFlashcardTopics(); useFlashcards(setKey, enabled).
- `frontend/src/aurora/screens/Flashcards.tsx` — difficulty toggle + topic picker (gates the
  deck; skipped when sessionStorage has tutor-session cards). Uses inline-styled wrapper +
  existing `aurora-topic-picker`/`aurora-topic-chip`/`aurora-topic-count`/`aurora-toggle` CSS.
- `tools/shared/static_pools.py` — pick_next_unseen (no-repeat), get_rotation_order,
  pick_by_day_count. Reused by flashcards + check-in.

Cases (DONE): `cases/*.json` (151 files), `tools/cases/topic_sets.py` (resolve_set, SET_LABELS,
explicit `topic_set` on each case), `tools/api/routers/cases.py` (topics + ?topic_set + rotation,
AI gen dropped), `tools/cases/load_case.py`. Case schema/rubric (40 pts) — copy an existing file.

Check-in (DONE): `tools/checkin/static_questions.py` (CLINICAL_POOL + OT_POOL MCQs, correct
option authored first), `tools/api/routers/checkin.py` (serves 1/day, shuffles options,
deterministic grading), `frontend/src/aurora/screens/CheckIn.tsx`.

KB / grounding: `workflows/ophthalmology_kb.md` (Module 1 — READ it; sections: Ocular
Emergencies, Triage Cat 1-4, History Taking, Basic Eye Evaluation [VA/IOP-NCT/AR-AK/Ishihara/
Amsler], OPD Skills [fall risk, eye drops, dilation, pre/post-op, triaging, lensometry,
retinoscopy, lid hygiene], Ophthalmic Investigations [A-scan, optical biometry, endothelial,
flare, HRT, ASOCT, HVF, GVF, PAM, topography], Dayward/Theatre [DISM], Orthoptics, Abbreviations).
Supabase KB queryable via `from tools.kb.search import search` (query embedding is a tiny PAID
call — not needed; Module 1 markdown is enough for flashcard authoring).

Repo: branch `main`; Render auto-deploys on push. Stack: Next 16 / React 19 / Tailwind v4 /
TanStack Query (frontend), FastAPI (backend, MOCK_MODE works without Gemini). `.env` has
GEMINI_API_KEY + SUPABASE_URL/SERVICE_ROLE_KEY. The earlier de-AI plan file:
`C:\Users\caleb\.claude\plans\golden-moseying-cray.md`.

## 9. GOTCHAS & CONSTRAINTS
- **Card dicts contain ONLY {front, back}** — the serving helper adds topic_tag + difficulty.
  Don't nest topic_tag in the card or you'll double-tag.
- **OA and PSA share the CLINICAL pool** — author once under "CLINICAL", both roles get it.
  Only OT is separate. (Same model as check-in.)
- **Verify after edits:** `set_card_counts('OA')` / `('OT')`; run
  `python -c "import tools.api.routers.student"` to catch syntax errors; before any FRONTEND
  edits run `npm --prefix frontend run typecheck`.
- **2 pre-existing test failures** in `tests/api/test_admin_endpoints.py` are NOT yours — ignore.
  `tests/cases` (10) is the cases suite; there is no flashcards unit test.
- **Medical accuracy is critical** — ground every card in the KB; reviewable, additive commits.
- **Auto commit + push after each topic/set.** Commit message style: `feat(flashcards): author
  CLINICAL <topic> set (easy+medium)`. Use a here-doc; end with the Co-Authored-By line.
- **INLINE execution, no subagents.** Don't overwrite `workflows/` files without permission.
- Never hardcode/log/commit secrets. Windows/PowerShell; git warns LF→CRLF (harmless).
- Do NOT re-run AURORA phases (#10–#19) or re-author cases/check-in — all shipped.

## 10. RESUME CHECKLIST
1. Read this file fully + `CLAUDE.md` (WAT framework) + `workflows/ophthalmology_kb.md`.
2. `git log --oneline -6` (confirm HEAD `304e4d4` or later) and `git status` (expect clean).
3. Rebuild the task list from §6 (#20 completed, #21 in_progress, #22 pending; rest completed).
4. Run the `set_card_counts` one-liner (§1) to see which flashcard sets are still 0.
5. Author the next topic's easy+medium (5 each) in `static_cards.py`, validate (counts + python
   import), commit+push one topic at a time. Tell the user the running total after each. Repeat
   on each "continue". When all 30 sets/pool = 5, do #22 (verify + ship): typecheck, tests/api,
   tests/cases, confirm no runtime Gemini for checkin/case/flashcard generation, smoke pickers.
