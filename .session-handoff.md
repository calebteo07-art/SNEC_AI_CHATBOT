# SESSION HANDOFF
> Black-box snapshot for a cross-account resume. Written 2026-07-12.
> To resume in the new session: run `/handoff-resume` (or paste this whole file and say
> "resume from this handoff").

## 1. NEXT ACTION (read this first)
**Phase 1 is COMPLETE and already PUSHED to origin/main** (fix commit `42a2f58` landed on top
of the 6 Phase-1 commits: migration backfill + idempotent column + end-to-end OSCE award test;
`python -m pytest -q` → 606 passed, only the 2 pre-existing unrelated failures in
`tests/test_flashcards_topic_prompts.py`). HEAD == origin/main == `49dd41f` (the handoff commit).
Start on **Phase 2 (Task 7 — remove the daily chat-XP cap)** and continue Phases 2→8 of the plan
`docs/superpowers/plans/2026-07-12-lumens-gamefeel.md` via subagent-driven development (one
implementer per phase, then a spec review and a code-quality review, fix loops, commit per task).
NOTE: Phase 1 backend is now LIVE on main (auto-deploys to Render) — it's additive + graceful, so
prod is fine, BUT **migration 009 still needs manual application in the Supabase SQL editor**
(see §9). Because Phase 1 already shipped, you MAY push each completed+reviewed phase as you go
(green only), or batch to the end — either is consistent with project policy; never push red.

## 2. SESSION GOAL
Ship a "make the whole app feel like a game" overhaul for EyeBot (SNEC allied-health training
platform, Next.js + FastAPI, production/Render). Deliverables: (a) unify all currency into ONE
coin called **"Lumens"** with a single gold-coin-with-engraved-iris icon used app-wide;
(b) replace the flashcards Exit button with a neon-red **Pause** button + a Pause menu
(Resume / Switch deck / Quit-with-Lumens-penalty); (c) replace weak "black text on white"
achievement toasts with big full-screen **nano-banana image reward banners** for every
achievement across tutor/flashcards/OSCE + streak-badge + Lumens-badge + level-up unlocks
(can interrupt the student); (d) add a homepage **Lumens vault** badge card (mirror of streak
badges, Selena mascot) replacing the WeekStats card; (e) **remove all daily earning caps**.

## 3. DECISIONS LOCKED (settled — do NOT re-litigate)
- Coin name = **Lumens**. Icon = gold coin with an engraved iris/pupil (inline SVG, not raster).
- Quit-a-flashcard-game penalty = **forfeit this round's unbanked Lumens + a flat −20**.
- Penalty stakes = **LIVE** — the −20 truly leaves the balance and can drop leaderboard rank;
  earned badges are protected by a separate monotonic lifetime counter (`coins_earned`).
- Currency architecture = **reskin** the existing `xp` balance as Lumens in the UI (NO DB column
  rename); add a new lifetime `coins_earned` column for badge permanence.
- Reward detection = **client-derived** queue watching `/api/progress` deltas + explicit
  achievement enqueues (no new backend unlock write-path).
- WeekStats stats (sessions/best-streak/recall/topics) = **DROPPED entirely** (not relocated).
- OSCE = **IN the economy** — awards Lumens = `round(score_100 * 2)` at end of station.
- Execution mode = **subagent-driven development** (user chose "Recommended").
- Work on **`main`** directly (project policy: no feature branch), local commits per task,
  **push only once at the very end** (Phase 8 Task 27) after the full green gate.
- Lumens vault tiers (by lifetime coins_earned): Spark 250 / Glimmer 1000 / Glow-Up 2500 /
  Floodlight 6000 / Blaze of Glory 12000 / Supernova 25000.
- Art = **placeholder-first (keyless, ships green); paid nano-banana gen ONLY on explicit
  user go-ahead** (a separate step after the code ships).

## 4. OPEN QUESTIONS
- None blocking. Tunable values the user deferred to me (already set in the plan; change only
  if the user asks): Lumens tier thresholds, the OSCE ×2 factor, the achievement list.
- One future user decision (NOT now): running the PAID nano-banana art generators
  (`tools/rewards/generate_lumen_badges.py` / `generate_reward_banners.py`). Needs explicit
  go-ahead. Placeholders ship until then.

## 5. ACTIVE SKILL / WORKFLOW
**superpowers:subagent-driven-development** — mid-flight. Per-task loop: dispatch implementer
subagent (give it the plan-file path + task numbers + scene-setting, NOT the raw history) →
handle status → dispatch spec-compliance reviewer → dispatch code-quality reviewer → fix loop
→ next. Continuous execution (do NOT check in with the user between tasks). We just finished
Phase 1's implement + spec-review + quality-review and dispatched the quality fix pass.
Remaining after Phase 1: Phases 2–8, then a final whole-implementation code review, then
superpowers:finishing-a-development-branch. Prompt templates live in the skill dir:
`C:\Users\caleb\.claude\plugins\cache\claude-plugins-official\superpowers\5.1.0\skills\subagent-driven-development\{implementer,spec-reviewer,code-quality-reviewer}-prompt.md`.
Brainstorming + writing-plans skills already COMPLETE (spec + plan written & committed).

## 6. TASK LIST SNAPSHOT
The TaskCreate/TaskList task list is EMPTY (progress was tracked in prose, not that tool).
Rebuild from the plan's 8 phases / 27 tasks if desired. Phase status:
- Phase 1 (Tasks 1–6, backend foundation): COMPLETE — implemented + spec-reviewed + quality-reviewed + fix pass landed (`42a2f58`) + PUSHED to origin/main. Migration 009 still PENDING Supabase apply.
- Phase 2 (Task 7, remove daily cap): NOT STARTED.
- Phase 3 (Tasks 8–9, Lumen icon + relabel): NOT STARTED.
- Phase 4 (Tasks 10–12, pause/quit): NOT STARTED.
- Phase 5 (Tasks 13–18, reward banner system + wire achievements + delete old toast): NOT STARTED.
- Phase 6 (Tasks 19–21, homepage Lumens vault card): NOT STARTED.
- Phase 7 (Tasks 22–23, art generators + keyless placeholders): NOT STARTED.
- Phase 8 (Tasks 24–27, design-locks + harness assert + green gate + verify + PUSH): NOT STARTED.

## 7. WORK PRODUCT SO FAR
Two committed docs + Phase-1 code (6 commits `545ecd8`..`b031cb5`), all LOCAL on `main`, NOT pushed:
- Spec: `docs/superpowers/specs/2026-07-12-lumens-gamefeel-design.md` (commit `91a5e01`).
- Plan: `docs/superpowers/plans/2026-07-12-lumens-gamefeel.md` (commit `4d4cb2c`) — the source of
  truth with EXACT code for all 27 tasks. Two inline edits were applied post-write:
  (a) pause shown on the study loop only (not intro); (b) RewardProvider gates its watcher behind
  auth via an inner `<RewardWatcher>` so `/api/progress` isn't fetched on the login route.
- Phase 1 code committed: migration `009_lumens.sql`, `coins_earned` in `update_profile.py` +
  `get_profile.py` + `get_progress.py`, `/api/flashcards/forfeit` + clamp 500→5000 in
  `student.py`, `osce_lumens()` + award + `lumens_awarded` in `cases.py`, tests
  `tests/profile/test_coins_earned.py` + `tests/api/test_lumens.py` + updated
  `tests/api/test_flashcards_complete.py`.

**FIX PASS — LANDED as commit `42a2f58` (already pushed). Both fixes done, no action needed:**
1. `tools/db/migrations/009_lumens.sql` now backfills + idempotent column (APPLIED.md still PENDING):
```sql
ALTER TABLE student_profiles ADD COLUMN IF NOT EXISTS coins_earned bigint NOT NULL DEFAULT 0;
UPDATE student_profiles SET coins_earned = xp WHERE coins_earned = 0;
ALTER TABLE student_profiles ADD CONSTRAINT student_profiles_coins_earned_nonneg CHECK (coins_earned >= 0);
```
   (WHY: without the backfill, the first post-migration earn makes `update_profile` read 0 and
   permanently under-count every existing student's lifetime Lumens.)
2. Add a test to `tests/api/test_lumens.py` driving `POST /api/cases/{case_id}/submit` (monkeypatch
   `load_case`/`_case_cache`, `_check_case_access`, `evaluate_case`, `compute_station_score`, `ask`,
   `log_session`, `log_case_completion`, `update_profile` — copy the pattern from existing OSCE submit
   tests, check `tests/cases/test_case_access.py` / `tests/api/test_cases*.py`) asserting
   `update_profile` was called with `xp_delta == round(score_100*2)` and the response `lumens_awarded`
   equals it (e.g. score_100=80 → 160).
   NOT FIXING (intentional): OSCE echoes computed `award` without a re-fetch — matches existing
   best-effort OSCE profile-write pattern; leave as-is.

## 8. KEY FILES & REPO FACTS
- Plan (SOURCE OF TRUTH for all remaining code): `docs/superpowers/plans/2026-07-12-lumens-gamefeel.md`.
- Currency truth today: `student_profiles.xp` (Supabase). Level = `xp//500+1`. Leaderboard ranks by xp.
- Flashcards live under `frontend/src/aurora/` (NOT `components/flashcards` at src root):
  orchestrator `screens/Flashcards.tsx`; shell `components/flashcards/FlashShell.tsx` (had the Exit
  button at line 30, `exit()` → `/dashboard`); topic selection is state-driven (`newDeck()` resets to
  fan); Dark Arcade CSS in `aurora/aurora.css` (`--fc-*` tokens ~line 2296; `.flash-press`,
  `.flash-advance`/`.flash-start` chunky toy buttons; `.flash-exit` ~line 2345).
- Home: `screens/Dashboard.tsx` renders `.hm-lower` = `<MilestoneLadder>` (streak badges) +
  `<WeekStats>` (the card to REPLACE). Streak badge pattern to MIRROR: `components/home/streakBadges.ts`,
  `SelenaBadge.tsx`, `MilestoneLadder.tsx`; assets `public/brand/badges/*.jpg`; CSS `home.css` (`.hm-badge`).
- Progress hook: `hooks/useProgress.ts` (`ProgressData`, key `["progress"]`, GET `/api/progress`).
- Flashcards hook: `hooks/useFlashcards.ts` (add `useFlashcardForfeit`).
- Auth: `screens/AuthContext.tsx` → `useAuth()`; the stable per-student id is `user.studentId`.
- Old toasts to retire: `screens/AchievementToast.tsx` (white glass card, top-right — the "boring" one)
  + sonner `toast.success` achievement/level-up calls in `Flashcards.tsx`/`Tutor.tsx`. Provider chain:
  `app/providers.tsx` (mount `<RewardProvider>` inside `<AuthProvider>`).
- Daily cap to remove: `CHAT_XP_DAILY_CAP` in `lib/legacy/gamification.ts:42` (+ tutor cap toast).
- Nano-banana pattern to COPY: `tools/leaderboard/crest_art.py` + `generate_crests.py`
  (`--estimate/--generate/--install`, refuses MOCK_MODE, flash model `gemini-3.1-flash-image`,
  keying). Image core: `tools/avatar/generate_sprites.py` (`generate_image_bytes(prompt, model, reference=)`,
  `MODELS["flash"]`, anchored to `frontend/public/brand/iris.png`). Iris/Selena = ONE-EYED HAIRLESS BLOB
  (not human) — bake into prompts.
- Backend: Python 3.12, tests auto MOCK_MODE. `update_profile.py` uses a "guarded separate DB call"
  pattern (missing column can't break main write) — the coins_earned write follows it.
- Design-locked surfaces (must amend `docs/design-locks.md` in Phase 8, don't silently rebuild):
  flashcards + home. Red was reserved for wrong-answer verdicts; the neon-red Pause is a deliberate lock
  amendment.

## 9. GOTCHAS & CONSTRAINTS
- **DO NOT PUSH until Phase 8 Task 27** (after full green gate). `main` auto-deploys to Render prod.
- **Never stage `mandy.md` / `rodtang.md`** — pre-existing unrelated untracked files at repo root.
- **Migration 009 must be applied to Supabase before/at deploy** (human step; code degrades gracefully
  via the `coins_earned → xp` fallback so main never boots broken). Emit paste-ready SQL, never a file
  path, into the Supabase editor. NO `ADD CONSTRAINT IF NOT EXISTS` (Postgres 42601). APPLIED.md currently
  marks 009 PENDING.
- **2 pre-existing test failures** in `tests/test_flashcards_topic_prompts.py` are UNRELATED (image-prompt
  module, ancestor commit `62ebf12`) — not a regression; do not "fix" them as part of this work.
- **PAID nano-banana gen needs explicit user go-ahead** — placeholders only until then.
- Frontend verification convention: `cd frontend && npm run typecheck && npm run build` + aurora harness
  + behavioral verify (NO frontend unit-test runner). Harness: `bash scripts/start-harness.sh aurora` or
  the warm-server recipe `node frontend/tests/aurora_assert.mjs http://127.0.0.1:3000`. NOTE: the aurora
  harness has a KNOWN pre-existing RED at the flashcards D2 back-face assertion — unrelated, don't chase it.
- Shell discipline: PowerShell cmdlets → PowerShell tool; POSIX (git, pytest) → Bash tool. A bash_guard
  hook blocks violations.
- Dev box is Windows; repo root `C:/Users/caleb/OneDrive/Desktop/SNEC_AI_CHATBOT`.
- Commit trailer required: `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`. Stage only the
  files a task changed.
- Bump `PERSIST_SCHEMA_VERSION` in `frontend/src/lib/queryClient.ts` IF a persisted-query shape changes
  (adding `coins_earned` to ProgressData is a persisted-shape change → bump it in Phase 6, or returning
  users may white-screen).

## 10. RESUME CHECKLIST
1. Read this file top to bottom.
2. Re-invoke **superpowers:subagent-driven-development** (Skill tool) to reload the workflow.
3. Open the plan `docs/superpowers/plans/2026-07-12-lumens-gamefeel.md` — it has exact code for every task.
4. Execute §1 NEXT ACTION: verify/complete the Phase-1 fix pass (backfill + OSCE test), confirm green.
5. Continue subagent-driven execution Phases 2→8 (implementer → spec review → quality review → fix loop
   per phase; commit per task; DO NOT push until Phase 8 Task 27). Use fast/standard models for
   mechanical/integration tasks, capable models for review.
6. At Phase 8: amend `docs/design-locks.md`, update the aurora harness assert, run the full green gate
   (`python -m pytest -q` + `cd frontend && npm run typecheck && npm run build` + harness), do the
   behavioral verify, then push to `main` and coordinate applying migration 009 in Supabase.
7. Do NOT run paid nano-banana art gen without asking the user first.
