# SESSION HANDOFF
> Black-box snapshot for a cross-account resume. Written 2026-06-13.
> To resume in the new session: run `/handoff-resume` (or paste this whole file and say
> "resume from this handoff").

## 1. NEXT ACTION (read this first)
We are mid-brainstorm on the **EyeBot frontend redesign**. Three art-direction candidates
have been presented to the user (A · Nocturne Atlas — dark editorial; B · Folio — light
editorial; C · Deep Field — dark instrument). **We are waiting on the user to pick one (or a
mix).** The moment they choose, proceed to present the full design in sections (architecture,
navigation model, per-screen layouts, motion system, component system), get approval per
section, then write the spec to `docs/superpowers/specs/2026-06-13-frontend-redesign-design.md`.

## 2. SESSION GOAL
The user dislikes the current EyeBot frontend (the PHOTOPIC v2 light theme) — everything
except the login screen, which they like and want to keep. They want to **strip the entire
frontend and conduct a full redesign — a rebuild from zero** (rethink which screens/features
exist; only the login screen and the backend API are sacred). Driven by personal taste, the
**IELA award submission due 22 Jun 2026** (9 days out — must impress judges in screenshots/
demos), and stakeholder feedback. We are running the `superpowers:brainstorming` skill to
turn this into an approved design spec, then an implementation plan.

## 3. DECISIONS LOCKED
From the user's answers to clarifying questions (do NOT re-ask these):
- **Login kept:** the current light PHOTOPIC login is the one they like. Relationship to new
  design = "login is just a keeper" — they like it as a screen but it is NOT a hard style
  constraint; login can be adjusted later to match the new direction if needed.
- **Dislikes (all four):** overall aesthetic, motion/FX overload, layout & information design,
  and visual details. The current frontend fails on every axis for them.
- **Strip depth:** Rebuild from zero — rethink screens and features, not just a reskin.
- **Drivers:** personal taste + IELA award (22 Jun deadline) + stakeholder feedback.
- **Motion:** "Rich but tasteful" — still wants wow-factor moments for the award demo, but
  fewer, slower, more intentional than the current FX-heavy build.
- **Centerpiece:** No single hero — all core screens get equal weight; the navigation model
  itself should be the star.
- **Cuts:** Cut nothing — all current features survive (chat tutor, clinical cases, flashcards,
  progress, summary, daily check-in, gamification, admin/supervisor), just redesigned and
  possibly reorganised.
- **Platform:** Desktop-first (judges demo on laptops; admin/supervisors live on desktop),
  adapt down to mobile gracefully.
- **Personality (two picks):** Cinematic/futuristic + Editorial/academic.
- **References:** Linear/Vercel + Apple (modern product-tool craft + polished consumer warmth).

## 4. OPEN QUESTIONS
**THE pending decision:** which art direction the user wants. Three candidates were rendered
as miniature dashboard mockups via the visualize/show_widget tool:
- **A · Nocturne Atlas** — midnight medical atlas. Near-black `#0D0F13`, ivory serif `#EDE6D6`,
  one luminous blue `#4D9FFF`, amber `#D9A441` accent. Engraved-plate anatomy, hairline rules,
  mono readouts. Marries BOTH personality picks (dark+dramatic AND scholarly). Strongest
  "judges remember it" bet. Risk: dark medical UI can drift moody.
- **B · Folio** — beautiful journal issue. Bone paper `#F6F3EC`, navy ink `#14202E`, single
  vermilion accent `#E04F2E`, oversized serif numerals. Cinema through scale, not darkness.
  Closest to the existing light login → most seamless login→app transition.
- **C · Deep Field** — precision optical instrument. Deep blue-black `#070A12`, cyan `#5EE1FF`,
  brand blue `#3C90FF`, glassy panels, mono readouts, live data viz. Apple-pro × Vercel.
  Strongest "advanced technology" signal; weakest editorial/academic signal.
User may also pick a mix (e.g. "A's darkness with C's data style"). After they choose, the
brainstorm continues into full design sections.

## 5. ACTIVE SKILL / WORKFLOW
**`superpowers:brainstorming`** is in flight. Checklist progress:
1. Explore project context — DONE
2. Offer visual companion — DONE (user accepted inline visuals)
3. Ask clarifying questions — DONE (two rounds of AskUserQuestion, ~11 questions answered)
4. Propose 2–3 approaches — DONE (the three art directions are the approaches; awaiting pick)
5. Present design + get approval per section — NOT STARTED (next, once direction chosen)
6. Write design doc to `docs/superpowers/specs/2026-06-13-frontend-redesign-design.md` + commit
7. Spec self-review (placeholders/contradictions/scope/ambiguity)
8. User reviews written spec
9. Transition to `superpowers:writing-plans` (the ONLY skill to invoke after brainstorming)
HARD GATE: do not write code / invoke implementation skills until the design is approved.

## 6. TASK LIST SNAPSHOT
- #1 Explore project context — completed
- #2 Offer visual companion — completed
- #3 Ask clarifying questions — in_progress (effectively done; advance to #4/#5)
- #4 Propose 2-3 redesign approaches — pending (done in substance; awaiting user pick)
- #5 Present design and get approval — pending
- #6 Write design doc — pending
- #7 Spec self-review — pending
- #8 User reviews written spec — pending
- #9 Transition to writing-plans — pending

## 7. WORK PRODUCT SO FAR
- Two `AskUserQuestion` rounds capturing all decisions in section 3.
- One `show_widget` render titled `eyebot_art_direction_candidates` with the three art
  directions A/B/C as miniature dashboard mockups (full colour/type specs in section 4).
- No spec file written yet. No code touched. No frontend files modified.
- Built `/handoff` + `/handoff-resume` slash commands (this snapshot mechanism) in
  `.claude/commands/` — a side task the user requested mid-brainstorm; now complete.

## 8. KEY FILES & REPO FACTS
- `.awwwards_state.md` (repo root, git-tracked) — the PHOTOPIC v2 state anchor. Describes the
  CURRENT frontend being replaced: Next.js 16.2.1 App Router + React 19 + Tailwind v4 + GSAP +
  R3F + Lenis + TanStack Query. Light "PHOTOPIC" art direction (paper `#FDFDFC`, ink `#1F1F1F`,
  brand blue `#3C90FF`). Read it for the full current-state inventory we're redesigning away from.
- `CLAUDE.md` — WAT framework (Workflows/Agents/Tools) operating instructions.
- `frontend/src/app/(auth)/page.tsx` → `@/screens/OnboardingScreen` — the login being KEPT.
  Uses GazeIris (R3F iris) + HiggsfieldLoop accent. Classes: `login-card`, `login-brand`, etc.
- `frontend/src/screens/` and `frontend/src/app/(shell)/` — all the screens to be redesigned
  (dashboard, chat, cases, flashcards, summary, progress, supervisor, profile, admin/*).
- `frontend/src/fx/` — the heavy motion/WebGL layer (fluid canvas, preloader, liquid tiles,
  scroll choreography) that the "motion/FX overload" complaint targets.
- Backend: FastAPI API-only on 127.0.0.1:8000; Next standalone server is the public process,
  proxies `/api/*`. Backend API is sacred — redesign is frontend-only.
- Deploy: Render builds the Dockerfile. Prod https://snec-ai-chatbot.onrender.com.

## 9. GOTCHAS & CONSTRAINTS
- **Auto commit + push after meaningful work** (standing user rule). Commit messages via
  `git commit -F` — PowerShell mangles embedded quotes.
- **Don't create/overwrite workflow files** (`workflows/*.md`) without explicit permission.
- **Paid actions need confirmation:** Higgsfield loops + Gemini media sweeps cost credits.
- Environment: Windows, PowerShell. Use PowerShell syntax. `.claude/` is gitignored — the
  slash command files live on-disk and survive account switches (same machine) but are NOT in
  git; only this `.session-handoff.md` is git-tracked and travels with the repo.
- Today is 2026-06-13. IELA deadline 22 Jun 2026 = 9 days. Keep scope deliverable in that window.
- This is a rebuild-from-zero of the frontend ONLY. Backend, auth, and the login screen stay.

## 10. RESUME CHECKLIST
1. Read this file in full, plus `.awwwards_state.md` and `CLAUDE.md`.
2. Rebuild the task list from section 6 (TaskCreate/TaskUpdate, preserving statuses).
3. Re-invoke `superpowers:brainstorming` with a note that we're at step 4→5, awaiting the
   user's art-direction pick.
4. Re-orient the user in a few lines (goal + locked decisions + the pending A/B/C question).
5. If the user has already stated a pick, proceed to present the full design sections; if not,
   re-surface the A/B/C choice (section 4). Do NOT re-ask anything in section 3.
