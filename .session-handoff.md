# SESSION HANDOFF
> Black-box snapshot for a cross-account resume. Written 2026-07-09 (resume session #3:
> Task 6 SHIPPED+PUSHED this session; Task 7 implemented + BOTH reviews ✅ + the 1 accepted
> fix (self-heal regression assert) LANDED as `5a3973a` (LOCAL, NOT pushed). Only the Task-7
> FULL-build harness gate + push remain — see §1).
> To resume in the new session: run `/handoff-resume` (or paste this whole file and say
> "resume from this handoff").

## 1. NEXT ACTION (read this first)
**Task 7 is CODE-COMPLETE: implemented + spec-review ✅ + quality-review ✅ + the one accepted
fix (self-heal once-per-session regression assert) LANDED as commit `5a3973a` (LOCAL-ONLY).
The ONLY remaining Task-7 steps are: an OPTIONAL quick re-review of the added test → the
FULL-build aurora harness gate → push. Do that, then start Task 8.**

Sanity-check on load (Bash tool, from repo root `C:\Users\caleb\OneDrive\Desktop\SNEC_AI_CHATBOT`):
```bash
git -C "C:\Users\caleb\OneDrive\Desktop\SNEC_AI_CHATBOT" log --oneline -3   # expect HEAD = 5a3973a (Task 7) on top of ec6caa0
grep -c "self-heal" "C:\Users\caleb\OneDrive\Desktop\SNEC_AI_CHATBOT\frontend\tests\aurora_assert.mjs"   # expect 6 (self-heal assert present)
```
Expected: HEAD = `5a3973a`, grep = 6 → the fix is in place; go straight to the OPTIONAL
re-review then the HARNESS GATE. (Safety net — only if HEAD is still `f73b1ae` and grep = 0,
the amend was somehow lost: re-dispatch a sonnet implementer with §7 "FIX INSTRUCTIONS —
verbatim" to re-add the assert + `git commit --amend --no-edit`, LOCAL, no push.)

OPTIONAL re-review: per subagent-driven-development's "reviewer reviews again" rule, you may
dispatch the code-quality reviewer (opus) on `ec6caa0..5a3973a` focused only on the new
`aurora_assert.mjs` self-heal section. It's a test-only, keyless addition doing exactly what
the reviewer asked (verified `node --check` OK) — a fast confirmation, not a blocker.

**HARNESS GATE (Task 7 — DO NOT skip, DO NOT use SKIP_BUILD=1 this time):**
The Task-7 implementer built into a SCRATCH dir (`.next-verify`, since reverted) because a
stale server held `.next/standalone` locked — so **`.next/standalone` does NOT yet contain
Task 7's frontend changes.** You MUST run a FULL build. Recipe (Bash tool):
```bash
# NO SKIP_BUILD — a fresh build is REQUIRED so .next/standalone reflects Task 7
bash "C:\Users\caleb\OneDrive\Desktop\SNEC_AI_CHATBOT\scripts\start-harness.sh" serve
# ^ this foregrounds/tails after warming; it may TIME OUT in the tool — that's EXPECTED.
#   The server still comes up. Verify it's live, then assert:
curl -s -o /dev/null -w "HTTP=%{http_code}\n" --max-time 10 http://127.0.0.1:3000/
node "C:\Users\caleb\OneDrive\Desktop\SNEC_AI_CHATBOT\frontend\tests\aurora_assert.mjs" http://127.0.0.1:3000; echo "EXIT=$?"
bash "C:\Users\caleb\OneDrive\Desktop\SNEC_AI_CHATBOT\scripts\start-harness.sh" stop
```
Expect EXIT=0 and ~53 PASS lines (51 prior + 1 greeting-hero + 1–2 self-heal). If EXIT=0:
```bash
git -C "C:\Users\caleb\OneDrive\Desktop\SNEC_AI_CHATBOT" push origin main
```
Then TaskUpdate #7 → completed, and start Task 8 (read plan §Task 8 first — see §8).
**No user question blocks any of this.** The only user gate left is the PAID Tasks 10–11.

## 2. SESSION GOAL
Execute the 11-task TDD plan `docs/superpowers/plans/2026-07-08-selena-seamless-custom.md`
via **superpowers:subagent-driven-development**. It replaces the user-rejected per-student
"custom Selena" (flat vector stickers composited over a raster — "extremely ugly") with ONE
transparent AI-rendered whole-look portrait per student, shown + CSS-animated on every
surface, plus a bigger/funnier option catalog and real AI option tiles in a "loadout" Studio.
Tasks 1–9 are code (auto-execute continuously, no check-ins); Tasks 10–11 are PAID live-Gemini
and are GATED on explicit user go-ahead.

## 3. DECISIONS LOCKED
- **Execution mode: Subagent-driven** (user chose "Subagent-driven (Recommended)"). Per task:
  implementer subagent → spec-compliance reviewer (sonnet) → code-quality reviewer (opus) →
  fix loop → verified LOCAL commit → CONTROLLER runs harness → push to `main` → next task.
  Continuous, NO check-ins between tasks.
- **Model selection:** implementer=sonnet, spec-reviewer=sonnet, quality-reviewer=opus; bump
  to opus only for architecture/judgment.
- Four locked design decisions from brainstorming:
  - (D-a) greeting-card animation = living CSS motion on ONE render per look (no 2nd pose frame).
  - (D-b) Studio option tiles = real AI renders (placeholders first, paid later).
  - (D-c) way more real AI tiles + "ridiculous and funny variety of every component".
  - (D-d) "loadout" hero — hero only ever shows real rendered art; picks dock as tiles; Save
    fuses a new render. The user-facing "✨ Fusing your look…" copy is LOCKED (don't reword);
    it already exists in SelenaStudio.tsx.
- **Tasks 10–11 are PAID and require explicit user go-ahead** before any live Gemini call
  (~103-tile art batch ≈ $1–2.50, then an end-to-end look smoke). Never fire live gen otherwise.
- Auto-ship: after each green+verified task, stage ONLY that task's files, commit, push
  directly to `main` (no feature branch). Never push red — frontend changes need the aurora
  harness green FIRST.
- **Task 4 (settled):** `TILES_ROOT` in `tools/avatar/tiles.py` is `Path(__file__)`-anchored
  (absolute) — do NOT "simplify" back to cwd-relative.
- **Task 5 (settled):** colour-axis tray chips deliberately render NO `<img>`; emoji + label
  only. Don't "restore" an img there.
- **Task 6 (SHIPPED this session):** dispositions from prior session stand; harness went green
  (51 PASS), pushed `c3052d6..ec6caa0`. Task 6 is DONE.
- **Task 7 code-review dispositions (settled THIS session — do not re-litigate):**
  - Spec review (sonnet): ✅ Spec compliant — exactly 6 files, clean tree, no config drift,
    the `reportCustomized` harness deviation traced and confirmed sound.
  - Quality review (opus): ✅ Ready-to-merge=With fixes. 0 Critical, 1 Important, 2 Minor.
  - **ACCEPTED — Important:** self-heal once-per-session gate had no automated coverage → add a
    regression assert (aligns with CLAUDE.md `/ship-check` mandate for once-per-X invariants;
    same bug class as the check-in show-once bug burned 5× in June). This is the fix being
    applied (§7).
  - **SKIPPED — Minor #1** (`onError` loop guard on the iris.png fallback): it's the verbatim
    plan snippet; `/brand/iris.png` is a reliable bundled asset (its failure = whole-app-broken)
    so the loop can't realistically occur; deviating from locked plan copy not warranted.
  - **SKIPPED — Minor #2** (a failed self-heal `mutate()` burns the session's single attempt):
    reviewer explicitly said "No change required" — correct/safe ordering, recoverable next
    session; acknowledged trade-off.

## 4. OPEN QUESTIONS
None for Tasks 7–9 — the plan is fully specified. The only pending user gate is the
**go-ahead for the PAID Tasks 10–11** (do not start them without it).

## 5. ACTIVE SKILL / WORKFLOW
**superpowers:subagent-driven-development** (mid-flight). Prompt templates live under
`C:\Users\caleb\.claude\plugins\cache\claude-plugins-official\superpowers\5.1.0\skills\subagent-driven-development\`
(`implementer-prompt.md`, `spec-reviewer-prompt.md`, `code-quality-reviewer-prompt.md`) and
`.../requesting-code-review/code-reviewer.md` (all four read this session).
**Current position: Task 7 implemented (`f73b1ae`, LOCAL) + spec-review ✅ + quality-review ✅;
the one accepted fix (self-heal regression assert) is being applied (state uncertain across
the switch — see §1 CASE A/B). Remaining Task-7 steps: (re-)apply the fix if lost → quick
re-review → controller FULL-build harness → push → mark #7 complete.** Then per-task cycle
for 8→9: read the task's full plan text → dispatch implementer (sonnet, LOCAL commit only, no
push, no full harness) → spec review (sonnet) → quality review (opus) → fix loop → controller
FULL-build harness → push green → next. After Task 9: final whole-implementation code review →
superpowers:finishing-a-development-branch → update memory (`project_ricoe_v2` + a new memory
for this seamless-custom brief).

## 6. TASK LIST SNAPSHOT
Rebuild with TaskCreate/TaskUpdate exactly (statuses verbatim):
- #1 [completed] Task 1: Promote keying to tools/shared/keying.py
- #2 [completed] Task 2: Portrait v2 — green-screen render → transparent webp
- #3 [completed] Task 3: Selena v3 raster-only — delete compositor, migrate consumers
- #4 [completed] Task 4: Tile tooling — placeholders + tile-file mandate
- #5 [completed] Task 5: Studio loadout builder
- #6 [completed] Task 6: Catalog expansion + phrase coverage gate  ← SHIPPED+PUSHED this session
- #7 [in_progress] Task 7: Greeting card living custom Selena + self-heal  ← code done + both reviews ✅; self-heal fix being applied; then harness(FULL build)+push remain
- #8 [pending] Task 8: Leaderboard portraits (bulk, no N+1)
- #9 [pending] Task 9: Design locks + final verification sweep

## 7. WORK PRODUCT SO FAR (this session)

### Task 6 — SHIPPED + PUSHED this session
Resumed from prior handoff with Task 6 code-complete + both reviews ✅ + Minor#1 applied,
committed LOCAL as `dd99251`. This session ran the ONLY remaining gate: the aurora warm-server
harness assert = **EXIT=0, 51 PASS lines** (incl. the Studio loadout / tile-art / Selena-
everywhere asserts). Then `git push origin main` → `c3052d6..ec6caa0` (pushed `dd99251` Task-6
commit + the prior handoff commit `ec6caa0`). TaskUpdate #6 → completed. Task 6 DONE.

### Task 7 — CODE-COMPLETE (`5a3973a`, LOCAL, NOT pushed) + BOTH REVIEWS ✅ + accepted fix LANDED
Commit message (KEEP EXACT if you re-amend): `feat(home): the student's OWN Selena lives on
the greeting card — entrance, breathe, tinted halo + once-per-session portrait self-heal`.
HEAD chain: `5a3973a` (Task 7, amended from the original `f73b1ae` to fold in the self-heal
assert) → `ec6caa0` (Task-6 handoff, = origin/main) → `dd99251` (Task 6).
**6 files changed** (all verified by an independent spec reviewer reading the diff `ec6caa0..f73b1ae`;
the amend `5a3973a` only added the self-heal section to `aurora_assert.mjs`):
- `frontend/src/hooks/useAvatar.ts` — NEW `useSelfHealPortrait(data?: AvatarResponse)`:
  `SELF_HEAL_KEY = "eyebot_portrait_heal"`; no-op unless `data.customized`; returns unless
  `portrait_status` is `"none"`/`"failed"`; sessionStorage gate → once per session; storage-
  throw path early-returns (does NOT fire mutate); then `useRequestPortrait().mutate()`. Effect
  deps `[data?.customized, data?.portrait_status, mutate]`. Added `useEffect` import.
- `frontend/src/aurora/components/home/GreetingHero.tsx` — new props `portraitUrl?: string|null`
  + `background?: string`; when `portraitUrl` truthy renders `.hm-selena` (with `--halo` =
  `backdropGlow(background)`) wrapping `.hm-selena-halo` + `<img className="hm-selena-img"
  onError→/brand/iris.png>`; else the existing `<SelenaLogo motion="hello" className="hm-iris"/>`.
  Imports `backdropGlow` from `@/aurora/avatar/backdrops` (confirmed real: `(id?: string)=>string`).
- `frontend/src/aurora/screens/Dashboard.tsx` — `useAvatar()` → `useSelfHealPortrait(avatar)` →
  `portraitUrl = avatar?.customized && avatar.portrait_status === "ready" ? avatar.portrait_url
  : null`; passes `portraitUrl` + `background={avatar?.config?.background}` to `<GreetingHero>`.
- `frontend/src/aurora/screens/SelenaStudio.tsx` — mounts `useSelfHealPortrait(data)` after its
  `useAvatar()` (avatar var is named `data`).
- `frontend/src/aurora/home.css` — `.hm-selena` (216², `hm-selena-enter` 0.9s), `.hm-selena-img`
  (reuses existing `hm-iris-bob`, transform-origin 50% 90%), `.hm-selena-halo` (radial-gradient
  `var(--halo,#f2e2d0)`, `hm-halo-breathe` 4.8s) + the 2 keyframes; BOTH reduced-motion blocks
  extended to freeze the 3 new classes; responsive `@media (max-width:900px)` adds `.hm-selena
  { width:158px; height:158px }`.
- `frontend/tests/aurora_assert.mjs` — (a) NEW greeting-hero assert in the studio "Selena
  everywhere" section: nav `/dashboard`, wait `.hm-selena img.hm-selena-img`, FAIL if `src` not
  `data:`, log `PASS: Home greeting — the student's OWN rendered Selena, animated…`; via a
  `reportCustomized` flag (default `false`) threaded into the studio `/api/avatar` GET mock's
  `customized` field, set `true` ONLY around the new `/dashboard` nav+assert then reset `false`
  synchronously before the next `await` (so later reduced-motion/onboarding sections still see
  `customized:false`/`SelenaLogo`) — spec-review confirmed sound. (b) LANDED in `5a3973a`: the
  self-heal REGRESSION assert in a fresh isolated `healCtx` (mirrors `onbCtx`, closed at end):
  mocks `GET /api/avatar` → `customized:true`, `portrait_status:"none"`; counts `POST
  /api/avatar/portrait` (verified endpoint, `useAvatar.ts:72`; stubbed keyless 200, MOCK-safe);
  asserts exactly 1 render fires on `/dashboard` mount, then nav away (`/leaderboard`) + back
  fires NO second request (once-per-session gate holds). PASS lines: `PASS: self-heal —
  customized+portrait-none student fires exactly one render on mount` and `PASS: self-heal —
  second mount in the same session does NOT re-fire (once-per-session gate holds)`.

**Task-7 gates already GREEN:** `npm run typecheck` clean; `npm run build` succeeded (via a
scratch `.next-verify` dir workaround — see §9 EBUSY; reverted next.config.ts/next-env.d.ts/
tsconfig.json cleanly); `node --check aurora_assert.mjs` = PARSE OK after the amend. Backend
pytest NOT needed (frontend-only task). **NOT yet run: the FULL-build aurora warm-server harness
(the last Task-7 gate) — and it MUST be a full build because `.next/standalone` does NOT contain
Task 7 (built to scratch dir).**

### FIX INSTRUCTIONS — verbatim (use in §1 CASE B to re-dispatch a sonnet implementer)
Add a once-per-session self-heal REGRESSION assert to `frontend/tests/aurora_assert.mjs`. It must prove:
1. A `customized:true` student whose `portrait_status` is `"none"` fires **exactly one**
   portrait-render request when the greeting card mounts (self-heal).
2. Re-mounting in the SAME browser session (nav away and back within the same Playwright
   context so sessionStorage persists) fires **NO** second request (once-per-session gate holds).
Guidance: FIRST read `useAvatar.ts` to confirm the EXACT method+URL `useRequestPortrait().mutate()`
hits (believed `POST /api/avatar/portrait` — verify). Use an ISOLATED Playwright context (fresh
sessionStorage), mirroring the onboarding-section `onbCtx` pattern; do NOT reuse `navCtx`; close
it at the end. Mock `GET /api/avatar` → `customized:true`, `portrait_status:"none"`, valid
`config` (greeting shows `SelenaLogo` fallback while self-heal fires). Intercept the render
endpoint, COUNT invocations, fulfill each with a harmless stubbed 200 (KEYLESS — no live Gemini).
Nav `/dashboard`, wait for 1 request, assert count===1; nav away+back, settle, assert count STILL
1. PASS lines e.g. `PASS: self-heal — customized+portrait-none student fires exactly one render
on mount` and `PASS: self-heal — second mount in the same session does NOT re-fire (once-per-
session gate holds)`. Use `console.error("FAIL: …")` + `process.exit(1)` on failure. Then
`node --check` the file, `git add frontend/tests/aurora_assert.mjs`, `git commit --amend
--no-edit` (fold into `f73b1ae`, LOCAL, NO push). SKIP both Minors (do not touch onError guard
or the mutate ordering).

## 8. KEY FILES & REPO FACTS
- Plan (source of truth, full TDD code per task): `docs/superpowers/plans/2026-07-08-selena-seamless-custom.md`
  (1535 lines). **Task 7 = lines 1214–1369 (DONE, fully read).** **Task 8 = lines 1371–~1470
  (Steps 1–3 read this session: pure `rank_entries` portraits passthrough test + impl, and the
  `get_avatar_images_bulk` bulk getter in `tools/shared/db.py` — READ THE REST, incl. the
  router wiring + frontend + harness steps, before dispatching).** Task 9 = after Task 8
  (~1470–1535, NOT read — read before dispatching).
- Spec: `docs/superpowers/specs/2026-07-07-selena-seamless-custom-design.md`.
- Task-8 shape (from the read portion): NO N+1 — one `get_avatar_images_bulk(config_hashes)`
  query in `tools/shared/db.py` (hash→ready image_url), `rank_entries` gains `portraits: dict|None`
  passthrough (`tests/gamification/test_leaderboard.py::test_rank_entries_carries_portrait_urls`
  written failing-first), router `tools/api/routers/student.py` wires the bulk lookup +
  `LbEntry.portrait_url`, frontend `useLeaderboard.ts` + `Leaderboard.tsx` render it, harness
  mock rows updated. Tests: `tests/gamification/test_leaderboard.py`, `tests/api/test_leaderboard_endpoint.py`.
  (Task 8 is backend+frontend → after impl, run BOTH `python -m pytest -q` AND the frontend
  harness before push.)
- `tools/avatar/parts.py` — AVATAR_AXES / DEFAULT_AVATAR / CONFIG_VERSION (single source of truth).
- `tools/avatar/portrait.py` — phrase maps + `PROMPT_MAPS` (10 gated axes; NOT background) +
  coverage gate `tests/avatar/test_prompt_coverage.py`.
- `frontend/src/hooks/useAvatar.ts` — `useAvatar`, `useRequestPortrait`, `useSelfHealPortrait`
  (NEW this task), `AvatarResponse` type (`customized`, `portrait_status`, `portrait_url`, `config`).
- `frontend/src/aurora/avatar/backdrops.ts` — `backdropGlow(id?)`, `GRADIENTS`.
- Gates: backend `python -m pytest -q` (MOCK_MODE auto-on, keyless — NEVER set GEMINI_API_KEY);
  frontend `cd frontend && npm run typecheck && npm run build`; robust harness gate = warm-server
  assert (§9). Suite was **603 green** at Task 6 (Task 7 added no backend tests).
- `scripts/start-harness.sh` — `serve` builds (unless SKIP_BUILD=1) → copies `.next/static`+`public`
  into `.next/standalone` → starts `node .next/standalone/server.js` on :3000 → warms routes →
  leaves server up (the tool call may TIME OUT because it foregrounds — server still up). `stop`
  kills the pidfile'd server. **For Task 7 you MUST NOT pass SKIP_BUILD=1** (standalone lacks Task 7).
- CLAUDE.md + memory `MEMORY.md` govern: WAT architecture, single-worker Render (never block the
  event loop), fail-closed config, JWT identity, PowerShell-tool-vs-Bash-tool discipline
  (bash_guard hook), DB migrations only via /db-migrate, never delete Dockerfile.

## 9. GOTCHAS & CONSTRAINTS
- **The self-heal fix LANDED** as amend `5a3973a` (a background implementer agent finished right
  as this handoff was being written). HEAD = `5a3973a`, `grep -c self-heal aurora_assert.mjs` = 6,
  tree clean apart from this handoff file. The §1 sanity-check should confirm this; the CASE-B
  re-dispatch path is only a safety net if the amend is somehow missing.
- **Task-7 harness needs a FULL build (NOT SKIP_BUILD=1)** — the implementer built Task 7 into a
  scratch `.next-verify` dir (then reverted), so `.next/standalone` on disk is the OLD Task-6
  build without Task 7. A SKIP_BUILD harness would falsely test stale code. Do a fresh build.
- **DO NOT push `f73b1ae` (or its amended sha) until the FULL-build aurora harness is EXIT=0.**
  Frontend change; `main` auto-deploys Render prod; CLAUDE.md /ship-check mandates a behavioral
  harness verify. All other gates (typecheck, build, both reviews) are green.
- **WARM-SERVER HARNESS RECIPE (robust gate):** `serve` (FULL build for Task 7), verify HTTP 200
  on :3000, then `node frontend/tests/aurora_assert.mjs http://127.0.0.1:3000` (expect EXIT=0),
  then `stop`. **Do NOT run the all-in-one `bash scripts/start-harness.sh aurora`** — it can pass
  every assert then HANG on teardown for tens of minutes. If a run wedges, kill ONLY the
  start-harness bash PID + playwright chromium (match `ms-playwright`/`_npx`) — NEVER the user's
  `chrome.exe`.
- **EBUSY / stale standalone server (HIT + FIXED this session):** a stale `node
  .next/standalone/server.js` (was PID 17064) held a Windows lock on `.next/standalone` →
  `npm run build` failed EBUSY. I killed it via PowerShell `Stop-Process -Id <pid> -Force`;
  port 3000 was free at snapshot. If build fails EBUSY again: find the pid on :3000
  (`netstat -ano | grep :3000`), confirm it's `node .next/standalone/server.js`
  (`wmic process where "ProcessId=<pid>" get CommandLine`), Stop-Process it, retry.
  NOTE: `serve` foregrounding times out the tool call (exit 143) but the server DOES come up —
  check `netstat`/`curl` rather than assuming failure; and its `stop` may miss a server whose
  serve was SIGTERM'd before writing its pidfile (that's exactly how 17064 got orphaned).
- **SHARED CWD / bash_guard:** the Bash tool and PowerShell tool share ONE persisted cwd. Never
  `cd` into a subdir inside a Bash compound command. Use absolute paths; for git use
  `git -C "C:\Users\caleb\OneDrive\Desktop\SNEC_AI_CHATBOT" …`. The frontend build's
  `(cd frontend && …)` subshell does NOT change parent cwd. Recovery:
  `Set-Location "C:\Users\caleb\OneDrive\Desktop\SNEC_AI_CHATBOT"` in the PowerShell tool.
- **PAID gating:** Tasks 10–11 and any `generate_tiles.py --generate` / live image call require
  explicit user go-ahead. Placeholders + tests are keyless.
- **Two-stage review order is mandatory:** spec compliance (sonnet) MUST be ✅ before code quality
  (opus). Apply receiving-code-review judgment (verify each finding on merits) — see §3 Task-7
  dispositions as the pattern.
- **Implementer must NOT run the full harness and must NOT push** — LOCAL commit only; controller
  runs the warm-server harness and pushes. Stage ONLY that task's files.
- Windows/PowerShell box: PowerShell cmdlets → PowerShell tool; POSIX/git/npm/bash-harness →
  Bash tool. bash_guard PreToolUse hook enforces this.
- Don't reword the LOCKED "✨ Fusing your look…" copy. Don't re-litigate §3 decisions.
- **Commits ahead of origin at snapshot:** `5a3973a` (Task 7, LOCAL, harness-unverified) + this
  handoff commit on top. origin/main = `ec6caa0`. **This handoff was committed LOCAL-ONLY and NOT
  pushed** — pushing it would also push the unverified Task-7 frontend `5a3973a` to `main` and
  auto-deploy Render prod, which CLAUDE.md forbids before the harness is green. Both push together
  once the Task-7 FULL-build harness is EXIT=0 (matches how the prior handoff sat local on top of
  the unverified Task-6 commit).

## 10. RESUME CHECKLIST
1. Read this file top to bottom. CLAUDE.md + MEMORY.md load automatically.
2. Rebuild the task list (§6) with TaskCreate/TaskUpdate (mark #1–6 completed, #7 in_progress).
3. Re-invoke the active skill: superpowers:subagent-driven-development (finishing Task 7).
4. Run the §1 sanity-check (`git log --oneline -3` → HEAD `5a3973a`; `grep -c self-heal
   aurora_assert.mjs` → 6). Expected: the fix is present → optional quick re-review of the added
   self-heal test (opus). Only if it's somehow missing, use the CASE-B safety path in §1.
5. **Execute the NEXT ACTION (§1): run the Task-7 FULL-build warm-server harness assert (NO
   SKIP_BUILD).** If EXIT=0, `git push origin main` (ships `5a3973a` + this handoff commit),
   TaskUpdate #7 → completed.
6. Continue Tasks 8→9 via the same cycle (implementer sonnet → spec sonnet → quality opus → fix
   loop → controller FULL-build harness → push). READ Task 8's remaining plan text (~1420–1470)
   and Task 9 (~1470–1535) BEFORE dispatching each. Task 8 is backend+frontend → run BOTH pytest
   and the harness. Do NOT start PAID Tasks 10–11 without explicit user go-ahead.
7. After Task 9: final whole-implementation code review → superpowers:finishing-a-development-branch
   → update memory (`project_ricoe_v2` + a new memory for this seamless-custom brief).
