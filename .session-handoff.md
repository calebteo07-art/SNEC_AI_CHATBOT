# SESSION HANDOFF
> Black-box snapshot for a cross-account resume. Written 2026-06-15.
> To resume in the new session: run `/handoff-resume` (or paste this whole file and say
> "resume from this handoff").

## 1. NEXT ACTION (read this first)
Continue implementing the **dark "Control" console for staff (admin + supervisor)** — the
plan is APPROVED and CSS is partly written. The immediate next step is **Task #23: build
`frontend/src/aurora/components/ConsoleRail.tsx` and branch `AppShell.tsx` on role**.
Task #22 (CSS scope) is ~95% done — only thing left is to double-check whether anything
else needs a dark patch once screens render; otherwise mark #22 complete and move to #23.
No pending user question — the user already answered both clarifying questions (see §3).
Just keep building. **Nothing is committed yet** — all edits are uncommitted in the working
tree.

## 2. SESSION GOAL
Redesign the EyeBot **admin website** into a **dark "command-center" console** that is
visually distinct from the light student app (AURORA). Requirements from the user:
(1) make the admin website dark mode, (2) staff can **only** access admin/oversight — no
student screens, (3) declutter the messy/cluttered admin screens and use purposeful,
differentiated colour instead of "all the same boring colours." Part of the IELA 2026 award
push. Purely presentation + navigation — **no backend/API changes, no change to the student
experience.**

## 3. DECISIONS LOCKED
- **Scope = admin + supervisor** (user picked "Admin + Supervisor (Recommended)"). BOTH
  roles become "staff": dark console + locked out of student learning screens. The
  `/supervisor` screen also goes dark.
- **Aesthetic = "Slate command-center"** (user picked it). Deep desaturated indigo-slate
  base (`--canvas:#0E1016`), glowing Gemini blue/purple/rose accents, **per-domain colour
  coding**: students=blue, active/engagement=green, at-risk=rose, AI cost=purple.
- **Students stay 100% light** — the dark scope is applied ONLY to the staff shell; the
  student AURORA app must be untouched (regression check required).
- Implementation strategy locked: re-theme via a **scoped dark token override**
  (`.console-dark`) rather than rewriting every component, since the admin UI is
  token-driven (`var(--surface/--ink/--hairline/--g-*)`).
- `ChangePasswordModal` stays light (self-contained `login-*` overlay) — do NOT re-theme it
  (would risk the real login screen).
- Approach approved via ExitPlanMode. Full plan saved at
  `C:\Users\caleb\.claude\plans\golden-moseying-cray.md` (overwrites the old de-AI plan).

## 4. OPEN QUESTIONS
None outstanding. Both clarifying questions were answered (§3). Minor design latitude was
delegated to the agent ("redesign with your own decisions") — proceed without re-asking.

## 5. ACTIVE SKILL / WORKFLOW
None mid-flight. Plan mode is already EXITED and the plan APPROVED, so just execute. (The
session earlier referenced superpowers:systematic-debugging — not active now.) Standing
behavioural rules from memory still apply: auto commit + push after completed work; don't
overwrite `workflows/` without permission; commit messages end with the Co-Authored-By
trailer.

## 6. TASK LIST SNAPSHOT
Rebuild these with TaskCreate/TaskUpdate (tasks #1–21 are prior completed work — only the
last 6 are this session):
- #22 [in_progress] Add .console-dark theme scope + StatCard green tone
- #23 [pending] Build dark ConsoleRail + branch AppShell on role
- #24 [pending] Slim AdminShell (drop redundant pill-tabs/header)
- #25 [pending] Redesign AdminOverview + Accounts (declutter + colour)
- #26 [pending] Re-theme AdminStudents, AdminActivity, Supervisor for dark
- #27 [pending] Verify (lint/tsc/build) + commit + push

## 7. WORK PRODUCT SO FAR
**Already edited (uncommitted) — Task #22, basically done:**
- `frontend/src/aurora/MotionSurface.tsx` — added `"tint-green"` to the `variant` union.
- `frontend/src/aurora/components/StatCard.tsx` — `StatTone` now `"blue"|"purple"|"rose"|"green"`.
- `frontend/src/aurora/tokens.css` — added a light `.aurora-flow[data-variant="tint-green"]`
  variant (right after the tint-rose line).
- `frontend/src/aurora/aurora.css` — added
  `.aurora-statcard[data-tone="green"] .aurora-statcard-label { color: var(--on-green-2); }`
  (after the rose rule), AND appended a big `/* Control console — dark scope (staff only) */`
  block at the END of the file. That block contains: `.console-dark` token overrides
  (paper#14161F, canvas#0E1016, surface#181B26, hairline rgba(255,255,255,.09),
  ink#F3F4F8/ink-2#AEB4C2/ink-3#7C8295, brightened g-blue#5B9DFF/g-purple#B68BE6/
  g-rose#F07A86/g-green#43C76B, on-* light variants, logo-ink#E8EAF2, color-scheme:dark);
  retinted `.aurora-mesh` blobs; white-alpha hover patches for navitem/signout/cmdk/
  btn-ghost; SNEC logo invert filter; `.aurora-btn` → brand gradient + dark text; trow-hover/
  badge/bar-track/select dark patches; brighter `tint-*` StatCard glows incl. tint-green;
  insight band; modal backdrop darken; PLUS new console chrome classes ready for the screens:
  `.console-rail-brand`, `.console-section-head`, `.console-tick[data-hue=...]`,
  `.console-card-accent`, `.console-segment` (segmented control for Accounts),
  `.console-disclosure`/`-body` (collapsible Promote panel), `.console-focus` +
  `.console-attn-count` (Overview at-risk focal panel).

**NOT yet started (designed in the plan, ready to build):**
- ConsoleRail + AppShell branch (#23), AdminShell slim-down (#24), Overview/Accounts
  redesign (#25), Students/Activity/Supervisor re-theme (#26), verify+commit (#27).

**Design intent for the unbuilt pieces (from the approved plan):**
- **AppShell.tsx**: compute `isStaff = role==='admin'||role==='supervisor'`. For staff render
  `<div className="aurora-shell console-dark">` + `<ConsoleRail/>` (instead of `<AtlasRail/>`)
  + the SAME `<main>`/mesh/scroll markup (mesh auto-retinted by scope). Student path
  unchanged. Filter `destinations` (⌘K palette) to the staff set for staff.
- **ConsoleRail.tsx** (NEW): dark staff-only nav reusing existing `.aurora-rail*`,
  `.aurora-navitem`, `.aurora-rail-foot`, `.aurora-profile`, `.aurora-avatar`,
  `.aurora-signout` classes. admin → Overview(`/admin`)/Students(`/admin/students`)/
  Accounts(`/admin/accounts`)/Activity(`/admin/activity`); supervisor → Supervisor
  (`/supervisor`) + Admin(`/admin`) cross-link. Footer: identity, **Change password**
  (mount `ChangePasswordModal` via local state), Sign out (reuse `useAuth().logout` +
  `router.push('/')`). Use `<Wordmark size={18}/>` (logo-ink is light in scope). The mobile
  bottom-bar behaviour comes free from the shared `.aurora-rail` @media (~aurora.css:190).
  Active-state via `usePathname` + `data-active`. Note `/admin` is active when pathname is
  exactly `/admin` (sub-tabs startWith `/admin/...`) — mirror AdminShell's existing
  activeTab logic.
- **AdminShell.tsx**: DROP the `.aurora-tabs` pill nav + the big `.aurora-admin-head` block
  (nav/identity now in rail). KEEP `AdminCtxProvider`, the `AdminStudentDetail` +
  `ChangePasswordModal` mounts, and a slim section title using `.console-section-head`
  + `.console-tick`.
- **AdminOverview.tsx**: KPI tones → Students=blue, Active this week=green, At risk=rose,
  AI tokens=purple (StatCard now supports green). Give at-risk the primary `.console-focus`
  panel; weak-topics secondary. Move "Refresh media library" out of the top into a quiet
  `.console-disclosure` utility (or small ghost button).
- **AdminAccounts.tsx** (the main clutter fix): collapse the two big Add + CSV `.aurora-card`s
  into ONE provisioning card with a `.console-segment` toggle ("One student" | "Import
  CSV") so only one form shows at a time; make the approved roster the primary content;
  move "Promote to staff" into a `.console-disclosure`. Status badges Active=green/
  Pending=amber. ALL existing handlers/endpoints unchanged.
- **AdminStudents.tsx**: keep table; dark via scope; keep OA/OT/PSA role badges; add a small
  rose at-risk row marker.
- **AdminActivity.tsx**: keep day-grouped feed (dots already colour-coded ✓green/✗rose/›blue);
  dark via scope.
- **Supervisor.tsx**: re-themed dark by scope; light declutter (group leaderboard/digest
  controls); keep KPIs/benchmarks/at-risk table.

## 8. KEY FILES & REPO FACTS
Working dir: `C:\Users\caleb\OneDrive\Desktop\SNEC_AI_CHATBOT` (Windows, PowerShell). Branch
`main`. Frontend = Next.js 16 in `frontend/`.
- `frontend/src/aurora/AppShell.tsx` — top shell; computes `role`, `destinations`; renders
  AtlasRail + main(mesh) + CommandPalette. **EDIT for staff branch.**
- `frontend/src/aurora/components/AtlasRail.tsx` — the light student rail (STUDY/INSIGHT/
  OVERSIGHT). Reference for ConsoleRail structure; do NOT break it.
- `frontend/src/aurora/screens/AdminShell.tsx` — admin layout: header + `.aurora-tabs` pill
  nav (TABS=Overview/Students/Accounts/Activity) + AdminCtxProvider + modals. **EDIT.**
- `frontend/src/aurora/screens/AdminOverview.tsx | AdminStudents.tsx | AdminActivity.tsx |
  AdminAccounts.tsx` — the 4 admin pages. **EDIT** (#25/#26).
- `frontend/src/aurora/screens/Supervisor.tsx` — supervisor dashboard (own `.aurora-admin`
  + header). **EDIT** (#26).
- `frontend/src/aurora/screens/AdminStudentDetail.tsx` — student-detail modal (NOT yet read;
  uses aurora-modal classes — re-themed by scope, but glance at it during #26).
- `frontend/src/screens/CheckInGuard.tsx` — **route lockout ALREADY EXISTS** (lines ~22-29):
  admin→`/admin`, supervisor→`/supervisor` (both keep `/profile`); students forced to
  `/checkin`. Don't duplicate this. Lockout is therefore a nav/visual problem, not routing.
- `frontend/src/screens/OnboardingScreen.tsx` (~158-160) — login already routes admin→/admin,
  supervisor→/supervisor, else→/checkin.
- `frontend/src/screens/AuthContext.tsx` — `User.role = "student"|"supervisor"|"admin"`;
  `useAuth()` exposes `user`, `logout`, `loading`, `isAuthenticated`.
- `frontend/src/screens/ChangePasswordModal.tsx` — self-contained `login-*` overlay; leave
  light.
- `frontend/src/screens/adminShared.tsx` — `useAdminOutlet`, types (`ApprovedStudent`,
  `StudentProfile`, `FeedItem`, `Credential`), `getInitials`, `fmtTokens`, role colours.
- `frontend/src/aurora/tokens.css` — base AURORA tokens (light). `.console-dark` overrides
  these.
- `frontend/src/aurora/aurora.css` — all `.aurora-*` shell/admin classes + the NEW
  `.console-dark` block at the bottom.
- `frontend/src/styles/index.css` — imports tokens.css then aurora.css (order matters; scope
  override works because `.console-dark` is more specific).
- `frontend/src/aurora/MotionSurface.tsx`, `components/StatCard.tsx` — green tone added.
- `frontend/src/aurora/Logo.tsx` — `Wordmark`/`Logo`; uses `--logo-ink` (overridden light in
  scope) and supports `tone="white"`.
- App Router routes: `frontend/src/app/(shell)/layout.tsx`→AppShell; `(shell)/admin/layout.tsx`
  →AdminGuard+AdminShell; `(shell)/admin/{page,students,accounts,activity}`; `(shell)/supervisor`.
- Plan file: `C:\Users\caleb\.claude\plans\golden-moseying-cray.md` (full approved plan).
- Memory index: `C:\Users\caleb\.claude\projects\C--Users-caleb-OneDrive-Desktop-SNEC-AI-CHATBOT\memory\MEMORY.md`.

## 9. GOTCHAS & CONSTRAINTS
- **Auto commit + push after completed work** (user standing rule). Commit messages MUST end
  with: `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>` (fixed trailer regardless
  of which account/model resumes).
- **Nothing is committed yet this session** — the working tree has all the §7 edits. Don't
  lose them.
- Do NOT touch the student experience or any API/backend. Do NOT change auth/role model.
- Do NOT re-theme `ChangePasswordModal` or `login-*` classes (risks the real login screen,
  which the user previously hand-tuned and wants kept verbatim).
- Windows + PowerShell environment (no `&&` chains in Windows PowerShell 5.1; use `;`/`if`).
  Verify build from `frontend/`: `npm run lint` + `npx tsc --noEmit` (or `npm run build`).
- Render prod = ONE uvicorn worker (single event loop); irrelevant to this frontend-only
  task but don't add blocking backend calls.
- NEVER commit secrets (`.env` has GEMINI/SUPABASE/BREVO keys).
- The `.console-dark` block is appended to aurora.css; if adding more dark patches, keep them
  in that block at the bottom.
- StatCard green tone is wired end-to-end (MotionSurface variant + StatTone + tint-green CSS +
  statcard-label colour) — safe to use `<StatCard tone="green" .../>`.

## 10. RESUME CHECKLIST
1. Read this file in full + `CLAUDE.md` + the plan at
   `C:\Users\caleb\.claude\plans\golden-moseying-cray.md`.
2. `git status` / `git log -1` to confirm the §7 edits are present and uncommitted.
3. Rebuild the task list (#22–#27) with TaskCreate/TaskUpdate, statuses per §6.
4. Verify Task #22 edits exist (grep `console-dark` in aurora.css; `tint-green` in
   tokens.css/MotionSurface; `green` in StatCard) → mark #22 complete.
5. Execute NEXT ACTION (§1): build `ConsoleRail.tsx` + branch `AppShell.tsx` (#23), then
   #24 → #25 → #26 → #27.
6. Verify (lint/tsc/build), then commit + push. Manual check: admin & supervisor land on
   dark console with staff-only nav + locked out of student screens; student app still
   light & intact.
