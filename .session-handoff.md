# SESSION HANDOFF
> Black-box snapshot for a cross-account resume. Written 2026-06-14 (cases-authoring session) — HEAD `7e34470`.
> To resume in the new session: run `/handoff-resume` (or paste this whole file and say
> "resume from this handoff").

## 1. NEXT ACTION (read this first)
Continue authoring **cases** to fill every topic-set to 5 (target 50/role), one
complete set per commit. The user's last messages were just "continue" / "continue you
decide" — they want steady, autonomous batches with NO questions. The moment you load:
pick the next thin/empty case set (see §7 for exact counts), author the missing cases
(KB-grounded, role-filtered, explicit `topic_set`, Singaporean voice, short/neutral,
medium difficulty, 40-pt rubric), validate that the set hits 5/5, then commit+push.
**OA AND OT ROLES ARE NOW COMPLETE (OA 51/50, OT 50/50, all sets ≥5).** Only **PSA remains
(31/50, +19)**. Next PSA thin sets — ocular_emergencies (+3), pupil_dilation (+3),
colour_macular (+3), triage_referral (+3), history_taking (+2), tonometry_iop (+2),
fall_risk (+2), visual_acuity (+1). Author one set per commit; next PSA case number = case_psa_032+.
After PSA hits 50, cases (#20) is DONE → move to flashcards (#21). No pending user question.

## 2. SESSION GOAL
De-AI the learning content for three features (check-in, cases, flashcards): replace
runtime Gemini generation with large, pre-authored, **KB-grounded**, role-filtered static
pools served deterministically with per-user no-repeat. Part of the IELA 2026 award push.
Current focus: building the **content + structure to the user's locked spec** (50 check-in
MCQ/role, 50 cases/role as 10 topic-sets, 250 flashcards/role as 50 sets). Check-in is
DONE; cases structure is DONE and content is ~⅔ authored; flashcards not started.

## 3. DECISIONS LOCKED (do NOT re-litigate)
Locked spec (user restated twice, verbatim intent):
- **Every question/case/flashcard relates ONLY to its role's field of study.** OT =
  ophthalmic investigations/imaging. **OA ≈ PSA** (they "study almost the same course") —
  they SHARE a clinical pool/taxonomy; OT is separate.
- **All content must be KB-grounded** ("backed by info from the database, not random").
  Tag each item with its `source`. Use ALL 90 KB files across all features (OSCE info,
  checklists, modules — "usable everywhere").
- **Volumes:** check-in = **50 MCQ/role** (easiest difficulty). cases = **50/role = 10
  topic-sets × 5** (medium difficulty), selectable by topic, realistic Singaporean context,
  patient language imperfect-but-English, **every case uses a checklist and/or OSCE**.
  flashcards = **250/role = 50 sets × 5 cards**, selectable by **topic AND difficulty**
  (easy + medium).
- **Short and neutral language in all features.**
- **Check-in MCQ shape:** the user dismissed the format question; default kept = 4 options,
  single best answer, auto-submit on tap, deterministic grading (no AI).
- Standing rules: **auto commit + push after each completed unit**; **INLINE execution, no
  subagents**; don't overwrite workflows without permission; **medical accuracy is critical**
  (author in reviewable batches — but the user is fine with committing additive case files
  to main as they land, one set per commit, since they're reversible).
- Earlier locked: cases serve curated files (no AI gen); grading STAYS AI (case scoring/
  debrief, flashcard free-text check, tutor, patient roleplay) EXCEPT check-in (now MCQ →
  deterministic).
- OSCE docs: the 8 source `.docx` were ingested into Supabase KB (redacted). The user
  confirmed they want them in the DB.

## 4. OPEN QUESTIONS (awaiting user)
None blocking. The user has delegated ordering ("you decide") and just says "continue".
After cases reach 50/role, the next big piece is **flashcards** (50 sets×5/role, topic +
difficulty picker) — replicate the cases topic-picker pattern, then author 750 cards.

## 5. ACTIVE SKILL / WORKFLOW
none. (No superpowers/plan-mode skill active. Plain build-and-commit loop.)

## 6. TASK LIST SNAPSHOT
- #1–#10 [completed] — AURORA redesign phases 0–9 (shipped earlier).
- #11 [completed] — Rewrite de-AI plan & get approval.
- #12 [completed] — Ingest source docs into Supabase KB.
- #13 [completed] — Check-in → MCQ (done: 50/role, deterministic, auto-submit).
- #14 [in_progress] — Cases → 50/role, topic-sets, no-repeat, drop AI gen. (structure DONE;
  content ~⅔; this is the ACTIVE task.)
- #15 [pending] — Flashcards → 250/role as 50 topic-sets (5 cards) + topic+difficulty picker.
- #16 [completed] — Remove dead AI content-gen code paths.
- #17 [pending] — Verify + ship de-AI overhaul (reviewable batches). (blocked by #14, #15)

## 7. WORK PRODUCT SO FAR (cases progress — the live work)
**Cases library is now 132 JSON files in `cases/` (target 150).** Counts by role:
**OA 51/50 COMPLETE, OT 50/50 COMPLETE, PSA 31/50.** Working tree is CLEAN (all committed).
Next case numbers: PSA→case_psa_032+ (OA last=case_oa_051, OT last=case_ot_050).
This session completed all of OA (Visual Acuity, Fall Risk, Perioperative, History, Colour) and
all remaining OT (OCT Imaging, Screening, Biometry, Anterior Segment, Orthoptics, Dayward/Theatre,
Visual Fields, Refraction) — one set per commit, all pushed (HEAD `7e34470`).

Per-role topic-set fill status (10 sets/role; each target 5). "✅" = 5/5 done this session.
Use `python -c "..."` with `resolve_set` (see §8) to recompute exact counts anytime.
- **OA:** Ocular Emergencies 5 ✅(pre-existing), Red Eye 6 ✅, History Taking 2 (+3),
  Visual Acuity & Refraction 1 (+4), Intraocular Pressure 5 ✅, Eye Drops 5 ✅, Pupil
  Dilation 5 ✅, Colour Vision & Amsler 2 (+3), Fall Risk 1 (+4), Pre/Post-Op 4 (+1).
- **OT:** OCT Imaging 2 (+3), Visual Fields 4 (+1), Biometry 3 (+2), Corneal Topography 5 ✅,
  Anterior Segment 3 (+2), Refraction & Acuity 4 (+1), Orthoptics 3 (+2), Screening 2 (+3),
  Potential Acuity & Pre-Cataract 5 ✅, Dayward & Theatre 3 (+2).
- **PSA:** Ocular Emergencies 2 (+3), Red Eye 5 ✅, History Taking 3 (+2), Visual Acuity &
  Near Vision 4 (+1), Non-Contact Tonometry 3 (+2), Eye Drops 5 ✅, Pupil Dilation 2 (+3),
  Colour & Amsler 2 (+3), Fall Risk & PFAER 3 (+2), Triage & Referral 2 (+3).

Sets completed THIS session (commit each): PSA Red Eye (`397fdb6`), OT PAM (`45d2372`),
OA Eye Drops (`7aaa48a`), OA Pupil Dilation (`7e1645d`), OA IOP (`972ed3d`), OT Corneal
Topography (`07bfdf4`), PSA Eye Drops (`e0254ec`).

**Authoring pattern (follow exactly):** copy the schema of an existing case (e.g.
`cases/case_ot_028_pam_good_potential_dense_cataract.json`). Required keys: case_id, title,
difficulty ("intermediate" = medium), topic, **`topic_set`** (set the explicit set key so
bucketing is reliable), role, estimated_minutes (12–15), checklist_procedure (a real KB
checklist name), patient{name,age,gender,occupation,presenting_complaint}, history{hpc,
pmhx,family_hx,medications,social_hx}, examination_findings{va,iop,anterior_segment,fundus},
investigations{...}, diagnosis, management{immediate,follow_up,patient_education}, rubric
{history,investigations,diagnosis,management} each {points:10, key_points:[4 short items]}
(rubric total MUST be 40). Fictional Singaporean patient names + light Singlish in the
patient's quoted speech. Next case numbers: OA→case_oa_037+, OT→case_ot_035+, PSA→case_psa_032+.

## 8. KEY FILES & REPO FACTS
SHIPPED this session (all on `main`, Render auto-deploys):
- **Check-in MCQ (`b12e8b3`):** `tools/checkin/static_questions.py` (MCQ pool: CLINICAL_POOL
  shared by OA+PSA, OT_POOL; ~50/role, each {question,options[4],answer,explanation,source}),
  `tools/api/routers/checkin.py` (serves 1 MCQ/day via `pick_by_day_count`, shuffles options
  deterministically per student/day, grades deterministically — NO AI), frontend
  `frontend/src/aurora/screens/CheckIn.tsx` (tap-to-auto-submit), CSS in
  `frontend/src/aurora/aurora.css` (.aurora-checkin-option*).
- **Cases topic-sets (`b65683b` + `45d2372`):** `tools/cases/topic_sets.py` = SINGLE SOURCE
  OF TRUTH: `SET_LABELS` (ordered 10 sets/role: OA, PSA, OT) + `resolve_set(role, topic)`
  (substring rules) + `label_for` + `sets_for`. Backend `tools/api/routers/cases.py`:
  `GET /api/cases/topics` (sets + total/completed counts), `GET /api/cases?topic_set=KEY`
  (that set's cases), no-arg = no-repeat rotation window (CASE_WINDOW=6). CaseInfo carries
  set_key/set_label. **Bucketing prefers an explicit `c.get("topic_set")` then falls back to
  resolve_set** — so always set `topic_set` in new cases to avoid keyword collisions.
- Frontend `frontend/src/aurora/screens/Cases.tsx`: topic-chip picker (fetches
  `/api/cases/topics`); "All topics" = Atlas Map + rotation, pick a topic = that set as a grid.
- KB ingestion (`f1c41fc`): `tools/kb/extract_docx.py` (stdlib .docx → text),
  `tools/kb/ingest_docx.py` (extract → REDACT credentials/NRICs → chunk → embed → insert;
  idempotent). 8 OSCE docs ingested as `OSCE/...` (module 2, clinical_procedure).
- Grounding source: `workflows/ophthalmology_kb.md` (469-line Module 1 — READ IT; it has the
  emergencies/triage/history/basic-eval/OPD-skills/investigations/orthoptics/abbreviations
  content most cases cite). Plus 90 docs / 630 chunks in Supabase (Module 1+2 PDFs + 8 OSCE),
  queryable via `tools/kb/search.py` (`from tools.kb.search import search`).
- Recompute set counts:
  `python -c "import sys;sys.path.insert(0,'.');from tools.cases.load_case import load_case,list_available_cases as L;from tools.cases.topic_sets import resolve_set as R;from collections import Counter;[print(r) for r in 1]"`
  — simpler: load each case, `setkey = c.get('topic_set') or R(c['role'], c['topic'])`, count per (role,setkey).
- Verify after edits: `python -m pytest tests/cases -q` (expect 10 passed) and a
  load/schema/40-pt check on the new files (see prior commits' validation one-liners).
- The earlier de-AI plan file: `C:\Users\caleb\.claude\plans\golden-moseying-cray.md` (approved).

## 9. GOTCHAS & CONSTRAINTS
- **Topic-keyword collisions:** `resolve_set` uses ordered substring rules; a topic string
  containing a keyword for another set mis-buckets (hit twice this session: "triage"/
  "referral"→triage_referral over red_eye; "macular"→oct_imaging over pam). FIX: always put
  an explicit `"topic_set": "<set_key>"` in every new case. Valid OA/PSA keys: ocular_emergencies,
  red_eye, history_taking, visual_acuity, tonometry_iop, eye_drops, pupil_dilation,
  colour_macular, fall_risk, and (OA) perioperative / (PSA) triage_referral. OT keys:
  oct_imaging, visual_fields, biometry, corneal_topography, anterior_segment, refraction_acuity,
  orthoptics, screening, precataract_pam, dayward_theatre.
- **8 pre-existing test failures** in `tests/api/test_admin_endpoints.py` (2) and
  `tests/profile/test_update_profile.py` (6) — NOT caused by this work (those files untouched;
  e.g. KeyError 'session_count' is test/impl drift). Ignore them; `tests/cases` is the
  relevant suite (10 passing).
- **`tools/kb/ingest_docx.py` source docs contained LIVE training credentials** (snecscmtrg01/
  snecpsa001 + passwords) and NRICs — these are redacted before ingest; never store/commit
  them. The user was advised to rotate those passwords.
- `.env` has GEMINI_API_KEY + SUPABASE_URL/SERVICE_ROLE_KEY (present). Embeddings = paid but
  tiny; only needed if ingesting more docs. No paid call needed for cases/flashcards authoring.
- Windows/PowerShell. Git: warns "LF will be replaced by CRLF" — harmless. Commit with a
  here-doc `git commit -q -F - <<'EOF' ... EOF`. End messages with the Co-Authored-By line.
- Frontend changes: run `npm --prefix frontend run typecheck` before committing UI edits.
- Do NOT re-run the AURORA phases (#1–#10) — long shipped.

## 10. RESUME CHECKLIST
1. Read this file fully + `CLAUDE.md` + `workflows/ophthalmology_kb.md` (grounding source).
2. `git log --oneline -8` — confirm HEAD `e0254ec` (or later); `git status` clean.
3. Rebuild the task list from §6 (only #14 in_progress, #15/#17 pending; rest completed).
4. Recompute per-set case counts (§7/§8) to find the next thin/empty set.
5. Author the next complete set (≥5/5) per the §7 pattern (explicit topic_set!), validate
   (load + 40-pt rubric + set==5), `pytest tests/cases -q`, then commit+push. Repeat on each
   "continue". Tell the user the running total (OA/OT/PSA out of 50) after each set.
6. When cases hit 50/role, start **flashcards** (#15): mirror the cases topic-picker
   (topic + difficulty), then author 250/role as 50 sets×5, KB-grounded, then do #17 verify+ship.
