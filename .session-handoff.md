# SESSION HANDOFF
> Black-box snapshot for a cross-account resume. Written 2026-07-02T06:33Z.
> To resume in the new session: run `/handoff-resume` (or paste this whole file and say
> "resume from this handoff").

## 1. NEXT ACTION (read this first)
**Do nothing until the user chooses how to proceed.** All work is implemented and green
locally but **NOTHING IS PUSHED** (16 commits ahead of `origin/main`, HEAD `ba9ce6f`).
The user was asked a 3-option question (run paid Task 5b + ship / ship OSCE only / hold)
and **dismissed it — meaning "do not proceed, wait for next instruction."** So on resume:
greet, restate the state below in 2-3 lines, and WAIT for the user to pick a direction.
Do NOT push and do NOT fire live Gemini without an explicit new go-ahead.

## 2. SESSION GOAL
Make EyeBot's flashcards AND OSCE content complete and correctly role-assigned. Concretely:
every topic/chapter in **"silly"** (the user's nickname for the ~93-doc Supabase knowledge
base) must be covered by BOTH flashcards and OSCE, for ALL roles. Governing rule discovered
this session: **OA ≡ PSA** (same content, different title only) → one shared "CLINICAL"
track; **OT** is the distinct track. Root cause found: flashcard topics were procedure-only
and missed whole knowledge domains (pharmacology, anatomy, disease, ethics — the user's
trigger: "why no pharmacology flashcards in my OT account?"). OSCE had OA/PSA as two
near-duplicate forked case sets.

## 3. DECISIONS LOCKED (do NOT re-litigate)
- **"silly"** = the ~93-doc KB. Coverage mandate: not a single topic/chapter missed, for
  flashcards AND OSCE, all roles, enumerated at chapter level.
- **OA ≡ PSA** (same content); OT distinct. Two content tracks, not three.
- **Flashcards taxonomy** = shared **FOUNDATIONS** pool (knowledge) + role procedural pool.
  `topics_for(role)` returns FOUNDATIONS + role pool. (User chose "Shared Foundations + role
  procedures".)
- **Disease split** = ~5-6 anatomy/region-based topics (user chose "5 anatomy-based topics";
  implemented as 6: eyelid_lacrimal_orbit, cornea_conjunctiva, uvea_retina, glaucoma,
  neuro_strabismus, systemic_disease).
- **11 FOUNDATIONS topics**: anatomy_physiology, microbiology_infection, pharmacology,
  ocular_emergencies (moved out of CLINICAL), professional_ethics, + the 6 disease regions.
- **Authoring method** = user delegated to me ("anything, you decide") → KB-grounded
  generator tool + verify. THEN user overrode: **"for any gemini calls, leave placeholders
  first"** — so cards are placeholders now; real generation is deferred/gated.
- **OSCE** = "One shared set, served to both" (user's exact choice). Implemented as
  pool-based resolver serving the FULL UNION of clinical cases to both OA & PSA (NO
  deletion — deviation from spec's "dedupe to 50-60", flagged & justified: OA/PSA files are
  complementary scenarios, trimming would lose content).
- **OSCE stays practical/procedural**; knowledge domains exercised within scenarios, mapped
  in the coverage matrix (user was told this; didn't object).
- Flashcard defect priorities (user's multi-select): "missing topics" + "topics on wrong
  track".
- Ship order: flashcards first, then OSCE (both now done locally).

## 4. OPEN QUESTIONS (awaiting user)
The dismissed question — user must pick one (options already on the table):
1. **Run Task 5b now (paid) + ship** — fire live Gemini (~45 calls) to replace all
   placeholder cards with real KB-grounded MCQs, verify, run aurora_assert + station_assert,
   then push all 16 commits.
2. **Ship OSCE only, keep flashcards local** — git surgery (branch/cherry-pick) to push the
   4 OSCE commits (no placeholders) while flashcards placeholders stay local.
3. **Hold everything** — keep all 16 commits local; user reviews first.
Sub-question raised but unanswered: whether lens/cataract disorders deserve a 7th disease
topic (currently folded into `disorders_cornea_conjunctiva` = anterior segment).

## 5. ACTIVE SKILL / WORKFLOW
Was executing `superpowers:executing-plans` (both plans fully executed). Flow already went
brainstorming → writing-plans → executing-plans. The executing-plans "finish" step
(finishing-a-development-branch) was intentionally NOT taken because pushing is blocked by
the placeholders-first rule. Effectively **between skills, waiting on the user.**

## 6. TASK LIST SNAPSHOT
TaskList is EMPTY — progress was tracked inline via plan checkboxes, not the task tool. The
two plans (with checkbox steps) are the source of truth:
- `docs/superpowers/plans/2026-07-02-flashcards-foundations-coverage.md` (Tasks 1-9 DONE
  with placeholders; **Task 5b = deferred gated live Gemini generation, NOT done**).
- `docs/superpowers/plans/2026-07-02-osce-oa-psa-merge.md` (Tasks 1-4 DONE; Task 4 Step 2
  station_assert + Step 3 push NOT done — held).

## 7. WORK PRODUCT SO FAR (16 commits on main, unpushed, HEAD ba9ce6f)
Spec: `docs/superpowers/specs/2026-07-02-silly-content-coverage-design.md`.
FLASHCARDS (placeholders, no Gemini spent):
- `tools/flashcards/flashcard_sets.py`: added FOUNDATIONS pool (11 topics), `pools_for()`,
  `topics_for()` = Foundations + role pool, moved ocular_emergencies out of CLINICAL, added
  4 OT gap-fill topics (aberrometry, lens_meter, retinal_imaging, dr_grading).
- `tools/flashcards/static_cards.py`: lookup helpers rewritten via `_by_diff()` to search
  `pools_for(role)`; added placeholder FOUNDATIONS pool + 4 OT topics (each card has
  `"placeholder": True`, ~12 cards/topic across easy/medium/hard).
- `tools/flashcards/generate_cards.py` (NEW): `build_prompt`, `validate_cards`,
  `placeholder_cards`, `generate_topic` (deferred live path via `gemini_client.ask` with
  json_schema), `emit_python_block`.
- `tools/flashcards/seed_placeholder_cards.py` (NEW): idempotent seeder that inserted the
  placeholders.
- `tools/media/generate_flashcards_topics.py`: added image-subject prompt strings for the
  15 new topics (images themselves deferred; fan uses hue fallback).
- `frontend/src/aurora/components/flashcards/StepTopic.tsx`: fan cards captioned
  "Foundations" vs "Skills" via the existing `sub` field.
- Tests: `tests/flashcards/test_foundations_taxonomy.py`, `test_generate_cards.py` (NEW);
  updated `tests/flashcards/test_flashcard_sets.py`; `tests/content/test_coverage.py` (NEW,
  incl. xfail `test_no_placeholder_cards_remain` tracking that cards are still placeholders).
- `tools/kb/dump_coverage_matrix.py` (NEW) + `docs/notes/silly-coverage-matrix.md` (NEW):
  all 93 silly docs → flashcard topic, 0 unmapped, + OSCE map + decision notes.
OSCE (real, no placeholders):
- `tools/cases/topic_sets.py`: added `case_pool()`, `case_visible()`, unified "CLINICAL"
  SET_LABELS (11 sets = OA∪PSA incl. perioperative + triage_referral) + "CLINICAL" _RULES;
  `resolve_set`/`sets_for`/`label_for` now route through `case_pool`. Old OA/PSA entries
  left dormant.
- `tools/api/routers/cases.py`: both role-filter sites now use `case_visible(role,
  case_role)`; imported it. (Two sites had DIFFERENT indentation — 12-space at line 244,
  8-space at line 343 — watch for that pattern.)
- Tests: `tests/cases/test_pool_visibility.py` (NEW, incl. async API test proving OA & PSA
  get identical case IDs); `tests/content/test_coverage.py` appended
  `test_every_case_set_has_at_least_one_case_per_pool`.
VERIFICATION PASSED: `python -m pytest -q` → **474 passed, 1 xfailed** (~96s; needs >2min,
first run timed out at 2min). `cd frontend && npm run typecheck && npm run build` → clean.

## 8. KEY FILES & REPO FACTS
- Flashcard taxonomy seam: `tools/flashcards/flashcard_sets.py` (`topics_for`, `pools_for`,
  `pool_for_role`, FLASHCARD_TOPICS with pools FOUNDATIONS/CLINICAL/OT).
- Card data + lookups: `tools/flashcards/static_cards.py` (520KB; FLASHCARDS[pool][topic]
  [difficulty]=[cards]; helpers get_topic_cards/topic_card_counts/get_set_cards/
  get_all_cards/set_card_counts all go through `_by_diff`).
- OSCE resolver: `tools/cases/topic_sets.py` + filter sites in
  `tools/api/routers/cases.py:244,343`.
- Cases live as JSON in `cases/` (51 case_oa_*, 50 case_ot_*, 50 case_psa_*). Role field
  = "OA"/"OT"/"PSA". NOT deleted/retagged.
- Provenance guard `tests/cases/test_checklist_provenance.py` must stay green (it did).
- CLAUDE.md: prod-bound; never block event loop; `main` auto-deploys to Render prod; auto
  commit+push to main after tasks; MOCK_MODE when no GEMINI_API_KEY; pytest is Python 3.12.
- DB access works locally: `from tools.kb.supabase_client import get_client`; tables:
  documents(93), chunks(732), checklists(22), images(5973). checklists.checklist_type ∈
  {PSA, OT, logbook} (NO "OA" type — OA shares logbook).

## 9. GOTCHAS & CONSTRAINTS
- **DO NOT PUSH** — 16 commits include flashcard PLACEHOLDER content; pushing ships
  placeholders to prod (forbidden by user rule `feedback_gemini_placeholders_first`). OSCE
  commits sit ON TOP of flashcards commits, so a plain `git push` carries both.
- **DO NOT fire live Gemini** (Task 5b, image gen) without an explicit fresh user
  go-ahead. Placeholders-first is a standing rule now.
- Windows/PowerShell dev box; console is cp1252 — use `PYTHONIOENCODING=utf-8 PYTHONUTF8=1`
  when a script prints non-ASCII (bit me on the coverage-matrix dumper).
- Full `pytest` takes ~96s — set Bash timeout > 120000ms or it dies mid-run.
- git shows CRLF→LF warnings on static_cards.py / matrix .md — harmless.
- `gemini_client` has `ask(...)` (json_mode + response_json_schema), NOT `generate_json`.
- `kb.search` exposes `search(query, top_k)` + `format_context(chunks)`.
- Memory files written this session (in ~/.claude/.../memory/): reference_silly_kb,
  project_role_content_model, feedback_gemini_placeholders_first (+ MEMORY.md index).
- Auto-commit+push is the user's standing rule — but SUPERSEDED here by placeholders-first
  (hold the push). Reconcile: only push once real cards replace placeholders OR user opts
  to ship OSCE-only via git surgery.

## 10. RESUME CHECKLIST
1. Read this file fully.
2. `git log --oneline origin/main..HEAD` — confirm 16 commits, HEAD ba9ce6f, tree clean.
3. Re-read the two plan files in `docs/superpowers/plans/2026-07-02-*` and the spec.
4. Optionally `python -m pytest -q` (expect 474 passed, 1 xfailed; timeout >120s) to confirm
   green state carried over.
5. Tell the user the state in 2-3 lines and present the 3 OPEN-QUESTION options (§4).
6. WAIT for the user's choice. Do not push, do not spend Gemini, until they pick.
7. Then execute their choice (Task 5b live gen is the flashcards-parts/gated path; OSCE is
   push-ready on its own if they pick option 2).
