# SESSION HANDOFF
> Black-box snapshot for a cross-account resume. Written 2026-07-08 16:38 MPST.
> To resume in the new session: run `/handoff-resume` (or paste this whole file and say
> "resume from this handoff").

## 1. NEXT ACTION (read this first)
**Finish committing Task 4** (tile tooling). Its code is ALREADY WRITTEN and sitting
uncommitted in the working tree, and the FULL pytest suite is GREEN (601 passed) with 50
valid placeholder webps. HEAD is still `4aaf0c1` (Task 3). Do this, in order:
1. Confirm the frontend harness is green for Task 4. A prior `bash scripts/start-harness.sh aurora`
   run got through EVERY relevant PASS line (Studio tiles, hero portrait swap, Selena
   everywhere, Leaderboard headshots) but then **wedged on teardown for 77 min** and was
   killed, so it never printed a final summary. The dev server may still be warm on
   `http://127.0.0.1:3000` (was HTTP 200 at snapshot time) — the robust gate is
   `node frontend/tests/aurora_assert.mjs http://127.0.0.1:3000` (NOT the full script; see
   Gotchas). If the server is dead, rebuild+serve per `project_harness_local_server` memory,
   or since Task 4 changed NO frontend source (only static webp assets + backend Python),
   you may treat the prior full PASS sweep as sufficient and note it.
2. Commit + push Task 4:
   ```
   git add tools/avatar/tiles.py tools/avatar/generate_tiles.py tools/avatar/make_tile_placeholders.py tools/avatar/portrait.py tests/avatar/test_tiles.py frontend/public/avatar/tiles
   git commit -m "feat(avatar): tile registry + keyless placeholder art + tile-file mandate (paid generator gated)"
   git push origin main
   ```
3. Then run the two-stage review on Task 4 (spec-compliance sonnet → code-quality opus)
   per the active workflow, fix any findings via the implementer, mark Task 4 complete.
4. Proceed to Task 5 (Studio loadout builder) — full text is in the plan file, lines ~904-991.

## 2. SESSION GOAL
Execute the 11-task TDD plan `docs/superpowers/plans/2026-07-08-selena-seamless-custom.md`
via **superpowers:subagent-driven-development**. It replaces the user-rejected per-student
"custom Selena" avatar (flat vector stickers composited over a raster — "extremely ugly")
with ONE transparent AI-rendered whole-look portrait per student, shown+animated on every
surface, plus a much bigger/funnier option catalog and real AI option tiles in a "loadout"
Studio. Tasks 1-9 are code (auto-execute continuously); Tasks 10-11 are PAID live-Gemini and
are GATED on explicit user go-ahead.

## 3. DECISIONS LOCKED
- **Execution mode: Subagent-driven** (user picked "Subagent-driven (Recommended)"). Per task:
  implementer subagent → spec-compliance reviewer (sonnet) → code-quality reviewer (opus) →
  fix loops routed back to the SAME implementer via SendMessage → verified commit on `main` →
  next task. Continuous, NO check-ins between tasks.
- **Model selection:** implementer=sonnet (standard multi-file), spec-reviewer=sonnet,
  quality-reviewer=opus; bump to opus only for architecture/judgment.
- Four locked design decisions from brainstorming:
  - (D-a) greeting-card animation = living CSS motion on ONE render per look (no 2nd pose frame).
  - (D-b) Studio option tiles = real AI renders (placeholders first, paid later).
  - (D-c) way more real AI tiles + "ridiculous and funny variety of every component".
  - (D-d) "loadout" hero — hero only ever shows real rendered art; picks dock as tiles; Save
    fuses a new render. The user-facing "✨ Fusing your look…" copy is LOCKED (don't reword).
- **Tasks 10-11 are PAID and require explicit user go-ahead** before any live Gemini call
  (~103-tile art batch ≈ $1-2.50, then an end-to-end look smoke). Never fire live gen otherwise.
- Auto-ship policy: after each green+verified task, stage ONLY that task's files, commit,
  push directly to `main` (no feature branch). Never push red.

## 4. OPEN QUESTIONS
None outstanding for Tasks 4-9 — the plan is fully specified. The only pending user gate is
the **go-ahead for the PAID Tasks 10-11** (do not start them without it).

## 5. ACTIVE SKILL / WORKFLOW
**superpowers:subagent-driven-development** (mid-flight, executing the plan task-by-task).
Prompt templates live under
`C:\Users\caleb\.claude\plugins\cache\claude-plugins-official\superpowers\5.1.0\skills\subagent-driven-development\`
(`implementer-prompt.md`, `spec-reviewer-prompt.md`, `code-quality-reviewer-prompt.md`) and
`.../requesting-code-review/code-reviewer.md`. Current position: **Task 4 implemented +
pytest-green, NOT yet committed, reviews NOT yet run.** Remaining workflow steps: commit Task 4
→ spec review → quality review → fix loop → mark complete → Tasks 5,6,7,8,9 each the same cycle
→ final whole-implementation code review → superpowers:finishing-a-development-branch → update
memory (project_ricoe_v2 + a new memory for this brief).

## 6. TASK LIST SNAPSHOT
Rebuild with TaskCreate/TaskUpdate exactly:
- #1 [completed] Task 1: Promote keying to tools/shared/keying.py
- #2 [completed] Task 2: Portrait v2 — green-screen render → transparent webp
- #3 [completed] Task 3: Selena v3 raster-only — delete compositor, migrate consumers
- #4 [in_progress] Task 4: Tile tooling — placeholders + tile-file mandate  ← code done, uncommitted, pytest green
- #5 [pending] Task 5: Studio loadout builder
- #6 [pending] Task 6: Catalog expansion + phrase coverage gate
- #7 [pending] Task 7: Greeting card living custom Selena + self-heal
- #8 [pending] Task 8: Leaderboard portraits (bulk, no N+1)
- #9 [pending] Task 9: Design locks + final verification sweep

## 7. WORK PRODUCT SO FAR
Committed this session (all pushed to `main`):
- `946454f`/`a2c1186`/`da98858` — Tasks 1 & 2 (keying promoted to tools/shared; portrait v2:
  green-screen → transparent 512² webp, `_HASH_SALT="portrait:v2"`, `background` removed from
  PORTRAIT_AXES; edge-restricted despill via `ImageFilter.MaxFilter(5)` rim mask; retry
  semantics: falsy data terminal / undecodable bytes retry once; `_MIN_ALPHA_RATIO=0.05`
  checked AFTER key_out, BEFORE normalize_512). Both passed spec+quality review (verdict Yes).
- `c5f6f21` + `4aaf0c1` — Task 3 (raster-only `<Selena>` v3; deleted `renderSelena.ts` +
  `SelenaPortrait.tsx`; new `backdrops.ts` + `tiles.ts`; migrated AtlasRail/Profile/Leaderboard/
  SelenaStudio; slimmed `manifest.ts` (dropped LAYER_ORDER + shape-type unions); `PERSIST_SCHEMA_VERSION`
  3→4; harness selectors svg→`img.selena-img`). Passed spec (✅) + quality (Yes, after 2 doc-only
  review fixes in `4aaf0c1`).

UNCOMMITTED Task 4 work (on disk, pytest-green, NOT yet committed — see git status in §8):
- `tools/avatar/tiles.py` (NEW): `TILE_AXES=["eyeShape","lashes","mouth","glasses","topper","accessory","outfit"]`,
  `tile_ids(axis)` (skips "none"), `tile_path(axis,id)`. `TILES_ROOT` is repo-root-RELATIVE
  (deliberate, well-commented — makes the Python path compare equal to the frontend
  `/avatar/tiles/<axis>/<id>.webp` convention; requires entry points run from repo root).
- `tools/avatar/portrait.py` (MODIFIED): added `PROMPT_MAPS` dict (bodyColor→_BODY … outfit→_OUTFIT)
  + `phrase_for(axis,id)->str|None`. NO new phrase entries were needed — every CURRENT
  (pre-expansion) tile id was already covered by existing D12 maps. (The big phrase batch is Task 6.)
- `tools/avatar/make_tile_placeholders.py` (NEW): keyless PIL placeholder generator — iris.png +
  per-axis hue tint + option-id/"PLACEHOLDER" watermark. Already RAN — wrote 50 webps.
- `tools/avatar/generate_tiles.py` (NEW): PAID Nano-Banana tile generator, MOCK_MODE-refusing,
  `--estimate`/`--generate`/`--install`. NOT run (no live calls). Verified it calls
  `generate_sprites.generate_image_bytes(prompt, model=MODELS["flash"], reference=True)` — signature
  confirmed real.
- `tests/avatar/test_tiles.py` (NEW): 5 tests, ALL PASS — TILE_AXES shape, tile_ids skips none,
  every tile id has a committed webp, path convention, `phrase_for` covers every tile id.
- `frontend/public/avatar/tiles/**` — 50 placeholder webps (all valid, none corrupt): accessory(5),
  eyeShape(7), glasses(8), lashes(3), mouth(7), outfit(9), topper(11).
- Full suite verified: **`python -m pytest -q` → 601 passed** (596 + 5 new).

## 8. KEY FILES & REPO FACTS
- Plan (THE source of truth, full TDD code per task): `docs/superpowers/plans/2026-07-08-selena-seamless-custom.md`
  (1535 lines). Task 4 = lines ~582-900, Task 5 = ~904-991, Task 6 = ~995-1119+ (read on to end),
  Tasks 7-9 = ~1120-1535 (NOT yet read this session — read before dispatching).
- Spec: `docs/superpowers/specs/2026-07-07-selena-seamless-custom-design.md`.
- `git status --porcelain` at snapshot:
  ```
   M tools/avatar/portrait.py
  ?? frontend/public/avatar/
  ?? tests/avatar/test_tiles.py
  ?? tools/avatar/generate_tiles.py
  ?? tools/avatar/make_tile_placeholders.py
  ?? tools/avatar/tiles.py
  ```
  (Plus this `.session-handoff.md`.) HEAD = `4aaf0c1`.
- Ordering constraint (already satisfied): the compositor had to die (Task 3) before the
  catalog can grow (Task 6) or typecheck breaks on the typed `Record<IdUnion,…>` maps.
- Gates: backend `python -m pytest -q` (MOCK_MODE auto-on, keyless — NEVER set GEMINI_API_KEY);
  frontend `cd frontend && npm run typecheck && npm run build`; harness `bash scripts/start-harness.sh aurora`.
- CLAUDE.md + memory `MEMORY.md` govern: WAT architecture, single-worker Render (never block
  the event loop), fail-closed config, JWT identity, PowerShell-tool-vs-Bash-tool discipline
  (bash_guard hook), DB migrations only via /db-migrate, never delete Dockerfile.

## 9. GOTCHAS & CONSTRAINTS
- **HARNESS WEDGE (new, important):** running the FULL `bash scripts/start-harness.sh aurora`
  this session got through the entire assert PASS list but then HUNG on teardown for 77 minutes
  without exiting. Two lessons: (1) do NOT pipe `start-harness.sh` through `| tail` — the pipe
  buffers ALL output until the script exits, so you see nothing and can't tell it wedged;
  (2) the robust gate is to run the assert directly against an already-warm server:
  `node frontend/tests/aurora_assert.mjs http://127.0.0.1:3000` (per `project_harness_local_server`
  memory). If a run wedges, kill ONLY the `scripts/start-harness.sh` bash PID and playwright
  chromium (match `ms-playwright`/`_npx` in CommandLine) — NEVER the user's `chrome.exe`.
- **Task 4 code is un-reviewed.** pytest is green but spec+quality reviews have NOT run. Commit
  it, THEN review (commit-then-review is the workflow order). Do not skip the reviews.
- **PAID gating:** Tasks 10-11 and any `generate_tiles.py --generate` / `generate_sprites`
  live image call require explicit user go-ahead. Placeholders + tests are keyless.
- Auto-commit+push to `main` after each green task; stage ONLY that task's files (tree carries
  unrelated dirty files — do not sweep them in). Never push red.
- `main` auto-deploys to Render prod — verify green before every push.
- Windows/PowerShell box: PowerShell cmdlets → PowerShell tool; POSIX/git → Bash tool. A
  PreToolUse hook blocks mixing them.
- A stale `node .next/standalone/server.js` process can hold a Windows build lock (EBUSY) and
  break `npm run build` — kill it first (seen twice this session: PIDs 30108, 8724-tree).

## 10. RESUME CHECKLIST
1. Read this file top to bottom.
2. Rebuild the task list (§6) with TaskCreate/TaskUpdate (mark #1-3 completed, #4 in_progress).
3. Re-invoke the active skill: superpowers:subagent-driven-development (you are mid-Task-4).
4. `git status` — confirm the uncommitted Task 4 files (§8) are still present; `python -m pytest -q`
   to re-confirm 601 green (fast, keyless).
5. Confirm/obtain harness green for Task 4 (warm-server assert; see §1 + §9). Then commit+push
   Task 4 with the message in §1.
6. Run Task 4 spec review (sonnet) → quality review (opus) → fix loop → mark #4 complete.
7. Continue Tasks 5→9 via the same subagent cycle. Read Tasks 7-9 from the plan first (not yet
   read this session). Do NOT start Tasks 10-11 without explicit user go-ahead.
8. After Task 9: final whole-implementation code review → superpowers:finishing-a-development-branch
   → update memory (`project_ricoe_v2` + a new memory for this seamless-custom brief).
