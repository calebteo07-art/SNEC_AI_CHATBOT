# SESSION HANDOFF
> Black-box snapshot for a cross-account resume. Written 2026-07-06.
> To resume in the new session: run `/handoff-resume` (or paste this whole file and say
> "resume from this handoff").

## 1. NEXT ACTION (read this first)
Everything this session is committed + pushed to `main` (HEAD = `f0d6b8b`) and green — the
working tree is clean. There is **no half-finished code**. The next action is to **ask the
user what to build next** (they were about to choose). Two things are teed up:
- **Outstanding coordination item (belongs to the USER, not code):** apply DB **migration
  008** (`tools/db/migrations/008_leaderboard_visibility.sql`) via the `/db-migrate` skill.
  Until applied, the new D7 leaderboard shows an EMPTY board and `POST /api/leaderboard/prefs`
  returns 503 in prod (degrades gracefully, nothing crashes). Same out-of-band pattern as
  migrations 006/007 which the user applied themselves.
- **Candidate next task the user floated:** add MORE wild customization **options** to Selena
  (new toppers / glasses / eye colours / backgrounds). Bigger than the prompt enrichment already
  shipped — it needs the backend registry (`tools/avatar/parts.py`), the `axes.generated.ts`
  export (regen via `tools/avatar/export_axes.py`), the SVG preview renderer
  (`frontend/src/aurora/avatar/renderSelena.ts`), the portrait prompt phrasing
  (`tools/avatar/portrait.py`), AND the parity test (`tests/avatar/test_axes_parity.py`) all kept
  in sync. Only start if the user confirms.

## 2. SESSION GOAL
Continue the RICOE v2 rebuild for EyeBot (SNEC allied-health training platform), pushing the
Selena avatar system and the leaderboard to a world-class, "ridiculous and eye-catching" finish.
This session delivered: the D12 3D-portrait fix, the D7 leaderboard (with Selena headshots),
"Selena everywhere" (identity chips + generated collectible streak badges), and a prompt
enrichment so every customisation element renders premium/over-the-top. Cadence = "keep shipping
autonomously," auto-commit+push to `main` after each green sub-task.

## 3. DECISIONS LOCKED
- **Standing model rule (user, 2026-07-06):** ALWAYS use `gemini-3.1-flash-image` (flash) for
  every Nano Banana / Gemini image generation. Use `gemini-3-pro-image` (pro) ONLY for
  clinical/medical imagery that needs professional accuracy — and there, Claude picks. Saved in
  memory `feedback_nano_banana_model_choice`.
- **User gave blanket go-ahead to use their GEMINI_API_KEY for flash image gen** this session ("I
  allow you to use my api key", "don't hold back"). NOTE: the auto-mode permission classifier may
  STILL block a paid render as needing per-instance approval — if so, surface it, don't fight it.
- **D7 leaderboard model (locked in RICOE spec):** everyone visible by DEFAULT, opt-out hide
  toggle, optional display name, ranked by XP ONLY (ties stable by name), role filter, Selena
  headshots. This REPLACED the v1 opt-in/supervisor-gated model.
- **Selena placement (respecting `docs/design-locks.md`):**
  - Identity surfaces (Profile avatar, Atlas Rail nav chip, Leaderboard rows) → the student's OWN
    CUSTOM saved Selena.
  - Tutor reply avatar (ricoe A3 lock) + Home greeting (Home lock) → stay the BASE mascot
    (`/brand/iris.png`). Do NOT put a custom avatar there — that would break a lock.
  - Streak tiers → BASE Selena, turned into "very extra / ridiculous / collectible badges students
    collect" (user's explicit direction this session).
- **Selena customization architecture (D12, validated):** NO per-element sprite library — flash
  bakes a whole cohesive look, not separable layer-sprites (D10 pilot finding). Each student's
  COMPLETE customised Selena is generated as ONE portrait on save, cached by config-hash. The
  builder's live preview stays free instant SVG.
- **Streak badges are FIXED static assets** (same six for everyone), committed to
  `frontend/public/brand/badges/`, generated once. NOT per-student.

## 4. OPEN QUESTIONS
- What to build next in RICOE (user was mid-decision). Options already on the table:
  1. Add MORE wild Selena customization **options** (needs registry+SVG+prompt+parity sync — §1).
  2. Move to another RICOE surface (per spec sequencing the remaining frontend surfaces are
     Flashcards "ivory & ink" retheme [risky — it's a locked design] and Branding/surfacing).
  3. Pause.
- (Reminder to raise, not a question) migration 008 needs applying by the user.

## 5. ACTIVE SKILL / WORKFLOW
none — the last unit of work (prompt enrichment) was completed via
`superpowers:test-driven-development` and is fully shipped. No skill mid-flight.

## 6. TASK LIST SNAPSHOT
All tasks completed (nothing to rebuild):
- #1 [completed] Task 2 — render_portrait/store_portrait + upload_avatar + migration 007
- #2 [completed] Task 3 — non-blocking portrait endpoints
- #3 [completed] Task 4 — frontend swap + CSS backdrop
- #4 [completed] Task 5 — bake background + fix JPEG format + opaque card
- #5 [completed] D7-A: migration 008 + backend leaderboard rework
- #6 [completed] D7-C: Leaderboard page + hook + nav
- #7 [completed] D7-D: harness assertion + ship

## 7. WORK PRODUCT SO FAR
All shipped to `main` (clean tree). Commits this session:
- `91b2e95` — Task 5: baked portrait background + JPEG format sniff (revised D12 — flash can't
  emit alpha; asked for transparency it paints an opaque JPEG with a CHECKERBOARD). Portrait is
  now an opaque card; home reverted to static iris.png.
- `0025e05` — D7 leaderboard BACKEND: pure `tools/gamification/leaderboard.py` (`rank_entries`,
  `short_name`), reworked `GET /api/leaderboard?role=` + `POST /api/leaderboard/prefs` in
  `tools/api/routers/student.py`, migration `008_leaderboard_visibility.sql`.
- `6e8fc31` — D7 leaderboard FRONTEND: `/leaderboard` page + `Leaderboard.tsx` + `leaderboard.css`,
  reworked `useLeaderboard.ts`, Atlas Rail nav entry, `PERSIST_SCHEMA_VERSION` 2→3.
- `fdc9bf0` — "Selena everywhere": identity chips (Profile + rail show custom Selena via
  `useAvatar(enabled)`, `[data-selena]` CSS) + collectible streak badges (6 generated JPEGs in
  `public/brand/badges/`, `streakBadges.ts`, `SelenaBadge.tsx`, rewritten `MilestoneLadder.tsx`,
  home.css medallion styles).
- `f0d6b8b` — enriched `config_to_prompt` so every element renders ridiculous/eye-catching
  (contract + per-axis phrasing maximalist; verified live: default look now premium).
- Live PAID flash generations done this session (validation): 6 badges (committed), 3 sample
  student portraits (cosmic-queen/minty-scholar/fire-punk) + 2 post-enrichment samples
  (default + cosmic-queen) — all confirmed no checkerboard, all axes apply, eye-catching. D12
  pipeline validated end-to-end. Samples were disposable scratch (not committed; portraits are
  generated on-demand per student).

## 8. KEY FILES & REPO FACTS
- `CLAUDE.md` — project operating guide (WAT architecture; auto-commit+push to `main`; never block
  the event loop; migrations via `/db-migrate`, never `ADD CONSTRAINT IF NOT EXISTS`; never delete
  the Dockerfile; MOCK_MODE keeps tests keyless).
- `docs/design-locks.md` — READ before touching Tutor/Home/Flashcards/OSCE. Tutor reply avatar =
  base mascot (ricoe A3); Home uses base Iris mascot.
- `docs/superpowers/specs/2026-07-05-ricoe-v2-design.md` — RICOE v2 governing spec (D1–D12 +
  sequencing).
- `tools/avatar/portrait.py` — on-save portrait generator; `config_to_prompt` (now enriched),
  `config_hash`, `render_portrait` (flash, refuses MOCK_MODE), `store_portrait` (sniffs format).
- `tools/avatar/generate_sprites.py` — `generate_image_bytes(prompt, model, reference=True)`;
  `MODELS["flash"]`; anchors to `frontend/public/brand/iris.png`.
- `tools/avatar/parts.py` — server-authoritative avatar axis registry (add options HERE first).
- `frontend/src/aurora/avatar/axes.generated.ts` — exported axis vocab; `renderSelena.ts` must
  handle every option (parity test `tests/avatar/test_axes_parity.py`).
- `tools/gamification/leaderboard.py` (new) — pure D7 ranking core.
- `tools/api/routers/student.py` — leaderboard endpoints (~line 458+).
- `frontend/src/aurora/components/home/{streakBadges.ts,SelenaBadge.tsx,MilestoneLadder.tsx}` —
  collectible badge shelf.
- `frontend/public/brand/badges/{first-light,clear-view,twenty-twenty,eagle-eye,hawkeye,visionary}.jpg`
  — the 6 committed badge images.
- `frontend/tests/aurora_assert.mjs` — Playwright harness (has leaderboard, badge, identity Selena
  assertions).
- Streak tier thresholds (mirror across `streak.py` TIERS + `streakBadges.ts`): 3 First Light,
  5 Clear View, 10 20/20 Vision, 20 Eagle Eye, 30 Hawkeye, 50 Visionary.

## 9. GOTCHAS & CONSTRAINTS
- **Windows / PowerShell dev box.** PowerShell cmdlets → PowerShell tool; POSIX → Bash tool
  (don't mix — a `bash_guard` hook blocks violations). Bash tool cwd can get stuck in `frontend/`;
  use absolute paths.
- **Harness flakiness (IMPORTANT):** `bash scripts/start-harness.sh aurora` REUSES a server on
  :3000 and SKIPS the rebuild — an orphan `node` on :3000 makes it assert a STALE bundle. Before a
  fresh run, KILL :3000 (PowerShell: `Get-NetTCPConnection -LocalPort 3000` → `Stop-Process
  -Force`). Early nav assertions ("routes to /cases", "feature card tap") are timing-flaky and each
  has failed spuriously then passed on a clean restart — one red early nav line ≠ regression;
  restart clean (kill :3000, `SKIP_BUILD=1 bash scripts/start-harness.sh aurora`) and re-read the
  log. Run harness via PowerShell invoking git-bash, redirect to a `.tmp/*.log`, and Read the log
  (PowerShell `Select-Object -Last` buffers until the pipeline ends).
- **Paid image gen:** flash costs real money/quota. User gave broad go-ahead this session, but the
  auto-mode classifier may still block a specific render — if blocked, surface it, don't retry-spam.
  Use `PYTHONIOENCODING=utf-8` when running gen scripts (cp1252 console crashes on unicode like →
  or ✅). flash returns opaque JPEG.
- **Verify green before push** (auto-deploys to Render prod): `python -m pytest -q` (mirror CI,
  Py3.12; local box runs 3.14 — fine) + `cd frontend && npm run typecheck` + the aurora harness.
  Never push red. Stage ONLY task-relevant files (tree often carries unrelated dirty files).
- **`PERSIST_SCHEMA_VERSION`** in `frontend/src/lib/queryClient.ts` is now "3" — bump it on ANY
  persisted-query shape change or returning users white-screen.
- Don't create/overwrite workflow files without explicit user permission.
- Memory dir: `C:\Users\caleb\.claude\projects\C--Users-caleb-OneDrive-Desktop-SNEC-AI-CHATBOT\memory\`
  — `MEMORY.md` is the index; `project_ricoe_v2.md` is the RICOE state of record (already updated
  this session with D7 + Selena-everywhere + prompt-enrichment).

## 10. RESUME CHECKLIST
1. Read this file (and skim `MEMORY.md` + `project_ricoe_v2.md` for full RICOE context).
2. Confirm the tree is clean and HEAD = `f0d6b8b` (`git log --oneline -3`).
3. Task list is all-completed — no rebuild needed unless starting a new tracked effort.
4. Read `docs/design-locks.md` before touching any locked surface.
5. Ask the user which of the §4 options to pursue (esp. "more customization options" vs another
   surface), and REMIND them migration 008 still needs applying via `/db-migrate`.
6. Execute the chosen work with TDD; commit + push to `main` per green sub-task.
