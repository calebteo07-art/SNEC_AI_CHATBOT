# SESSION HANDOFF
> Black-box snapshot for a cross-account resume. Written 2026-07-09 (session #4).
> Context: the 11-task "seamless-custom Selena" plan is FULLY SHIPPED (Tasks 1-11, incl. paid
> art). We then pivoted to a NEW bug the user reported about the Selena customization page.
> To resume in the new session: run `/handoff-resume` (or paste this whole file and say
> "resume from this handoff").

## 1. NEXT ACTION (read this first)
**We are WAITING ON THE USER.** They must: (a) after the Render auto-deploy of `66cc08b`
finishes, SAVE a Selena look in the live app, then (b) read Render → Logs for the JSON line
from logger `eyebot.avatar` = "portrait render failed (hash=…): <reason+traceback>", and (c)
check Render → Environment that `GEMINI_API_KEY` is set AND its Google project has IMAGE
generation enabled (billing on, `gemini-3.1-flash-image` accessible). The user will paste
that log line back. **When they do:** diagnose the exact reason (almost certainly the prod
GEMINI key lacks image-gen). The last question I asked them, still open: *"Want me to proceed
with the preview redesign now anyway, or pause here so you can check the Render key first?"*
— do NOT start the big redesign until they answer. **USER IS NEAR THEIR CLAUDE QUOTA LIMIT
(~1% weekly left)** — be maximally token-frugal: minimal narration, no speculative builds,
no subagents, batch commands.

## 2. SESSION GOAL
The user reported the Selena customization page is broken: (1) selecting an accessory does
NOT update the Selena preview, and (2) after Save, every screen (greeting/leaderboard/
profile) shows the DEFAULT Selena, never their custom one. Goal: root-cause and fix so a
student's saved custom Selena actually renders across the app, and the Studio gives real
feedback when picking options. This is a production bug on the live Render deployment of
EyeBot (SNEC training platform).

## 3. DECISIONS LOCKED
- **Root cause is CONFIRMED (evidence-based, not a guess):** prod portrait renders fail
  100% (the `avatar_images` cache had 4 rows, ALL `status:"failed"`, timestamped at the
  user's save times today). The bucket + `store_portrait` + public URL WORK (I uploaded a
  test webp → HTTP 200 image/webp). The `avatar_images` read/write WORKS. The EXACT
  `_generate_portrait` background path (render→store→upsert→read→fetch) runs GREEN **locally**
  end-to-end (status "ready", URL 200). ⇒ the CODE IS SOUND; the failure is PROD-ENV-specific
  at the live Gemini image call inside `render_portrait`. Do NOT re-investigate whether the
  code/bucket/table works — it does. The break is prod's live image render.
- **Most likely prod cause:** Render's `GEMINI_API_KEY` lacks image-generation access (text
  tutor works, but image gen is a separate capability/billing). Every tile/pose/patient-face
  asset was generated on the DEV box and committed as static files; PORTRAITS are the only
  runtime image-gen path, and it has apparently never worked in prod (oldest failed row
  2026-07-07, when the pipeline shipped).
- **User's answer to my fix-direction question (multi-select): ALL THREE** — (1) add instant
  preview + no silent-default, (2) fix the Render GEMINI key, (3) confirm the exact prod
  error first. Agreed SEQUENCE: confirm error (done: observability shipped) → fix key (user's
  Render action) → then the instant-preview redesign (bigger, needs a brainstorm).
- **Observability fix already SHIPPED** this session as `66cc08b` (see §7).
- Standing project rules still apply (from CLAUDE.md / memory): auto commit+push to `main`
  after each completed task; never block the async event loop; DB migrations only via
  /db-migrate; never delete the Dockerfile; PowerShell-tool vs Bash-tool discipline
  (bash_guard hook); paid Gemini calls need explicit user go-ahead.

## 4. OPEN QUESTIONS
- **Awaiting the user:** the exact prod error line from Render logs + whether Render's
  GEMINI key has image-gen. (Options already on the table for the fix: it's the key/billing;
  or a prod library/Pillow-WebP difference; or Render worker recycling — but "failed" rows
  mean the background task RAN and an exception was caught in-process, so it's a render
  EXCEPTION, most consistent with the image call failing.)
- **Awaiting the user:** "proceed with the preview redesign now, or pause until you've checked
  the Render key?" (I recommended pause-and-check-key first, to conserve their quota.)
- **Design tension to resolve when we do the instant-preview redesign (needs brainstorm):**
  the 103 AI tiles are WHOLE-character renders (a full Selena wearing ONE addition), so they
  CANNOT be composited client-side into a combined look. So "instant preview of the exact
  combined look" fundamentally requires EITHER the paid AI whole-look render (slow ~15-30s /
  flaky) OR a client composite (the vector-sticker approach the user already rejected as
  "ugly", deleted in Task 3). Resolving this is the crux of the redesign. Candidate ideas not
  yet explored with the user: show selected option TILES as a loadout/tray (what exists) +
  clearer "fusing…" state; pre-render popular combos; a lighter preview + AI as the "final";
  non-silent "failed" state = a Retry chip instead of silent default.

## 5. ACTIVE SKILL / WORKFLOW
**superpowers:systematic-debugging** — Phase 1 (root cause) COMPLETE and confirmed with
evidence; Phase 4 (implementation) is BLOCKED on the user's env action (the actual fix is a
Render dashboard change) + their decision on the redesign. The observability step (Phase-1
instrumentation) is shipped. When the redesign starts, FIRST invoke
**superpowers:brainstorming** (it's a creative/design change) before any build, then TDD.
No other skill mid-flight.

## 6. TASK LIST SNAPSHOT
The task list holds ONLY the completed 11-task plan (all done). The new bug work is NOT yet
in the task list — rebuild these as completed for context, then add new tasks for the bug if
useful:
- #1 [completed] Task 1: Promote keying to tools/shared/keying.py
- #2 [completed] Task 2: Portrait v2 — green-screen render → transparent webp
- #3 [completed] Task 3: Selena v3 raster-only — delete compositor, migrate consumers
- #4 [completed] Task 4: Tile tooling — placeholders + tile-file mandate
- #5 [completed] Task 5: Studio loadout builder
- #6 [completed] Task 6: Catalog expansion + phrase coverage gate
- #7 [completed] Task 7: Greeting card living custom Selena + self-heal
- #8 [completed] Task 8: Leaderboard portraits (bulk, no N+1)
- #9 [completed] Task 9: Design locks + final verification sweep
(Paid Tasks 10-11 were also completed this session and shipped as `048de9c` — 103 real AI
tiles installed + render smoke green.)

## 7. WORK PRODUCT SO FAR (this session, after the plan finished)
### Plan completion (all pushed to main)
- `45ad4a9` fix(harness): navCtx onboarded student (Task-7 harness fix — a `customized:false`
  GET tripped CheckInGuard's welcome-Studio gate, redirecting /profile).
- `447920d` Task 8 leaderboard bulk portraits. `d662eaa` Task 9 design-locks.
- `048de9c` Task 10 PAID: 103 real AI Selena tiles generated (`generate_tiles.py --generate`;
  1 flake `mouth/laugh` refilled via `--only mouth`) + installed to
  `frontend/public/avatar/tiles` (webp, 512² RGBA, already keyed transparent at generation).
  Task 11 PAID render smoke passed (moderate + maximalist looks → transparent RGBA 512²).
### The bug investigation (this session's core work)
- Read the whole portrait pipeline (see §8). Ran 3 diagnostics against REAL Supabase (via
  `.env`, which points at the prod project `gkzypxvdxzkcganfxwur.supabase.co`):
  1. `.tmp/diag_portrait.py`: `avatar_images` → 4 rows ALL "failed"; NO "ready" rows; test
     upload of a webp → public URL fetched **200 image/webp** (bucket/store/URL all fine).
  2. `.tmp/diag_generate.py`: ran the exact `_generate_portrait(cfg,hash)` locally for a fresh
     config → `status: ready`, URL fetched 200 image/webp. **Code path is sound locally.**
     (This left a real "ready" row for hash `9f31d788305e670f` = mint/sunset/wizardHat/round,
     and a bucket object `diagtest_maximalist.webp` — harmless test artifacts, can ignore.)
- Conclusion: renders fail ONLY in prod. Audit log (`tools/shared/audit_log.py`) writes to a
  LOCAL `.tmp/audit_log.jsonl` file → lost on Render → failures were invisible.
### SHIPPED FIX (observability) — `66cc08b`
Edited `tools/api/routers/avatar.py`: added `import logging`, a module logger
`_log = logging.getLogger("eyebot.avatar")`, and in `_generate_portrait`'s `except` block a
`_log.exception("portrait render failed (hash=%s): %s", chash, exc)` (goes to stdout →
Render-visible via the JSON root handler in `tools/shared/logging_config.py`) + enriched the
audit `detail` to `f"{type(exc).__name__}: {exc}"`. Backend-only; 57 avatar/portrait tests
green; no harness needed. Pushed to main → auto-deploying to Render.
### Diagnostic scripts (in .tmp/, gitignored, safe to reuse/delete)
`.tmp/diag_portrait.py`, `.tmp/diag_generate.py`, `.tmp/smoke_looks.py`, `.tmp/smoke/*.webp`.
Note: bare scripts must `from tools.shared.gemini_client import ...` (or import
`tools.kb.supabase_client`) BEFORE reading env — that import runs `load_dotenv(.env)`.
`MOCK_MODE` lives in `tools.shared.gemini_client`, NOT `tools.shared.config`.

## 8. KEY FILES & REPO FACTS
- `tools/api/routers/avatar.py` — the avatar endpoints. GET /api/avatar returns
  `{config, axes, portrait_status, portrait_url, customized}`; `_portrait_state(config)` reads
  `get_avatar_image(config_hash(config))`. PUT persists config (sets customized). POST
  /api/avatar/portrait: cache-gated; on miss `upsert_avatar_image(hash,"pending")` +
  `background_tasks.add_task(_generate_portrait, config, hash)`. `_generate_portrait` =
  `render_portrait`(to_thread) → `store_portrait`(to_thread) → `upsert "ready"`; **any
  exception → `upsert "failed"`** (avatar.py ~line 107). THIS is why failures show default.
- `tools/avatar/portrait.py` — `render_portrait(config, model=flash)` (LINE 240): refuses in
  MOCK_MODE; calls `generate_sprites.generate_image_bytes(prompt, model=model)` (NO reference),
  keys green→alpha, `_MIN_ALPHA_RATIO=0.05`, retries once, returns RGBA webp bytes. Raises
  RuntimeError on no-image / undecodable / <5% alpha. `store_portrait(hash,bytes)` (LINE 292)
  → `supabase_client.upload_avatar`. `config_hash` LINE 195 (`_HASH_SALT="portrait:v2"`).
- `tools/kb/supabase_client.py:126` `upload_avatar(path,bytes,content_type)` → uploads to
  PUBLIC bucket `selena-avatars` (upsert=true), returns `get_public_url`. WORKS in prod.
- `tools/shared/db.py` — `get_avatar_image` (368), `get_avatar_images_bulk` (384, Task 8),
  `upsert_avatar_image` (403), `update_profile`. avatar_images table = migration 007 (applied).
- Frontend: `frontend/src/hooks/useAvatar.ts` — `useAvatar` (polls every 4s while
  `portrait_status==="pending"`), `useRequestPortrait` (POST portrait), `useSaveAvatar` (PUT),
  `useSelfHealPortrait` (fires one render/session when customized + status none/failed).
  `frontend/src/aurora/avatar/Selena.tsx` — raster-only `<img src={portraitUrl||"/brand/iris.png"}>`
  (NO client compositing; onError → iris.png). `frontend/src/aurora/screens/Dashboard.tsx:35`
  and `Profile.tsx:42` gate the custom render on `customized && portrait_status==="ready"`.
  `frontend/src/aurora/screens/SelenaStudio.tsx` — the customization page (loadout hero +
  tray tiles; LOCKED copy "✨ Fusing your look…" — don't reword).
- `tools/shared/logging_config.py` — JSON root logger → stdout (Render captures). `MOCK_MODE`
  in `tools/shared/gemini_client.py`.
- CLAUDE.md + memory `MEMORY.md` govern standing rules. The memory
  `project_selena_seamless_custom.md` was updated this session with the full prod-bug
  diagnosis + the "OPEN PROD BUG" section — READ IT.

## 9. GOTCHAS & CONSTRAINTS
- **USER NEAR CLAUDE QUOTA (~1% weekly).** Be extremely token-frugal. Do NOT spawn subagents,
  do NOT run redundant harness/build, do NOT re-investigate the confirmed root cause.
- **The fix for the render is a RENDER DASHBOARD action (the user's), not code.** Don't try to
  "fix" it by editing render code — the code is proven sound. If the user confirms the key is
  the issue and fixes it, existing "failed" `avatar_images` rows won't auto-recover until
  re-saved or self-heal fires (once/session on status none/failed) — may want to note that.
- **Instant-preview redesign is a DESIGN change** — invoke superpowers:brainstorming FIRST;
  it touches the LOCKED "no client compositing" decision (docs/design-locks.md "Custom Selena
  surfaces LOCKED 2026-07-08"). Tiles are whole-character renders → cannot composite a combined
  look. Don't silently rebuild; name the acceptance criterion and get user agreement.
- `.env` points at the PROD Supabase project — diagnostics write to the real bucket/table (the
  test rows/objects left are harmless). Live Gemini image calls COST money (~1-2¢ each) — the
  smoke + diag_generate already spent a few cents with prior user go-ahead; don't fire more
  paid renders without a reason.
- Windows/PowerShell box: PowerShell cmdlets → PowerShell tool; git/npm/pytest/bash → Bash
  tool (bash_guard hook enforces). Running `powershell.exe -Command` inside the Bash tool has
  worked for quick process checks. `git -C "C:\Users\caleb\OneDrive\Desktop\SNEC_AI_CHATBOT"`.
- Harness gotchas (if any frontend work resumes): the `/cases` cold-nav 6s flake is
  double-warming CONTENTION (let ONE warm loop finish); navCtx needs `eyebot_selena_onboarded=1`.
  See memory `project_selena_seamless_custom.md`.

## 10. RESUME CHECKLIST
1. Read this file top-to-bottom. CLAUDE.md + MEMORY.md load automatically; also read memory
   `project_selena_seamless_custom.md` (has the full prod-bug diagnosis).
2. Rebuild the task list (§6) as reference (all 9 completed).
3. Re-orient: the 11-task plan is DONE+shipped; we are debugging a prod bug where custom
   Selenas show the default because portrait renders fail in prod. Root cause is CONFIRMED
   (prod-env render failure; code sound). Observability shipped as `66cc08b`.
4. Re-invoke skill context: superpowers:systematic-debugging (Phase 1 done). Do NOT re-run the
   investigation.
5. Execute the NEXT ACTION (§1): wait for the user's Render log line + key check; when they
   answer the open question, either diagnose the pasted error (likely the GEMINI image key) or
   — if they say go — START the instant-preview redesign by FIRST invoking
   superpowers:brainstorming (respect the quota; keep it lean). Do not re-litigate §3.
