# SESSION HANDOFF
> Black-box snapshot for a cross-account resume. Written 2026-06-13 (evening) — HEAD `b36fb71`.
> To resume in the new session: run `/handoff-resume` (or paste this whole file and say
> "resume from this handoff").

## 1. NEXT ACTION (read this first)
**Latest:** the user approved eye photo **candidate 02**, then asked to **crop it to the eyelid
band** (no brow/face). Done by cropping the existing PNG (896x1200 → 896x565, **no API spend**) —
commit `9701ab8`, pushed (Render auto-deploying). Verified in-panel via Playwright: the wide crop
reads as a bold lids-to-lids close-up filling the right panel (`object-fit: cover`, unchanged CSS).
The original is recoverable from git (`ee9d388:frontend/public/brand/login-eye.png`).
Then (commit `b36fb71`): the login **background** was reworked from flat white to a **soft warm
cream gradient (no white)** that warms toward the right so the eye's faded edge melts into it (+ a
gentle amber glow behind the iris) — see the `.screen-login` root + void-div inline styles in
`OnboardingScreen.tsx`. The **SNEC logo moved up** from the footer into an "EyeBot · by [SNEC]"
lockup under the tagline (`.login-by` in `eyebot.css`); the footer keeps a small attribution line.
**Only open item:** the user said yes to a **live prod smoke-test** — run once Render finishes
building `9701ab8`: verify the prod login renders (cropped eye + SNEC logo + inputs, no console
errors). NOTE: the authenticated flow (Dashboard→Cases→Tutor) needs real login creds the agent does
NOT have — ask the user to log in / provide a test account, or just verify the public login surface
(the inner screens are already proven by `aurora_assert` 17/17 + local screenshots). The full AURORA
redesign is shipped; no other pending work.

## 2. SESSION GOAL
Post-Phase-9 **polish of the AURORA login screen** for the IELA award (deadline 22 Jun
2026). The full AURORA redesign (Phases 0–9) was already shipped before this session. This
session was a series of user-driven refinements to make the login feel real and on-brand:
transparent SNEC co-brand logo, a believable (non-AI-looking) eye graphic, and legible input
text. The headline deliverable: the login "eye that watches you" is now a **real macro
photograph of a human eye** (not a procedural shader), kept alive with lightweight motion.

## 3. DECISIONS LOCKED
- **Login otherwise unchanged.** Earlier the user said "leave the login, I like the moving
  eye," then explicitly approved iterating the eye itself. Everything else on the login
  (layout, copy, card, flow) stays as-is.
- **SNEC logo = transparent PNG, no white box.** Shipped in commit `bfe8be8`. The white
  background is keyed out by border flood-fill (`tools/media/strip_logo_bg.py`, preserves the
  white inside the blue eye icon). Login footer points at `/brand/snec-logo.png`, `mixBlendMode`
  hack removed.
- **Input text color = `#1F1F1F`** (was `#fff` → invisible on the light card). `.login-input`
  in `frontend/src/styles/eyebot.css`. Verified computed `rgb(31,31,31)`.
- **Left-side eye DELETED.** The `HiggsfieldLoop` (`left: 0`) sat behind the login card and was
  never visible → removed + import dropped (commit `d08b73c`).
- **Eye colour = natural brown** — the user's directive was "natural existing scientific
  backed eye colour"; brown is the most common human iris (~79% worldwide). First done as a
  shader recolour (commit `d08b73c`), then SUPERSEDED (see next bullet).
- **Right-side eye = REAL PHOTO.** After the recoloured shader, the user said it "still looks
  very fake and ai-generated" and suggested "add partial human face." Decision: replace the
  WebGL shader entirely with a generated **photorealistic macro eye in a partial face**.
- **Image generation via Nano Banana Pro APPROVED by the user** ("you can use gemini nano
  banana api key to generate"). Generated **3 candidates at 3:4 portrait**; chose **candidate
  02** (richest iris detail, real catchlight, cleanest sclera, best partial-face crop). The
  other two candidates were deleted; only `login-eye.png` remains.
- **Motion preserved without three.js.** EyeHero reproduces the "living eye" feel with CSS
  transforms: gaze-follows-cursor parallax (exponential lag + idle wander), slow breathing
  zoom, focus-lean on input focus, per-keystroke saccade, and the charcoal "engulf" flood on
  login success. The `GazeHandle` interface is unchanged so existing wiring still works.
- **Standing rules:** auto commit + push after each meaningful task; **INLINE execution (no
  subagents)**; don't create/overwrite workflow files without explicit permission.

## 4. OPEN QUESTIONS
- **Eye photo approval.** Is candidate 02 the keeper, or does the user want a different look
  (different candidate, gaze direction, ethnicity, tighter/looser crop, 1:1 vs 3:4)? Options
  already on the table: regenerate with `python tools/media/generate_login_eye.py --count N`
  (edit `PROMPT`/`--aspect` in that script first). PAID but pre-approved for this work.
- **Live prod smoke-test** of `ee9d388` once Render finishes the rebuild (login → Dashboard →
  Cases → Tutor). Render auto-deploys `main`; rebuild takes a few minutes.

## 5. ACTIVE SKILL / WORKFLOW
none. (`handoff-resume` ran at the start of the broader arc; it is complete. No skill is
mid-flight.)

## 6. TASK LIST SNAPSHOT
The tracked task list is the original 10-phase AURORA plan — **all completed**. This
session's login polish was ad-hoc post-completion refinement and was NOT tracked as tasks.
Verbatim:
- #1  [completed] Phase 0 — Foundation: tokens, fonts, Spark Eye logo, MotionSurface, glyphs
- #2  [completed] Phase 1 — Shell rebuild + legacy FX teardown
- #3  [completed] Phase 2 — Dashboard (judge-facing #1)
- #4  [completed] Phase 3 — Cases + Atlas Map (judge-facing #2, centrepiece)
- #5  [completed] Phase 4 — Tutor chat (judge-facing #3)
- #6  [completed] Phase 5 — Progress (judge-facing #4) + Summary
- #7  [completed] Phase 6 — Remaining screens (Flashcards, Check-in, Profile, Admin×4, Supervisor)
- #8  [completed] Phase 7 — Login reconciliation (USER-GATED)
- #9  [completed] Phase 8 — Realistic imagery pass (Nano Banana Pro — PAID, USER-GATED)
- #10 [completed] Phase 9 — Hardening, a11y, performance, cleanup, deploy

## 7. WORK PRODUCT SO FAR
**Everything this session is committed + pushed.** Three commits, in order:
- `bfe8be8` — transparent SNEC logo (`snec-logo.png` + `tools/media/strip_logo_bg.py` + login
  footer img swap).
- `d08b73c` — natural brown shader iris + delete HiggsfieldLoop + input text `#1F1F1F`.
  (The brown-shader part is now superseded by the photo; the input-text + HiggsfieldLoop parts
  remain in effect.)
- `ee9d388` — **photographic eye hero** (current state): new `login-eye.png` (candidate 02),
  new `EyeHero.tsx`, new `generate_login_eye.py`, OnboardingScreen rewired to EyeHero, CSS
  added, GazeIris.tsx + gazeShaders.ts deleted.
Verification done before handoff: `npm run typecheck` clean; `npm run build` clean (16
routes, "✓ Compiled successfully"); live dev (localhost:3000) confirmed via Playwright — photo
renders, parallax transform updates with cursor (`translate3d(8.7,10.8)` → `translate3d(-8.7,
-8.2)`), input color `rgb(31,31,31)`, engulf overlay present. No uncommitted feature work
remains.

## 8. KEY FILES & REPO FACTS
- `frontend/src/screens/EyeHero.tsx` (NEW) — the photo hero. forwardRef<GazeHandle>;
  `SRC = "/brand/login-eye.png"`; RAF loop applies parallax+breathing to the `<img>`;
  `expandPupil()` toggles `.login-eye-engulf.is-on`. Reduced-motion safe (matchMedia +
  `data-motion="reduce"`).
- `frontend/src/screens/OnboardingScreen.tsx` (MOD) — imports `{ EyeHero, type GazeHandle }`
  from `./EyeHero` (replaced the lazy `@/fx/canvas/GazeIris` import + `Suspense`); `<EyeHero
  ref={gazeRef} />` in render; card inline style got `zIndex: 2`; void fallback radial warmed
  to amber; SNEC footer img = `/brand/snec-logo.png`. `completeLogin` still calls
  `gazeRef.current?.expandPupil()` (line ~174).
- `frontend/src/styles/eyebot.css` (MOD) — `.login-eye` / `.login-eye img` / `.login-eye-engulf`
  + `@media (max-width:720px)` ambient fallback + reduced-motion rule, inserted just before
  `.login-card`. `.login-input { color:#1F1F1F }` (was `#fff`).
- `frontend/public/brand/login-eye.png` (NEW) — chosen macro eye photo (candidate 02), 3:4, ~655KB.
- `frontend/public/brand/snec-logo.png` (NEW) — transparent SNEC logo. `snec-logo.jpg` (orig,
  white bg) kept as the source for the strip tool.
- `tools/media/generate_login_eye.py` (NEW) — login-hero generator. `PROMPT` engineered for
  photoreal partial-face brown eye on pale bg; `--count`, `--aspect` (default 3:4); writes
  `login-eye-NN.png` to `frontend/public/brand/`. Uses `gemini-3-pro-image`.
- `tools/media/strip_logo_bg.py` (committed `bfe8be8`) — flood-fill white-bg remover for the logo.
- `tools/media/generate_accents.py` / `tools/media/prompts.py` — the shell-plate raster
  pipeline (dark-field 16:9, dashboard/cases/case-session/flashcards). NOT used for the login.
- DELETED: `frontend/src/fx/canvas/GazeIris.tsx`, `frontend/src/fx/canvas/gazeShaders.ts`.
- Repo facts: Next 16.2.1 (App Router, Turbopack, React 19.2, TS strict), Tailwind v4. AURORA
  system in `frontend/src/aurora/`. Behavioural test harness `frontend/tests/aurora_assert.mjs`
  (17/17 — targets shell routes, NOT login). Memory: `project_frontend_redesign.md` documents
  the whole AURORA redesign. See `CLAUDE.md` (WAT framework) and `.awwwards_state.md` (older
  PHOTOPIC visual rules — partly stale post-AURORA).

## 9. GOTCHAS & CONSTRAINTS
- **Do not try to edit `GazeIris.tsx` / `gazeShaders.ts` — they are deleted.** The procedural
  iris approach is retired; the login eye is now a photo. The brown-iris recolour (commit
  `d08b73c`) is superseded.
- **Nano Banana Pro is PAID.** Regenerating the eye needs the user's OK (already granted for
  this round). `GEMINI_API_KEY` is present in `.env` (39 chars); `NB_MODEL` default
  `gemini-3-pro-image`. Run: `python tools/media/generate_login_eye.py --count 3`.
- **`/api/auth/me` 500 in dev is EXPECTED** — there's no FastAPI backend in the dev session;
  it is not a bug introduced by these changes.
- **`frontend/next-env.d.ts` flip-flops** between `.next/dev/types/...` (dev) and
  `.next/types/...` (build). It's auto-managed noise — do NOT commit it (`git checkout --` it).
- **Dev server may already be running on port 3000** (the user's, PID seen as 28216). A second
  `npm run dev` will grab 3001 or defer. Turbopack hot-reloads edits.
- **Playwright MCP is available** for visual verification (load via ToolSearch: `select:`
  the browser_* tools). Screenshots land in repo root + `.playwright-mcp/` — **delete them
  before committing** (don't leave throwaway PNGs in the tree).
- INLINE execution only (no subagents). Auto commit + push after meaningful work. Don't
  overwrite workflows without permission.
- Login layout: card is on the LEFT (flex-start), eye fills the RIGHT via `.login-eye`
  (`width:min(54vw,780px)`), left edge masked into the paper. On ≤720px the eye demotes to a
  faint centred backdrop.

## 10. RESUME CHECKLIST
1. Read this file fully (+ `CLAUDE.md`; skim `project_frontend_redesign.md` memory).
2. `git log --oneline -5` — confirm HEAD is `ee9d388` (or later) and check Render deployed it.
3. Rebuild the task list if desired (10 phases, all completed — section 6). The login polish
   is untracked ad-hoc work; no new tasks are strictly needed.
4. Give the user a 4–6 line re-orientation: photo eye hero is shipped (`ee9d388`), what changed,
   and the two open questions (section 4).
5. Ask the user the section-4 questions (eye-photo approval + whether to smoke-test prod).
   Do NOT re-litigate section 3. Execute only what they confirm.
