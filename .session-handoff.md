# SESSION HANDOFF
> Black-box snapshot for a cross-account resume. Written 2026-06-15 (Socratic-tutor redesign, mid-brainstorm).
> To resume in the new session: run `/handoff-resume` (or paste this whole file and say
> "resume from this handoff").

## 1. NEXT ACTION (read this first)
We are mid-**brainstorm** for redesigning the **Socratic Tutor** (the student chat at
`/chat`). The previous turn presented an **Instagram-DM-style preview** (via show_widget)
and asked the user to APPROVE it. **The user has NOT yet approved** — they invoked
`/handoff` instead. So the moment you load: **re-show / confirm the Instagram-DM tutor
design and get the user's approval** (including the one sub-nuance: keep a faint lavender
tint on the 💭 "think" bubble, or make it 100% pure-IG grey). On approval → write the spec
(`docs/superpowers/specs/2026-06-15-socratic-tutor-redesign-design.md`), then `writing-plans`,
then implement. **Nothing for this tutor task is coded yet — design phase only.**

## 2. SESSION GOAL
Redesign the EyeBot **Socratic Tutor** (student `/chat`) per the user's complaints:
(1) make it **full-screen immersive**, (2) **format outputs Socratic-part-first then the
rest**, (3) fix **plain/boring colour & design** → make it look like **default Instagram DM**,
(4) replace **boring fonts** (IG uses a plain system sans — so DROP the serif idea),
(5) answers are **long-winded** → make concise, and (6) make the tutor feel like a
**close friend** (warm, casual). This touches BOTH the frontend (layout/skin/rendering) AND
the backend system prompt (persona/voice/format). Part of the IELA 2026 award polish.

## 3. DECISIONS LOCKED
- **Full screen = Immersive, hide the rail** (user picked it). On `/chat` the Atlas Rail
  collapses; chat fills the whole viewport; small `‹ Back` chip + EyeBot avatar/name top-left;
  comfortable centered message column; composer pinned to the bottom edge.
- **Output format = Two styled parts** (user picked it). Every teaching reply leads with a
  warm reflective question/nudge block FIRST, then the concise answer/explanation below.
- **Visual skin = Instagram DM** (user's latest message; they called the first proposal's
  warm peach/lavender gradient + Instrument Serif **"too ugly"**). So: clean white thread,
  **grey received bubbles** from EyeBot, **Instagram blue→purple→pink gradient** on the
  student's *sent* bubbles, **plain system sans (NO serif)**, IG-style header (back chevron,
  gradient-ring circular avatar, "eyebot · Active now", phone/video icons) + IG composer
  (camera circle, "Message…" placeholder, mic/photo/emoji icons).
- **Voice = close friend**: warm, casual, encouraging, uses the student's first name,
  genuinely concise (a few sentences, not a lecture); DROP the current stiff "banned filler"
  rules; KEEP clinical accuracy, KB/RAG grounding, guardrails, role focus, and the
  "answer within ~2 nudges" discipline so it's not long-winded.
- **Two-part rendering mechanism**: backend prompt makes the AI lead each reply with the
  reflective question prefixed `💭 `, a blank line, then the answer. Frontend parses the
  `💭` lead → renders a "think" bubble above the "answer" bubble. Degrades gracefully (no
  `💭` ⇒ render as one normal answer bubble). Works incrementally during SSE streaming.

## 4. OPEN QUESTIONS
- **Approve the Instagram-DM design?** (the single pending gate). Sub-nuance offered:
  💭 think bubble = faint lavender tint (as previewed) **vs** 100% pure-IG plain grey
  (rely only on the "let's think it through 💭" label to distinguish). Also offered minor
  tweaks the user may want: the "Active now" status line, the name styling, bubble roundness.

## 5. ACTIVE SKILL / WORKFLOW
**superpowers:brainstorming**, at **step 5 (present design → get user approval)** — iterating
on the visual after the user rejected v1 (warm gradient) and asked for Instagram DM (v2,
just shown, awaiting approval). Remaining brainstorming steps after approval: 6 write spec
doc + commit, 7 spec self-review, 8 user reviews spec (NOTE: this session the user has
consistently waived the separate spec-review stop and said "just implement" — offer but
don't block), 9 invoke **superpowers:writing-plans** → then **superpowers:executing-plans**
to build it. (Terminal state of brainstorming = invoke writing-plans; do NOT jump straight
to an implementation skill.)

## 6. TASK LIST SNAPSHOT
Rebuild with TaskCreate/TaskUpdate. #1–#21 are PRIOR completed work this session (dark
console, legibility pass, motion system — all shipped). The tutor task is #22–#24:
- #1–#21 [completed] (staff console + legibility + full student-app motion system; all pushed)
- #22 [completed] Explore tutor context (prompt + layout + CSS)
- #23 [in_progress] Clarify Socratic-first format + answer philosophy (both Qs answered §3)
- #24 [pending] Present design + get approval  ← we are here (IG-DM preview shown, awaiting yes)
New tasks to add once approved: write spec; writing-plans; backend persona rewrite;
AppShell immersive `/chat` branch; Tutor header+two-part parse; MessageBubble two-part IG
render; Composer IG style; aurora.css IG skin; verify (tsc/build/sweep/reduced-motion) + commit.

## 7. WORK PRODUCT SO FAR (tutor task — not yet in any file)
- **Two design previews shown via `show_widget`** (visualize MCP), not committed anywhere:
  - v1 = warm peach→lavender gradient field + Instrument Serif "think" block → **REJECTED**
    by user ("color is too ugly").
  - v2 = **Instagram DM** look (CURRENT proposal, awaiting approval): white thread; IG
    gradient-ring avatar; "eyebot · Active now" + phone/video; student sent bubble =
    `linear-gradient(135deg,#5B51D8,#833AB4,#C13584)` white text; EyeBot received = grey
    (`var(--color-background-secondary)`); 💭 think bubble = faint lavender
    `rgba(120,110,220,0.09)` + "let's think it through 💭" tag, stacked above the grey answer
    bubble; IG composer (camera circle `linear-gradient(135deg,#4F5BD5,#C13584)`, "Message…",
    mic/photo/emoji). Sample copy demonstrated the warm, concise two-part voice.
- **Planned implementation (designed, not built):**
  - **Backend** `tools/api/shared.py` `_TUTOR_BASE` (lines 27–47): rewrite persona →
    close-friend voice, concise, two-part `💭`-led format; relax the banned-filler block;
    keep guardrails/KB/role/2-nudge discipline. Optionally surface the student's FIRST NAME
    into `_student_context_block` (currently injects role/streak/weak-topics but NOT name —
    check `get_profile` shape before relying on a name field).
  - **Frontend**: `AppShell.tsx` add an immersive `/chat` branch (no AtlasRail, no mesh,
    full-bleed) keyed off `usePathname()`; `Tutor.tsx` add IG header (Back→dashboard, avatar,
    "Active now") + parse the `💭` two-part reply, KEEP SSE streaming verbatim;
    `MessageBubble.tsx` render eyebot replies as think-bubble + answer-bubble (IG styling);
    `Composer.tsx` IG-style input row; `aurora.css` (~lines 586–624) swap the lavender wash
    + remove serif → IG-DM skin (white thread, grey received, IG-gradient sent, IG header/
    composer). Keep reduced-motion safety (motion.css already styles `.aurora-msg`/typing).

## 8. KEY FILES & REPO FACTS
Working dir `C:\Users\caleb\OneDrive\Desktop\SNEC_AI_CHATBOT` (Windows/PowerShell). Branch
`main`. Frontend = Next 16 in `frontend/`; backend = FastAPI in `tools/`.
- `tools/api/routers/chat.py` — `/api/chat` SSE endpoint; builds `system_prompt =
  tutor_system(role) + RAG`. **Do NOT change the SSE contract / streaming.**
- `tools/api/shared.py:27` `_TUTOR_BASE`, `:68` `tutor_system(role)`, `:76`
  `_student_context_block()`. **EDIT `_TUTOR_BASE`** (the Socratic persona).
- `frontend/src/aurora/screens/Tutor.tsx` — chat screen: header + `ChatThread` +
  `MessageBubble` + `Composer` + `FollowupChip` + verbatim SSE `sendMessage`. **EDIT.**
- `frontend/src/aurora/components/MessageBubble.tsx` — bubble render (role eyebot/user,
  `streaming` caret). **EDIT for two-part.**
- `frontend/src/aurora/components/Composer.tsx` — input row (attach + textarea + gradient
  send). **EDIT for IG style.**
- `frontend/src/aurora/components/ChatThread.tsx` — forwardRef scroll wrapper.
- `frontend/src/aurora/AppShell.tsx` — shell; has student branch + staff `.console-dark`
  branch + `RouteReveal` wrap (student only). **EDIT: immersive `/chat`.**
- `frontend/src/aurora/aurora.css` ~586–624 — chat classes (`.aurora-chat*`, `.aurora-msg*`,
  `.aurora-composer*`) + uses `--wash-lavender`. **EDIT for IG skin.**
- `frontend/src/aurora/motion.css` — student motion engine (already added `.aurora-msg`
  bubble-pop, `.aurora-typing` pulse, composer focus glow). Reduced-motion reset at bottom.
- Specs/plans from earlier this session: `docs/superpowers/specs/2026-06-15-student-app-motion-design.md`,
  `docs/superpowers/plans/2026-06-15-student-app-motion.md`.
- Memory index: `C:\Users\caleb\.claude\projects\C--Users-caleb-OneDrive-Desktop-SNEC-AI-CHATBOT\memory\MEMORY.md`
  (see [[project_student_motion_system]] — the MotionProvider gotcha).
- See `.awwwards_state.md` (PHOTOPIC art direction) + `CLAUDE.md` (WAT framework).

## 9. GOTCHAS & CONSTRAINTS
- **MotionProvider is NOT mounted in the AURORA app** → `fx/text/SplitText.tsx` and
  `fx/cursor/Magnetic.tsx` call `useFx()` and **WILL CRASH** if used. Use CSS only.
  `fx/confetti.ts` is standalone/safe. (Saved as memory `project_student_motion_system.md`.)
- **Do NOT change** the `/api/chat` SSE streaming contract; keep input_filter +
  output_validator guardrails, KB/RAG, role focus intact when rewriting the prompt.
- **Auto commit + push after meaningful work** (user standing rule). Commit messages END WITH:
  `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`. Use `git commit -F` with a
  here-doc (PowerShell mangles embedded quotes). Render auto-deploys `main`.
- **`frontend/next-env.d.ts` auto-regenerates** (oscillates `.next/types` ↔ `.next/dev/types`
  between build and dev) — `git checkout --` it; do NOT commit it.
- Windows PowerShell: no `&&` chains; use `;` / `if ($?)`. Verify from `frontend/`:
  `npx tsc --noEmit` then `npm run build` (there is NO `npm run lint` script). Visual check:
  `node tests/visual_sweep.mjs <prefix> http://127.0.0.1:3000` after `npm run dev` (mocks all
  APIs; the `/checkin` `.map` error + 404s are pre-existing harness mock gaps, NOT regressions).
  Reduced-motion check: spin a temp `tests/_reduce_check.mjs` with Playwright
  `newContext({reducedMotion:'reduce'})` + `localStorage eyebot_motion=reduce`, screenshot,
  then DELETE it (don't commit).
- Don't re-theme the staff `.console-dark` console, admin/supervisor, `ChangePasswordModal`,
  or the login screen. Don't overwrite `workflows/` without permission.
- **NEVER commit secrets** (`.env` has GEMINI/SUPABASE/BREVO keys).
- Already shipped & pushed THIS session (all on `main`): dark staff console (`42f3da8`),
  low-vision legibility pass (`b5df8c0`), student-app motion system (`91a6b36` engine +
  `eff5ccf` rollout), docs (`c6b9cf9`, `946342e`). The student app now has an app-wide
  CSS-only motion engine (`motion.css` + `fx/Reveal.tsx` + `hooks/useCountUp.ts`).

## 10. RESUME CHECKLIST
1. Read this file in full + `CLAUDE.md` + `.awwwards_state.md` + memory `MEMORY.md`
   (esp. `project_student_motion_system.md`).
2. `git log -1` / `git status` to confirm the tree is clean (only this handoff committed;
   no tutor code yet).
3. Rebuild the task list (#22 done, #23 in_progress, #24 pending) with TaskCreate/TaskUpdate.
4. Re-invoke **superpowers:brainstorming** (resume at step 5: present design → approval).
5. Re-confirm the **Instagram-DM tutor design** with the user and get approval (incl. the
   think-bubble tint-vs-pure-grey nuance). Re-show via `show_widget` if helpful. Do NOT
   re-litigate §3 (full-screen immersive + two-part format + IG skin + friend voice are LOCKED).
6. On approval → write the spec, then `writing-plans`, then `executing-plans`: backend
   `_TUTOR_BASE` rewrite + AppShell immersive `/chat` + Tutor/MessageBubble two-part IG render
   + Composer IG + aurora.css IG skin → verify (tsc/build/sweep/reduced-motion) → commit + push.
