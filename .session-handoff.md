# SESSION HANDOFF
> Black-box snapshot for a cross-account resume. Written 2026-07-12 (afternoon).
> To resume in the new session: run `/handoff-resume` (or paste this whole file and say
> "resume from this handoff").

## 1. NEXT ACTION (read this first)
**Almost done — one important fix, then ship.** ALL 8 phases of the Lumens overhaul are
IMPLEMENTED + per-phase-reviewed + fixed, LOCAL on `main` (24 commits `1d7662e`..`38dda31`,
**NOT pushed**). The final whole-implementation review found ONE important cross-phase bug to
fix before the push:

- **FIX (important):** `useFlashcardComplete.onSuccess` in `frontend/src/hooks/useFlashcards.ts`
  (~line 117-120) invalidates `["flashcards"]` and `["flashcard-due-count"]` but NOT
  `["progress"]`. Because `["progress"]` has a 5-min staleTime (queryClient default), after
  finishing a deck (the PRIMARY earning surface) the home LumenLadder shows stale
  `coins_earned` AND the reward watcher can't see the new level/lumen tier, so **level-up and
  lumen-badge reward banners don't fire promptly** (up to 5 min / until reload). Fix: add
  `qc.invalidateQueries({ queryKey: ["progress"] });` to `useFlashcardComplete.onSuccess`
  (alongside the two existing invalidations). The sibling `useFlashcardForfeit` already does
  this, so it's a consistency fix.
- **SAME FIX for OSCE (rated minor, same root cause):** `frontend/src/aurora/screens/CaseSession.tsx`
  submit handler (~line 323-346) never invalidates `["progress"]` (it imports no
  `useQueryClient`). Add `import { useQueryClient } from "@tanstack/react-query";`, a
  `const qc = useQueryClient();` hook, and `qc.invalidateQueries({ queryKey: ["progress"] });`
  after `setResult(...)` in the successful-submit try block.
- **Optionally** the 2 cosmetic a11y minors (see §7).

**Then finish Phase 8 Task 27:** (1) re-run the green gate quickly (`cd frontend && npm run
typecheck && npm run build`; the fix is tiny), (2) the aurora harness
already **PASSED 61/61** (incl. the new `[data-testid="lumen-ladder"]` home assertion; NO
failures this run — the previously-noted flashcards D2 back-face RED did not occur). Re-run only
to re-confirm after your fix: `SKIP_BUILD=1 bash scripts/start-harness.sh aurora`. (3) do a light **behavioral verify** (drive
the running app: home Lumens vault renders placeholder medallions, flashcards neon Pause opens
the PauseMenu, Lumens coin+label in HUD/leaderboard), (4) **push to `main`** (`git push origin
main` — project policy auto-push, no need to ask), (5) tell the user migration **009 still needs
Supabase apply** and give them the paste-ready SQL (see §9).

## 2. SESSION GOAL
Ship a "make the whole app feel like a game" overhaul for EyeBot (SNEC allied-health training
platform, Next.js 16 + FastAPI, production/Render). Five deliverables: (a) unify ALL currency
into ONE coin **"Lumens"** with a single gold engraved-iris coin glyph (`<Lumen>`) used
app-wide; (b) flashcards: neon-red **Pause** button + PauseMenu (Resume / Switch deck /
Quit-with-−20-Lumens-penalty); (c) full-screen **image reward banners** for achievements across
tutor/flashcards/OSCE + streak-badge + Lumens-badge + level-up; (d) homepage **Lumens vault**
badge card (LumenLadder) replacing WeekStats; (e) **remove all daily earning caps**. Executed
via subagent-driven development against the plan (below), one implementer per phase + a
multi-lens adversarial review workflow per phase + fix loops.

## 3. DECISIONS LOCKED (settled — do NOT re-litigate)
- Coin = **Lumens**; icon = gold coin, engraved iris/pupil, inline SVG (`<Lumen>` component).
- Quit-a-flashcard-game penalty = forfeit round's unbanked Lumens + flat **−20**; stakes LIVE
  (−20 truly leaves balance, can drop rank); badges protected by monotonic `coins_earned`.
- **Switch deck is penalty-free by design** (only Quit-to-dashboard costs −20). A review flagged
  this as "dodgeable" but the plan settled it — DO NOT change without user say-so.
- Currency architecture = reskin the existing `xp` balance as Lumens in UI (NO DB column rename);
  new lifetime `coins_earned` column for badge permanence.
- Reward detection = client-derived queue watching `/api/progress` deltas + explicit achievement
  enqueues (no new backend unlock write-path).
- WeekStats stats (sessions/best-streak/recall/topics) = DROPPED entirely (accepted tradeoff).
- OSCE in the economy — awards Lumens = `round(score_100 * 2)`.
- Lumens vault tiers (lifetime coins_earned): Spark 250 / Glimmer 1000 / Glow-Up 2500 /
  Floodlight 6000 / Blaze of Glory 12000 / Supernova 25000.
- Art = placeholder-first (keyless, ships green); **PAID nano-banana gen ONLY on explicit user
  go-ahead** — a SEPARATE future step, not done yet.
- Execution = subagent-driven development on `main` directly (no feature branch); push once at the
  very end after full green gate + behavioral verify.

## 4. OPEN QUESTIONS
- **None blocking the code.** One FUTURE user decision (NOT now): running the PAID nano-banana art
  generators (`tools/rewards/generate_lumen_badges.py --generate/--install` and
  `generate_reward_banners.py`) to replace the placeholder medallions/banners with real art.
  Needs explicit go-ahead. Placeholders ship until then.

## 5. ACTIVE SKILL / WORKFLOW
**superpowers:subagent-driven-development** — at the FINAL step. Per-phase loop (implement →
multi-lens adversarial review Workflow → fix loop → commit) is COMPLETE for all 8 phases. The
final whole-implementation review is DONE (findings in §7). Remaining skill steps: apply the
final-review fix (§1), then `superpowers:finishing-a-development-branch` (here: green gate +
behavioral verify + push to main). NOTE: **Ultracode is ON** this session — reviews were run as
multi-agent Workflow tool passes (goal: exhaustive/correct, cost no object); keep doing that for
any remaining substantive verification.

## 6. TASK LIST SNAPSHOT (rebuild with TaskCreate/TaskUpdate)
Tracked at PHASE granularity (one task per plan phase):
- #1 Phase 1 — Backend Lumens foundation (Tasks 1–6): **completed** (was pushed last session).
- #2 Phase 2 — Remove daily earning cap (Task 7): **completed**.
- #3 Phase 3 — Lumens visual identity (Tasks 8–9): **completed**.
- #4 Phase 4 — Flashcards pause/quit (Tasks 10–12): **completed**.
- #5 Phase 5 — Reward banner system (Tasks 13–18): **completed**.
- #6 Phase 6 — Homepage Lumens vault card (Tasks 19–21): **completed**.
- #7 Phase 7 — Art generators + keyless placeholders (Tasks 22–23): **completed**.
- #8 Phase 8 — Design-locks, harness, green gate, verify, PUSH (Tasks 24–27): **in_progress**
  (design-locks amended, harness assertion added, green gate PASSED, final review DONE; REMAINING:
  apply the §1 `["progress"]` fix → re-gate → check/rerun harness → behavioral verify → PUSH →
  coordinate migration 009).

## 7. WORK PRODUCT SO FAR
**Plan (SOURCE OF TRUTH):** `docs/superpowers/plans/2026-07-12-lumens-gamefeel.md` (8 phases,
27 tasks, exact code for each). Spec: `docs/superpowers/specs/2026-07-12-lumens-gamefeel-design.md`.

**The 24 unpushed commits on `main` (origin/main = `21084ff`; HEAD = `38dda31`), oldest→newest:**
`1d7662e` remove daily chat XP cap · `fb7d445` Lumen coin SVG · `5acd5b7` relabel XP/points→Lumens ·
`d8933d5` Lumen coin polish (greeting align/useId/decorative) · `a5e7af5` neon Pause in FlashShell ·
`e2289c3` PauseMenu modal · `fb78c33` pause+quit-forfeit wired · `7f3e52e` gate keyboard advance
while paused (+pause polish) · `6eb107c` reward types+catalog · `4dfa8c9` reward store · `942b203`
grantAchievements · `0cf9461` coins_earned in ProgressData (+PERSIST_SCHEMA_VERSION 4→5) · `efa62fe`
Lumens vault tiers+medallion+ladder · `e7cc879` reward banner queue+watcher · `b3e89fe` fire banners
for flashcards/tutor/OSCE · `114a3f2` remove legacy AchievementToast · `4a116b1` WeekStats→LumenLadder ·
`0b4e1c8` reduced-motion banner + stable dismiss + perfect-deck gating + cleanup · `36c24e7` art
generators · `75f7643` keyless placeholder assets (11 files) · `a97b9df` unify HUD coin to `<Lumen>` ·
`0545c94` design-locks amendments · `0971249` harness lumen-ladder assertion · `38dda31` remove
orphaned `.flash-coin-ico`.

**GREEN GATE (already run, on HEAD `38dda31`):**
- Backend `python -m pytest -q` → **606 passed, 2 failed**. The 2 fails are the KNOWN pre-existing
  unrelated ones in `tests/test_flashcards_topic_prompts.py` (image-prompt module, ancestor commit
  `62ebf12`) — NOT a regression, do not "fix" them. New `tests/api/test_lumens.py` PASSES.
- Frontend `npm run typecheck && npm run build` → **clean**, all 16 routes compiled.
- Harness → **PASSED 61/61** (aurora), incl. the new `[data-testid="lumen-ladder"]` home assertion;
  ZERO failures (the previously-noted D2 back-face RED did not occur this run).

**FINAL WHOLE-IMPLEMENTATION REVIEW (2-lens goal+integration / sweep, + adversarial verify) —
output file:** `C:\Users\caleb\AppData\Local\Temp\claude\...\tasks\wucdi15lu.output`. Verdict:
sweep lens found **ZERO** defects (all dead code removed, no dangling refs, art filenames match UI,
11 placeholders present, generators correct, pytest import surface clean). Deliverables (a),(b),(e)
MET; (c),(d) had the `["progress"]`-invalidation gap (the §1 fix). Confirmed findings:
1. **important** — flashcards `useFlashcardComplete.onSuccess` missing `["progress"]` invalidation (§1).
2. **minor** — OSCE `CaseSession.tsx` submit missing `["progress"]` invalidation (§1, same fix).
3. **minor a11y** — `RewardBanner.tsx:51` `<Lumen size={22} />` sits beside visible "+N Lumens" text
   but is NOT `decorative` → SR double-reads "Lumens". Fix: add `decorative`.
4. **minor a11y** — `McqCard.tsx:153` HUD score coin IS `decorative` but stands beside the raw score
   with NO "Lumens" word (SR hears "Score 1234", no currency). Inconsistent w/ `Payoff.tsx:63`
   (standalone coin left non-decorative). Fix: drop `decorative` here OR add an sr-only "Lumens" —
   pick one, apply consistently.

**Per-phase review workflow outputs (if needed):** phase3 `wa2rlq2aq`, phase4 `ws64m5e6r`, phase5+6
`w5m2uh6rb`, final `wucdi15lu` — all under the same `tasks/` dir.

## 8. KEY FILES & REPO FACTS
- Plan/spec: `docs/superpowers/plans/2026-07-12-lumens-gamefeel.md` (+ `specs/...-design.md`).
- Reward system (NEW): `frontend/src/aurora/rewards/{types,catalog,store,achieve,useRewards,RewardBanner,RewardProvider}.ts(x)`;
  provider mounted in `frontend/src/app/providers.tsx` (wraps children inside `<AuthProvider>`).
- Lumen coin: `frontend/src/aurora/components/Lumen.tsx` (`<Lumen size decorative>`; `decorative` →
  aria-hidden; `useId()` gradient ids). `LumenCount` was deleted (unused).
- Home vault: `frontend/src/aurora/components/home/{lumenBadges.ts,LumenBadge.tsx,LumenLadder.tsx}`;
  Dashboard renders `<LumenLadder current={coinsEarned} />` in `.hm-lower` (WeekStats deleted).
- Flashcards pause: `FlashShell.tsx` (neon Pause when `onPause` set, else Home pill), `PauseMenu.tsx`,
  `Flashcards.tsx` (`paused` state, `quitForfeit`/`switchDeck`), `useFlashcards.ts` (`useFlashcardForfeit`).
  Keyboard advance is gated by a `paused` prop threaded Flashcards→StudyStage→McqCard.
- Progress hook: `frontend/src/hooks/useProgress.ts` (`ProgressData` now has `coins_earned`; query key
  `["progress"]`; **5-min default staleTime** from `frontend/src/lib/queryClient.ts` — the cause of §1).
- Achievement wiring: `Flashcards.tsx` (combo_godlike/first_deck/perfect_deck — perfect_deck gated to
  non-drill), `Tutor.tsx` (first_chat; dead cap-toast removed), `CaseSession.tsx`
  (first_station/station_pass/flawless_station + lumens_awarded). Backend `CaseSubmitResponse` has
  TOP-LEVEL `lumens_awarded:int` and `result:DomainScore{score_100:int,safe:bool,missed_critical:list[str]}`
  — the frontend reads `data.lumens_awarded` and `data.result.missed_critical.length===0` (verified correct).
- Art: `tools/rewards/{__init__,lumen_badge_art,generate_lumen_badges,banner_art,generate_reward_banners,make_reward_placeholders}.py`.
  11 placeholder assets committed in `frontend/public/brand/{lumen-badges/*.jpg,reward-banners/*.webp}`.
  BADGES keys spark/glimmer/glow-up/floodlight/blaze/supernova; BANNERS keys
  achievement-flashcards/-tutor/-osce/level-up/badge-unlock — MATCH the UI paths exactly.
- Design-locks amended: `docs/design-locks.md` (flashcards pause + "only red" reword + coin unification;
  home Lumens vault supersedes WeekStats). Harness: `frontend/tests/aurora_assert.mjs` asserts
  `[data-testid="lumen-ladder"]`.
- Backend (already pushed last session, NOT in the 24 commits): migration `009_lumens.sql`,
  `coins_earned` in `update_profile.py`/`get_profile.py`/`get_progress.py`, `/api/flashcards/forfeit`
  + clamp 500→5000 in `student.py`, `osce_lumens()`+award in `cases.py`.

## 9. GOTCHAS & CONSTRAINTS
- **DO NOT `git push` until the §1 fix + behavioral verify are done.** `main` auto-deploys to Render
  PROD. The 24 commits are green but the §1 reward-banner-timing fix should land first. (This is why
  the handoff commit below is LOCAL-ONLY — pushing it would ship all 24 unverified commits to prod.)
  Project policy: push directly to main, no feature branch, no asking (user policy 2026-06-29) — but
  only after green + verify.
- **Migration 009 still PENDING Supabase apply** (human step). Code degrades gracefully
  (`coins_earned → xp` fallback in get_progress; guarded write in update_profile), so main never boots
  broken without it — the home vault just shows xp-based tiers until it's applied. Paste-ready SQL for
  the Supabase SQL editor (via `/db-migrate`; NEVER paste a file path; NO `ADD CONSTRAINT IF NOT EXISTS`):
  ```sql
  ALTER TABLE student_profiles ADD COLUMN IF NOT EXISTS coins_earned bigint NOT NULL DEFAULT 0;
  UPDATE student_profiles SET coins_earned = xp WHERE coins_earned = 0;
  ALTER TABLE student_profiles ADD CONSTRAINT student_profiles_coins_earned_nonneg CHECK (coins_earned >= 0);
  ```
  After applying, tick it in `tools/db/migrations/APPLIED.md` (currently marked PENDING).
- **Never stage `mandy.md` / `rodtang.md`** — pre-existing unrelated untracked files at repo root.
  Stage explicit paths, never `git add -A`.
- **PAID nano-banana art needs explicit user go-ahead** — placeholders only until then. Never run
  `generate_lumen_badges.py --generate` / `generate_reward_banners.py --generate` without it.
- **Harness:** aurora harness has a KNOWN pre-existing RED at the flashcards D2 back-face assertion
  (unrelated — do NOT chase). All HOME assertions (incl. the new lumen-ladder) pass BEFORE that point.
  Flaky on cold nav; robust recipe = warm server then `node frontend/tests/aurora_assert.mjs
  http://127.0.0.1:3000`, or `SKIP_BUILD=1 bash scripts/start-harness.sh aurora`.
- **Ultracode is ON** — use the Workflow tool + adversarial verification for substantive verification.
- Shell discipline: PowerShell cmdlets → PowerShell tool; POSIX (git/pytest/npm/node) → Bash tool.
  `.claude/hooks/bash_guard.py` blocks violations. Dev box Windows; repo
  `C:/Users/caleb/OneDrive/Desktop/SNEC_AI_CHATBOT`.
- Commit trailer required: `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
- **`piped exit codes lie`**: `pytest ... | tail` reports tail's exit (0). Read the output file for the
  real summary line.

## 10. RESUME CHECKLIST
1. Read this file top to bottom. Also read `CLAUDE.md`.
2. Rebuild the task list (§6) with TaskCreate/TaskUpdate; mark #1–#7 completed, #8 in_progress.
3. Re-invoke **superpowers:subagent-driven-development** (mid-final-step) to reload the workflow.
4. Apply the §1 `["progress"]`-invalidation fix (flashcards `useFlashcardComplete.onSuccess` +
   CaseSession OSCE submit) and, if quick, the 2 a11y minors (§7 items 3–4). Commit (stage only the
   touched files; trailer required).
5. Re-run `cd frontend && npm run typecheck && npm run build` (fix is tiny). Check/rerun the aurora
   harness (§1). Do the light behavioral verify.
6. **Push to `main`** (`git push origin main`) — ships all commits to prod. Then tell the user migration
   009 needs applying and give the §9 SQL.
7. Do NOT run paid nano-banana art gen without asking the user.
