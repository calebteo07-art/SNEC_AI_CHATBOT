# SESSION HANDOFF
> Black-box snapshot for a cross-account resume. Written 2026-07-13 22:55 +0800 (SGT).
> To resume in the new session: run `/handoff-resume` (or paste this whole file and say
> "resume from this handoff").

## 1. NEXT ACTION (read this first)
Execute the **committed implementation plan** at
`docs/superpowers/plans/2026-07-13-trainer-role-analytics.md` **subagent-driven** (the user's
`/handoff` arg was literally "subagent-driven"). On resume: (a) `git fetch origin` and check
`git rev-list --left-right --count origin/main...HEAD` — this session left commits that should
already be pushed, but verify (concurrent sessions force-push `main`); (b) invoke
**superpowers:subagent-driven-development**; (c) give the user a one-line "starting Phase 1,
Task P1.1 — say stop to review the plan first" confirm (the earlier execution-choice question
was *dismissed*, then answered via the `/handoff` arg, so a quick go-ahead is courteous before
mass-editing production code); (d) begin at **Phase 1 → Task P1.1** (guard collapse
`require_supervisor`→`require_staff`). No product code has been written yet — only two design/plan
docs are committed.

## 2. SESSION GOAL
Add a new top-level role **`trainer`** to EyeBot (production SNEC training app). Trainer **and**
admin get the **exact student app** plus two additions: (1) a **loud role/content toggle beside the
Level chip on the homepage top-right** that switches between OA·PSA and OT content, and (2) a
**separate dark, PowerBI-style Analytics page** (sidebar link) for real-time, in-depth per-student +
cohort tracking with a **downloadable per-student report**. Admin (not trainer) can additionally
**add/remove students** on that Analytics page. The **`supervisor` role is removed** and the old dark
admin console is **retired/consolidated** into the new Analytics page. Deliverable = ship the whole
thing to `main` (auto-deploys to Render), foundations-first, never red.

## 3. DECISIONS LOCKED (do NOT re-litigate)
- **Roles collapse to three:** `student` / `trainer` / `admin`. `supervisor` **removed entirely**
  (role + its dark console + `/supervisor` route + `Supervisor.tsx`/`SupervisorDrillDown.tsx`).
- **Trainer ≈ admin = identical to the student app** (light shell, all learning features), + the
  homepage toggle + the Analytics sidebar page. **Only admin-only delta:** add/remove/provision
  students on the Analytics page (trainers cannot).
- **Old dark admin console (Overview/Students/Accounts/Activity): RETIRE & consolidate** into the new
  Analytics page. (User AskUserQuestion answer: "Retire & consolidate".)
- **Analytics depth: DEEP** — build the dashboard AND add **two additive DB migrations**
  (`010_flashcard_attempts`, `011_case_progress_grade`) to unlock per-topic flashcard accuracy + full
  OSCE grade (safety-gate, sub-scores, most-missed steps). (Answer: "Deep (add persistence)".)
- **Student parity: LITERALLY IDENTICAL** — trainer/admin also do the daily check-in, the **mandatory
  first-login Eyecon** gate, and full gamification (XP/streak/**leaderboard, full participant**).
  (Answer: "Literally identical".)
- **Provisioning: BOTH paths** — promote an existing email, or add via the account form's role
  dropdown (OA/OT/PSA/**Trainer**/**Admin**). (Earlier answer: "Both paths".)
- **Report file format: self-contained HTML** (client-side, print-to-PDF, emailable) — agent's call
  per the user delegating format ("i prioritize speed and simple and ease of sharing").
- **Toggle mechanism:** flips the staff member's **own `student_profiles.role`** between `OA` and `OT`
  via existing `PATCH /api/profile/role`; every content + gamification path already derives its pool
  from `profile.role`, so no new content plumbing.
- **Guard model:** rename `require_supervisor`→**`require_staff`** = `{admin, trainer}` (all analytics);
  **`require_admin`** = `{admin}` stays on add/remove/CSV/promote (the one trainer exception).
- **Charts:** bespoke, dependency-free dark **SVG** primitives (project's "custom everything" standard),
  NOT a charting library.
- **Execution approach:** **subagent-driven** (from the `/handoff` arg).

## 4. OPEN QUESTIONS (flagged, defaults chosen — confirm if the user cares)
- **Legacy `supervisor` DB rows:** spec §3.1 defaults to **auto-demote to `trainer` on login** (keep
  analytics, lose provisioning). User was told this; alternatives on the table = map to `admin`, or
  delete. Not explicitly confirmed — proceed with `trainer` unless told otherwise.
- **time-on-task / session-duration analytics:** genuinely **not available** today (the event log is an
  ephemeral, hashed `.tmp` file). Scoped **OUT**; dashboard will label it "unavailable" rather than
  fake it. (Spec §8.3.) A future durable events table would unlock it — not in this plan.
- **Chart library vs bespoke SVG:** defaulted to bespoke SVG; user invited to ask for Recharts instead
  — no objection raised.
- The execution-choice question (subagent / inline / review-first) was **dismissed**, then effectively
  answered by the `/handoff` arg = subagent-driven. A quick user confirm before starting is prudent.

## 5. ACTIVE SKILL / WORKFLOW
`superpowers:writing-plans` is **complete** (plan written, self-reviewed against the spec, committed).
Its terminal step is the execution handoff. The next skill to invoke on resume is
**`superpowers:subagent-driven-development`** (also named as the plan header's REQUIRED SUB-SKILL).
Prior skills this session, in order: `superpowers:brainstorming` (complete) → `superpowers:writing-plans`
(complete). No workflow is mid-flight (the two authoring workflows finished — see §7).

## 6. TASK LIST SNAPSHOT
Harness task list is **empty** (`TaskList` → "No tasks found") — this session drove work through skills,
not `TaskCreate`. **The real task list is the 39 tasks inside the plan doc**, organized as:
Phase 1 (P1.1–P1.x backend role/guards/auth), Phase 2 (migrations + persistence), Phase 3 (frontend
shell/routing consolidation), Phase 4 (homepage toggle), Phase 5 (Analytics dashboard — largest, ~6–9
tasks), Phase 6 (HTML report), Phase 7 (docs/design-lock/rollout/verify). Total = **7 phases, 39 tasks**.
Rebuild via `TaskCreate` from the plan if the executing sub-skill wants harness tracking, otherwise track
by the plan's `- [ ]` checkboxes.

## 7. WORK PRODUCT SO FAR (all committed — nothing uncommitted)
- **Spec (committed `30f452e`):** `docs/superpowers/specs/2026-07-13-trainer-role-analytics-design.md`
  — full design (role model, guard collapse, toggle mechanism, shell consolidation, dashboard,
  report, two migrations, provisioning, tests, rollout, appendix file-touch-list with file:line).
- **Plan (committed `d422794`):** `docs/superpowers/plans/2026-07-13-trainer-role-analytics.md` — 7-phase
  TDD plan, 39 tasks, complete real code grounded in the actual source, no placeholders (self-reviewed).
- **Two read-only authoring workflows already ran** (outputs are ephemeral task files, but their findings
  are fully distilled into the spec/plan — do NOT re-run):
  1. Codebase map (`wf_ddcc8ab3-fdd`): staff shell/routing, admin+supervisor endpoints, existing analytics
     screens, per-student data model, report-export pattern, exhaustive role touch-list.
  2. Plan authoring (`wf_780044c0-29e`): 7 phase-drafters.
- **Key grounded facts baked into the plan:** content pool = `student_profiles.role` (server-derived);
  analytics endpoints already exist under `/api/supervisor/*` + read `/api/admin/*` (re-gate, don't
  rebuild); `sessionExport.ts` is the report pattern to clone; next migration numbers are **010 & 011**
  (dir ends at `009_lumens.sql`, and **009 is still PENDING** in APPLIED.md — `coins_earned` falls back to
  `xp`); no chart lib installed; NO `middleware.ts` (all role gating is client-side).

## 8. KEY FILES & REPO FACTS
Read `CLAUDE.md` (agent operating guide) first. Primary source files the plan edits (see the plan for
exact edits + line numbers):
- Backend: `tools/shared/jwt_utils.py` (guards), `tools/api/routers/auth.py` (role resolution + `/auth/me`),
  `tools/api/routers/admin.py` (re-gate + staff provisioning), `tools/api/routers/supervisor.py`
  (inherits `require_staff`), `tools/api/routers/student.py` (flashcards_complete persistence,
  `/api/profile/role`), `tools/api/routers/cases.py` + `tools/cases/log_case_completion.py` (OSCE grade
  wire-through), `tools/shared/db.py` (new attempt helpers, widen case-result), `tools/profile/*`.
- Migrations: `tools/db/migrations/010_flashcard_attempts.sql`, `011_case_progress_grade.sql`, `APPLIED.md`.
- Frontend: `frontend/src/screens/AuthContext.tsx` (role union), `OnboardingScreen.tsx`, `AdminGuard.tsx`
  → new `AnalyticsGuard.tsx`, `CheckInGuard.tsx`, `frontend/src/aurora/AppShell.tsx`,
  `components/AtlasRail.tsx`, `screens/Dashboard.tsx` (toggle), `aurora.css` (`.aurora-analytics`),
  NEW: `app/(shell)/analytics/page.tsx`, `screens/Analytics.tsx`, `components/analytics/{TrendChart,
  DonutGauge,BarSeries}.tsx`, `components/home/PoolToggle.tsx`, `lib/studentReportExport.ts`,
  `hooks/useAnalytics.ts`.
- **Preserved (do NOT delete):** `screens/AdminAccounts.tsx`, `screens/AdminStudentDetail.tsx`,
  `screens/adminShared.tsx`, `components/{StatCard,EngagementBlock,Heatmap,ProgressBar}.tsx` — reused by
  Phase 5. **Retire:** `ConsoleRail.tsx`, `AdminShell.tsx`, `/admin/*` route group + `AdminOverview/
  AdminStudents/AdminActivity`, `Supervisor.tsx`, `SupervisorDrillDown.tsx`, `/supervisor` route.
- Tests: `tests/shared/test_jwt_utils.py`, `tests/api/test_admin_endpoints.py`, `test_auth_endpoints.py`,
  `tests/api/test_flashcards_complete.py`, `tests/cases/*`; `frontend/tests/` Node harnesses (report + toggle).
- Docs: `docs/ARCHITECTURE.md`, `docs/SECURITY.md`, `docs/design-locks.md` (Phase 7).
Repo facts: single-container Next.js(16)+FastAPI(3.12); JWT in HttpOnly `eyebot_token`; content pools
CLINICAL(OA≡PSA) + OT; commands — `python -m pytest -q`, `cd frontend && npm run typecheck && npm run build`,
`bash scripts/start-harness.sh aurora`.

## 9. GOTCHAS & CONSTRAINTS
- **Production, auto-deploy:** `main` auto-deploys to Render. **Verify green** (pytest + typecheck +
  build + aurora harness) **before every push**. Never ship red.
- **Concurrent sessions force-push `main` and can stash your uncommitted work.** ALWAYS
  `git fetch` + `git rev-list --left-right --count origin/main...main` before pushing. Isolated-ship
  recipe (worktree at `origin/main`) is in memory `project_concurrent_sessions_isolated_ship`.
- **DB migrations (010, 011):** apply via **`/db-migrate`** (never paste a file path into Supabase SQL
  editor; no `ADD CONSTRAINT/CREATE POLICY IF NOT EXISTS`). Code must **degrade gracefully** until applied
  so `main` never boots broken. Ledger both in `APPLIED.md`. **009_lumens is still PENDING** too.
- **User standing rule:** after every completed task, **stage + commit + push to `main`** (no feature
  branch, no asking) — but green-first, and stage only that task's files (tree carries unrelated dirty files).
- **Shell discipline:** PowerShell cmdlets → PowerShell tool; POSIX → Bash tool. `.claude/hooks/bash_guard.py`
  blocks violations. Bash + PowerShell share cwd — no stray `cd`.
- **Live Gemini calls cost real money/quota** — tests/harnesses run keyless in `MOCK_MODE`; don't fire live
  text/image calls without explicit go-ahead.
- **Don't create/overwrite workflow SOPs** (`workflows/`) without explicit permission.
- **Design is locked** (spec + the decisions in §3) — refine within it, don't rebuild; add the
  `docs/design-locks.md` entry in Phase 7. **`SELENA` == `IRIS` == `Eyecon` mascot** (renamed); the toggle
  is content-pool, not mascot.
- **Two docs commits (`30f452e`, `d422794`) + this handoff commit** should be pushed; the working tree is
  otherwise clean (no other uncommitted work to carry).

## 10. RESUME CHECKLIST
1. Read this file end-to-end.
2. Read `CLAUDE.md`, then the spec (`…specs/2026-07-13-trainer-role-analytics-design.md`) and the plan
   (`…plans/2026-07-13-trainer-role-analytics.md`).
3. `git fetch origin`; confirm `origin/main` contains `d422794` (plan) + `30f452e` (design) + the handoff
   commit; if not, push (after the divergence check in §9).
4. Invoke **`superpowers:subagent-driven-development`** (the execution sub-skill).
5. Give the user a one-line confirm: "Resuming subagent-driven at Phase 1 Task P1.1 — say 'review' to read
   the plan first, else I start." Wait for go-ahead.
6. Execute **NEXT ACTION** (§1): Phase 1 → Task P1.1, then proceed task-by-task with review between tasks,
   committing (+push, green-first) per the user's standing rule.
