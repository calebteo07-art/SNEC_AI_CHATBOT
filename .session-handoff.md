# SESSION HANDOFF
> Black-box snapshot for a cross-account resume. Written 2026-07-11.
> To resume in the new session: run `/handoff-resume` (or paste this whole file and say
> "resume from this handoff").

## 1. NEXT ACTION (read this first)
**Debug a NEW bug the user just reported: "when I click on a topic card it shows a blank
screen."** This appeared AFTER the previous fix (`0991eba`) made the topic click actually
navigate. Use `superpowers:systematic-debugging`. **Do NOT trust the aurora harness green
signal here** — it mocks the API and, critically, runs the study screen ONLY under reduced
motion, so it cannot see a full-motion-only blank. **Reproduce in FULL motion** (see §7 for
the exact probe recipe), open the browser console, and get the real error. Primary
hypothesis: the study card (McqCard cockpit chrome, shipped `af3e285`) or the TopicIntro
blanks in full motion — a latent bug users could not reach until the click was fixed.

## 2. SESSION GOAL
Make the live `/flashcards` "Grand Prix" (Mario-Kart) feature fully work + match the mockup.
This session already: (A) fixed "clicking a topic does nothing" by resolving the pick at the
stage (shipped `0991eba`, harness-green). The user is now reporting the very next screen
after a successful click is **blank**. Deliver a working topic-pick → intro → study flow in
FULL motion (the real user path), not just under the harness's reduced-motion path.

## 3. DECISIONS LOCKED
- Flashcards theme is **"Grand Prix" (Mario Kart)** — coverflow kart-grid selection + dark
  graphite cockpit-dashboard flip. Supersedes the old "ivory & ink" D2. (memory:
  `project_flashcards_grand_prix.md`, design-lock in `docs/design-locks.md` line ~41.)
- **Topic pick MUST be stage-resolved, never per-card** (this session's fix, `0991eba`):
  `.fan-card` are `pointer-events:none`; `CardFanCarousel` resolves a tap at `.fan-layout`
  via `onPointerUp` → nearest card by live `getBoundingClientRect` centre-X → `onPick`;
  keyboard Enter still uses the button `onClick`. Never revert to per-card onClick. The
  harness must click a topic in FULL motion (now does, before emulating reduced motion).
- Both faces of the study card stay **dark graphite** (harness luminance ≤ 90 contract).
- 46 topic images already regenerated (flash/pro mix, SG-context, T&C-locked) — DONE, not
  part of this bug. Do not regenerate without explicit go-ahead (paid).
- Git policy: after a completed+verified task, stage only the task's files, commit, and
  **push directly to `main`** (auto-deploys to Render prod — verify green first).

## 4. OPEN QUESTIONS
- **What exactly is "blank"?** Unknown yet — need the repro to confirm: (a) the TopicIntro
  screen ("On the grid" / topic name / "Start your engines"), (b) the study card after
  pressing Begin, or (c) a full white-screen-of-death (unhandled React crash). Options are
  enumerated in §7; the next agent should reproduce rather than ask, then confirm the fix
  behaviourally with the user.

## 5. ACTIVE SKILL / WORKFLOW
`superpowers:systematic-debugging` — re-invoke it for the blank-screen bug. We are at Phase
1 (Root Cause Investigation): reproduce consistently in full motion + read the actual
console error BEFORE proposing any fix. Remaining phases: 2 pattern analysis, 3 single
hypothesis + minimal test, 4 failing test → minimal fix → verify. TDD per CLAUDE.md.

## 6. TASK LIST SNAPSHOT
All previous tasks are completed; create fresh ones for the new bug:
- #1 [completed] Finish Grand Prix visual port (props → ChargeBeat → aurora.css)
- #2 [completed] Verify port: typecheck + build + aurora harness green
- #3 [completed] Regenerate 46 SG clinical topic images (PAID)
- #4 [completed] Ship: update design-locks + memory, commit + push to main
- #5 [completed] Reproduce "topic click does nothing" in full motion
- #6 [completed] Root-cause the swallowed topic click
- #7 [completed] Fix + add full-motion regression coverage  (shipped `0991eba`)
- NEW #8 [pending] Reproduce the blank screen after a topic pick in FULL motion + capture console error
- NEW #9 [pending] Root-cause + fix the blank screen (likely full-motion McqCard/TopicIntro render)
- NEW #10 [pending] Add full-motion regression coverage into study (harness only tests study under reduced motion) + ship

## 7. WORK PRODUCT SO FAR
**Completed + shipped this session (`0991eba`, on `main`):** stage-resolved topic pick.
- `CardFanCarousel.tsx`: cards `pointer-events:none`; new `resolvePick(clientX)` (nearest
  `.fan-card` by rect centre-X → `onPick`), new `onPointerUp` (tap = `!moved` &&
  `<700ms` → resolvePick, else settle), removed the `if (reduced) return` early-out in
  `onPointerDown`, `TAP_SLOP=8`, `onPointerMove` repaints under reduced motion, removed the
  per-frame `pointerEvents` toggling. Button `onClick` now keyboard-only.
- `aurora.css`: `.fan-card { pointer-events: none }`.
- `aurora_assert.mjs`: flashcards pick now a FULL-MOTION `.fan-stage`-centre `mouse.click`
  BEFORE `emulateMedia(reduce)`; the stale-cards test (~line 869) also uses a stage-centre
  click. `locator('[data-card-id]').click()` now FAILS (`.fan-layout intercepts pointer
  events`) — any fan pick in a test must be a stage-coordinate click.
- `docs/design-locks.md` (~line 65) + memory `project_flashcards_grand_prix.md` updated
  with the stage-resolved-pick invariant.
- Verified: typecheck + `next build` + full aurora harness **EXIT 0** (52 checks, incl. the
  new "FULL-MOTION topic tap opens the intro").

**NEW bug analysis (NOT yet reproduced — the harness is blind to it):**
- The pick flow (`Flashcards.tsx`): `onPick(card)` → `onStart(setKey)` →
  `setSetKey; setPickerDone(true); setIntro(true)` → renders the intro branch:
  `if (intro && !fromSession && !reviewMode) return <FlashShell engraved><TopicIntro …/></FlashShell>`.
  Begin (`setIntro(false)`) → `generating||deck.length===0||!card` loading/empty, else
  `<StudyStage>` (→ McqCard cockpit).
- `TopicIntro.tsx` and `FlashShell.tsx` (both read this session) render real content and are
  harness-green + visible. So a blank intro would have to be real-data/runtime specific.
- **Strongest hypothesis:** the study card (`McqCard.tsx`, cockpit chrome from `af3e285`) or
  its flip host blanks in **full motion**. The harness ONLY runs study under
  `emulateMedia({reducedMotion:"reduce"})` (aurora_assert.mjs ~line 288–373), so a
  full-motion-only render/CSS collapse is invisible to it. Precedent: a prior `.flash-lift`
  + `.flash-boost { display:none }` grouping collapsed the whole flip host to a zero-box
  back face (see memory gotcha). A similar full-motion CSS interaction in the new
  HUD/boost-meter/flip could blank the card. The `0991eba` fix simply let users REACH this
  screen in full motion for the first time (the fan click was dead before).
- Secondary hypotheses to check: (b) an unhandled throw → error boundary renders blank
  (check `topicBlurb`/`topicHue` in `types.ts`, or an McqCard prop crash with real API card
  shapes; the `coins`/`position` HUD props from Part A); (c) `fromSession` leftover in
  sessionStorage (`eyebot_session`) skipping the intro into a bad study state; (d) Render
  hadn't finished redeploying `0991eba` when the user tested (transient) — lowest
  likelihood, don't assume it.

**Repro recipe (do this first):**
1. Build+serve the standalone harness (known-good): `bash scripts/start-harness.sh serve`
   (builds → copies `.next/static`+`public` into `.next/standalone` → `node server.js` →
   warms). Wait for `curl -sf http://127.0.0.1:3000/flashcards`. Kill any stale `:3000`
   listener first (EBUSY-on-`.next/standalone` gotcha): PowerShell
   `Get-NetTCPConnection -LocalPort 3000 -State Listen` → `Stop-Process -Force`.
2. Write a Playwright probe in `frontend/tests/` (so `playwright` resolves — bare ESM import
   needs `frontend/node_modules`; do NOT run it from repo root). Mirror aurora_assert.mjs
   setup (studentUser initscript + `eyebot_token` cookie + `route('**/api/**')` catch-all +
   `/api/auth/me` + `/api/flashcards/topics` + `/api/flashcards/generate**` returning ≥2
   cards incl. one `requires_explanation`). Then, WITHOUT emulating reduced motion:
   goto `/flashcards` → wait `[data-testid="flash-fan"]` → `waitForTimeout(700)` →
   `mouse.click` the `.fan-stage` centre → wait `[data-testid="flash-intro"]` →
   click `[data-testid="flash-intro-begin"]` → wait/inspect `[data-testid="study-stage"]`.
   Capture `page.on("console"/"pageerror")` AND assert the study card actually has a
   non-empty rendered box (e.g. `.flash-card` boundingBox height > 0, `.flash-face.is-front`
   visible). This is the coverage the harness is missing. The prior probe
   `frontend/tests/_fullmotion_probe.mjs` was deleted after use — recreate similarly. Delete
   scratch probes when done; fold the permanent check into aurora_assert.mjs.
3. If the study card is blank in full motion: it's the `af3e285` cockpit-chrome CSS/flip —
   inspect `.flash-lift` / `.flash-boost` / `.flash-face` / the HUD in `aurora.css` (~line
   2277+, and the "ACTIVITY — the cockpit dashboard card (flip)" block ~line 2434+) for a
   full-motion `display:none`/zero-height/backface collapse. Compare full vs reduced
   computed styles on `.flash-card`/`.flash-lift`/`.flash-face.is-front`.

## 8. KEY FILES & REPO FACTS
- `frontend/src/aurora/screens/Flashcards.tsx` — orchestrator; render branches
  selection→intro→loading→results→study; `onPick`→`onStart`; `coins`/`position` HUD state.
- `frontend/src/aurora/components/flashcards/CardFanCarousel.tsx` — the fixed coverflow
  (stage-resolved pick). Reference pattern it mirrors:
  `frontend/src/aurora/components/home/FeatureCarousel.tsx` (`openNearest`).
- `frontend/src/aurora/components/flashcards/McqCard.tsx` — **prime suspect** for the blank
  (cockpit chrome: HUD `.flash-hud`, sky-blue `.flash-qhead`, boost `.flash-meter`, flip
  `.flash-lift`/`.flash-boost`). Shipped `af3e285`; only ever harness-tested reduced-motion.
- `frontend/src/aurora/components/flashcards/StudyStage.tsx` — wraps McqCard, passes
  `coins`/`position`.
- `frontend/src/aurora/components/flashcards/TopicIntro.tsx` + `FlashShell.tsx` — read; render
  fine + harness-visible (less likely the culprit, but confirm).
- `frontend/src/aurora/components/flashcards/ChargeBeat.tsx` — transparent tap-through timer.
- `frontend/src/aurora/aurora.css` — Grand Prix block ~line 2277+; cockpit flip block
  ~line 2434+; `.fan-card` at ~line 2384 (now `pointer-events:none`).
- `frontend/tests/aurora_assert.mjs` — the harness; flashcards section ~line 252–425;
  stale-cards ~line 843–882. Runs against a warm `:3000` standalone server.
- Repo facts (CLAUDE.md): Next.js 16 standalone + FastAPI; MOCK_MODE keyless; never fire a
  live Gemini call without go-ahead; `main` auto-deploys to Render; commit only task files
  (tree carries untracked `mandy.md`/`rodtang.md` — LEAVE them). Dev box = Windows/
  PowerShell; PowerShell cmdlets in the PowerShell tool, POSIX in the Bash tool
  (`.claude/hooks/bash_guard.py` enforces). NOTE: a PowerShell `Set-Location frontend`
  drifts the SHARED cwd and breaks the bash_guard relative path — reset with
  `Set-Location C:\Users\caleb\OneDrive\Desktop\SNEC_AI_CHATBOT`; in Bash use a subshell
  `(cd frontend && …)` so cwd doesn't persist.

## 9. GOTCHAS & CONSTRAINTS
- **Harness green ≠ user-working.** It mocks the API and runs study under REDUCED motion, so
  it MISSED both the dead full-motion click (last bug) and (probably) this blank. Always do
  a behavioral verify in FULL motion on the running app.
- Do NOT revert `.fan-card { pointer-events:none }` or the stage-resolved pick (it's the
  fix for the previous bug; locked in design-locks + memory).
- Keep both study faces dark graphite (luminance ≤ 90) — harness D2 assertion.
- Reduced-motion `display:none` must stay OFF `.flash-lift` (past blank-flip cause). Watch
  for the same class of full-motion CSS collapse in the cockpit chrome.
- Standalone `serve` EBUSY: kill the stale `:3000` node before rebuilding.
- Run Playwright probes from `frontend/` (bare ESM import resolution); delete scratch probes
  after; never `next start` (flaky under `output:standalone`).
- Paid image gen only on explicit go-ahead. Don't create/overwrite `workflows/*.md` without
  permission. Secrets only in `.env` (gitignored).
- Memory files live in `C:\Users\caleb\.claude\projects\…\memory\` (NOT committed); update
  `project_flashcards_grand_prix.md` + `MEMORY.md` index when this bug is fixed.

## 10. RESUME CHECKLIST
1. Read this file in full (+ `CLAUDE.md`; `.awwwards_state.md` if present).
2. `git log -1` — confirm `0991eba` is HEAD; `git status` — only `mandy.md`/`rodtang.md`
   untracked (leave them).
3. Rebuild the task list from §6 (mark #1–#7 completed; create #8–#10 pending).
4. Re-invoke `superpowers:systematic-debugging` for the blank-screen bug.
5. Execute NEXT ACTION (§1): build+serve, run the FULL-MOTION probe (§7 recipe) into the
   study screen, capture the console error, root-cause (prime suspect: McqCard cockpit
   chrome full-motion render), fix minimally with a failing test first, verify
   typecheck+build+harness green AND a full-motion behavioral check, then commit + push to
   `main` and update design-locks + memory.
