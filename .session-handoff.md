# SESSION HANDOFF
> Black-box snapshot for a cross-account resume. Written 2026-06-13 (evening) — HEAD `4f3bdc1`.
> To resume in the new session: run `/handoff-resume` (or paste this whole file and say
> "resume from this handoff").

## 1. NEXT ACTION (read this first)
**Phase 6 is IN PROGRESS (executing INLINE).** Done + pushed this session: **Flashcards** + **SNEC
co-brand** (commit `d3f5c32`) and **Profile** + **Daily check-in** (commit `deb38c2`). Profile has
the working **reduced-motion toggle** (writes `localStorage["eyebot_motion"]` + flips
`html[data-motion]`); check-in is the standalone (auth-group, no rail) card; SNEC logo sits in the
Atlas Rail footer app-wide (`frontend/public/brand/snec-logo.jpg`, mix-blend multiply).
`aurora_assert.mjs` now **13/13**; typecheck + build clean.
**REMAINING Phase 6 (NEXT):** **Admin** (`/admin` overview + `/admin/students` + `/admin/accounts`
+ `/admin/activity` — dense tables, cohort heatmap, at-risk list, student drill-down, account mgmt,
activity log; the admin layout `app/(shell)/admin/layout.tsx` uses **AdminGuard, NOT CheckInGuard**;
legacy pieces to replace/port: `AdminLayout.tsx`, `adminShared.tsx`, `AdminOverviewPage.tsx`,
`AdminStudentsPage.tsx`, `AdminAccountsPage.tsx`, `AdminActivityPage.tsx`, `AdminStudentDetail.tsx`,
`AtRiskTable.tsx`, `CohortHeatmap.tsx`, `StudentDrillDown.tsx`) and **Supervisor** (`/supervisor` —
`SupervisorDashboard.tsx`). Rebuild in AURORA (reuse `Heatmap`/`StatCard`/`.aurora-card`; keep the
data hooks + AdminGuard), repoint pages, remove legacy, add assertions, verify, commit + push. THEN
Phase 7 (login: swap to the Spark Eye logo + add the SNEC logo), Phase 8 (PAID Nano Banana imagery —
confirm credits), Phase 9 (hardening). Execute INLINE, no subagents.

## 2. SESSION GOAL
Strip and rebuild the **entire EyeBot frontend** in the **AURORA** design language (light +
Google Gemini gradient as the ONLY chromatic source + Google-Sans-analog type + monochrome
"Spark Eye" logo + Atlas Rail nav + realistic Nano-Banana eye imagery), keeping only the
**login screen** and the **FastAPI backend**. Driven by the **IELA award deadline 22 Jun
2026**. Execute the 10-phase plan judge-path-first; commit+push every phase (Render
auto-deploys `main`). EyeBot = AI ophthalmology tutor for SNEC residents/students.

## 3. DECISIONS LOCKED (do NOT re-litigate)
- **Execution mode: INLINE.** User initially picked "subagent-driven" via the question, then
  **rejected the actual subagent dispatch and said "continue task"** → I have been doing all
  work **directly myself, no subagents**. Keep doing that. Do NOT spawn implementer/reviewer
  subagents.
- **Push cadence: push per phase** (user explicitly chose this when asked). Commit + push to
  `main` after every phase; Render auto-deploys to the live IELA URL
  `https://snec-ai-chatbot.onrender.com`. User accepts that prod looks mid-redesign between
  phases.
- All AURORA design decisions from the prior handoff remain locked: palette (paper #F6F7F9,
  canvas #EEF0F8, surface #FFF, ink #1F1F1F/#5F6368/#9AA0A6), Gemini gradient
  `linear(100deg,#4285F4,#9B72CB,#D96570)` as the only colour, Inter+JetBrains-Mono analog
  type, monochrome static Spark Eye logo, Atlas Rail + Atlas Map nav, lavender chat wash
  `linear-gradient(160deg,#EAEBFB,#EFEAF6 52%,#F6ECF1)` (= `--wash-lavender` token).
- **fx teardown approach (decided this session):** do NOT delete `frontend/src/fx/**`
  wholesale — the login + not-yet-rebuilt screens still import fx hooks. Instead: slim the
  providers, make `useWipeNavigate`/`useShellScroll` degrade gracefully (no-op when their
  providers are unmounted), and delete fx files **incrementally** as each screen is rebuilt
  (final sweep in Phase 9). Heavy app-wide runtime (FluidCanvas/Preloader/Lenis/transition)
  is already gone.
- **Reduced motion is owned by the AURORA `useReducedMotion()` hook** (mounted in the new
  AppShell) + a pure-CSS `@media (prefers-reduced-motion: reduce)` guarantee. The legacy
  PHOTOPIC MotionProvider (which wrote `data-motion="on"`) is unmounted.
- **Verification = `npm run typecheck` + `npm run build` + Playwright** (`frontend/tests/
  aurora_assert.mjs` for behaviour, `visual_sweep.mjs` for screenshots). There is NO unit
  test runner.

## 4. OPEN QUESTIONS
- **none** blocking. Phase 8 (Nano Banana imagery) is PAID + user-gated — must confirm
  credits before any generation (not reached yet).

## 5. ACTIVE SKILL / WORKFLOW
Executing the plan `docs/superpowers/plans/2026-06-13-aurora-frontend-redesign.md` **INLINE**
(no subagents — see §3). Phases 0–5 COMPLETE; **Phase 6 in progress** — Flashcards, SNEC logo,
Profile, Daily check-in DONE; **Admin (×4) + Supervisor remain**. No skill is mid-flight; just
continue building screens inline using the per-screen pattern in §8.

## 6. TASK LIST SNAPSHOT
Rebuild with TaskCreate/TaskUpdate (one task per phase):
- #1 Phase 0 — Foundation (tokens/fonts/logo/MotionSurface/glyphs) — **completed**
- #2 Phase 1 — Shell rebuild + legacy FX teardown — **completed**
- #3 Phase 2 — Dashboard (judge-facing #1) — **completed**
- #4 Phase 3 — Cases + Atlas Map (judge-facing #2, centrepiece) — **completed**
- #5 Phase 4 — Tutor chat (judge-facing #3) — **completed** (commit b7652ef)
- #6 Phase 5 — Progress (judge-facing #4) + Summary — **completed** (commit d6cafbb)
- #7 Phase 6 — Remaining screens — **in_progress**: Flashcards + SNEC (d3f5c32), Profile +
  Check-in (deb38c2) DONE; remaining = Admin×4, Supervisor
- #8 Phase 7 — Login reconciliation (USER-GATED) — pending
- #9 Phase 8 — Realistic imagery pass (Nano Banana Pro — PAID, USER-GATED) — pending
- #10 Phase 9 — Hardening, a11y, performance, cleanup, deploy — pending

## 7. WORK PRODUCT SO FAR
**HEAD = `4f3bdc1`. Working tree clean — nothing uncommitted, no partial work in flight.**
All committed + pushed (Render auto-deploys `main`). Phase commits since the plan:
- Phase 0 `210ae72`(tokens) `d6d4cb8`(fonts) `360d1bf`(logo+favicon) `e7438b1`(MotionSurface) `928a4a6`(glyphs)
- Phase 1 `eb92be3` (Atlas Rail shell + slim providers + graceful fx teardown)
- Phase 2 `e4d75db` (Dashboard) · Phase 3 `3cb037f` (Cases+Atlas Map) + `8da7d1f` (case session)
- Phase 4 `a8fe598` (chat components) + `b7652ef` (Tutor screen, SSE intact)
- Phase 5 `d6cafbb` (Progress + Summary)
- Phase 6a `d3f5c32` (Flashcards + SNEC logo) · Phase 6b `deb38c2` (Profile + Daily check-in)

**Rebuilt AURORA screens (all live, in `frontend/src/aurora/screens/`):** Dashboard, Cases,
CaseSession, Tutor, Progress, Summary, Flashcards, Profile, CheckIn. Login = the original (kept,
unchanged). The legacy `@/screens/*` for every rebuilt screen has been `git rm`'d.
**SNEC co-brand:** `frontend/public/brand/snec-logo.jpg` in the Atlas Rail footer (mix-blend
multiply); also to be added to login in Phase 7.
**Verification:** `frontend/tests/aurora_assert.mjs` = **13 assertions, all passing** (logo,
reduced-motion, rail nav, dashboard, atlas region filter, tutor render + SSE-append, progress,
flashcards reveal→4 chips, SNEC logo, profile reduced-motion toggle, check-in). typecheck + build clean.

## 8. KEY FILES & REPO FACTS
- Spec: `docs/superpowers/specs/2026-06-13-frontend-redesign-design.md` (§5.3 = Tutor chat).
- Plan: `docs/superpowers/plans/2026-06-13-aurora-frontend-redesign.md` (Phase 4 = chat).
- **AURORA design system lives in `frontend/src/aurora/`:** `tokens.css` (vars, imported by
  `styles/index.css`), `aurora.css` (component CSS, imported by index.css), `Logo.tsx`,
  `MotionSurface.tsx`, `motion.ts` (`useReducedMotion`), `icons.tsx` (`Icon.*` glyphs),
  `AppShell.tsx` (Atlas Rail shell), `components/*` (incl. `Heatmap`, `StatCard`, `PlateWell`,
  `Sparkline`, `ProgressBar`, `AtlasMap`, `CaseCard`, `TrackChips`, chat parts), `screens/*` =
  Dashboard, Cases, CaseSession, Tutor, Progress, Summary, Flashcards, Profile, CheckIn (ADD: Admin, Supervisor).
- `frontend/src/styles/index.css` — imports tailwind → tokens.css → aurora.css; also holds
  the `.aurora-flow` keyframes (the animated gradient class) and the legacy PHOTOPIC `:root`
  + `@theme inline` + eyebot.css/gemini-gradients.css/fx.css imports (still present; legacy
  tokens like `--teal` still resolve so un-rebuilt screens render).
- **Admin/Supervisor to rebuild (legacy in `frontend/src/screens/`, READ for data hooks first):**
  `AdminLayout.tsx`, `adminShared.tsx`, `AdminOverviewPage.tsx`, `AdminStudentsPage.tsx`,
  `AdminAccountsPage.tsx`, `AdminActivityPage.tsx`, `AdminStudentDetail.tsx`, `AtRiskTable.tsx`,
  `CohortHeatmap.tsx`, `StudentDrillDown.tsx`, `SupervisorDashboard.tsx`. Pages:
  `app/(shell)/admin/{layout,page,students,accounts,activity}.tsx` (admin layout uses **AdminGuard,
  NOT CheckInGuard**) + `app/(shell)/supervisor/page.tsx`.
- Data hooks: `useAuth()` (`@/screens/AuthContext`) → `{user:{fullName,email,role,studentId,
  studentRole,mustChangePassword}, logout, setMustChangePassword,...}`; `useProgress()`
  (`@/hooks/useProgress`) → `{data:{xp,hearts,level,streak,session_count,learning_velocity,
  weak_topics[],topic_performance[{topic,score}],sessions[{session_id,timestamp,topic,
  summary,mode}]}}`.
- Pattern from Phases 2/3: create `@/aurora/screens/<Name>.tsx`, repoint the `(shell)/.../
  page.tsx` (keep `CheckInGuard`), `git rm` the old `@/screens/<Old>.tsx`, add CSS to
  `aurora.css`, add assertion, verify, commit.
- `frontend/tests/aurora_assert.mjs` — runs vs the dev server on :3000; has an authenticated
  `navCtx` (seeds localStorage user + `eyebot_token` cookie + mocks `/api/auth/me`,
  `/api/progress`, `/api/cases`). **13 assertions pass** (full list in §7) — incl. logo, reduced-motion,
  rail nav, dashboard, atlas filter, tutor + SSE, progress, flashcards, SNEC logo, profile toggle, check-in.
- `frontend/tests/visual_sweep.mjs` — screenshot harness; usage `node tests/visual_sweep.mjs
  <prefix> http://127.0.0.1:3000 <route...>`. Mocks all APIs incl. SSE `/api/chat`.
- Backend: FastAPI api-only :8000; Next standalone proxies `/api/*` (SSE intact). Dev:
  `uvicorn tools.api.server:app --port 8000` + (in `frontend/`) `npm run dev`. Prod
  https://snec-ai-chatbot.onrender.com (Render builds the Dockerfile from `main`).
- `.gitignore` now ignores `frontend/*.png` (sweep screenshots) — they won't be committed.

## 9. GOTCHAS & CONSTRAINTS
- **Execute INLINE, not via subagents** (§3). **Commit + push after the phase.** Commit msgs
  via Bash `git commit -F - <<'EOF' ... EOF` (PowerShell mangles quotes); end body with
  `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
- **Run `visual_sweep.mjs` / any node script with `/route` args via the PowerShell tool, NOT
  the Bash tool** — Git-bash mangles leading-slash args (`/chat` → `C:/Program Files/Git/
  chat` → about:blank, false "CLEAN"). `aurora_assert.mjs` (no slash args) is fine via Bash.
- A background `npm run dev` may be running on :3000 (id may be stale). If port 3000 is held
  by an old server, `taskkill //PID <pid> //F` (or `taskkill //IM node.exe //F`) and restart
  `npm run dev` fresh, then wait for :3000 before running Playwright. HMR after big edits can
  go stale — restart when in doubt.
- **SSE must stay intact** on the chat rebuild — keep the streaming fetch/reader logic
  verbatim; chat route stays native-scroll (no smooth scroll). Smoke-test that tokens stream
  incrementally (visual_sweep mocks `/api/chat` as an event-stream).
- **Do NOT overwrite files in `workflows/`** without explicit user permission.
- **Phase 8 Nano Banana generation is PAID — confirm with the user before running.** Verify
  the Gemini model id (`gemini-3-pro-image`) via ListModels before generating (ids drift).
- A11y invariants per screen: exactly one `<h1>`, no nested `<main>` (the shell already
  provides `<main id="main">`), decorative svg/canvas `aria-hidden`, no horizontal overflow
  at 390px. The shell renders `<main id="main">` so screens use `<div>`/`<section>`/`<aside>`.
- Windows + PowerShell environment. Today is 2026-06-13; IELA deadline 22 Jun 2026 (~9 days).
  Phases 0–5 are the judge path (login already good; Dashboard/Cases/Tutor/Progress). Phases
  6 & 8 may trail; ship CSS-placeholder plates if imagery isn't generated yet.

## 10. RESUME CHECKLIST
1. Read this file, then `CLAUDE.md`, the spec, and the plan's Phase 6 section.
2. Rebuild the task list (§6) with TaskCreate/TaskUpdate (one per phase; #1–#6 completed, #7 Phase 6
   in_progress).
3. Re-orient the user briefly (all student/judge-facing screens are rebuilt + shipped; **Admin +
   Supervisor remain** in Phase 6). Do NOT re-ask the locked decisions in §3.
4. Continue **inline**: rebuild Admin (overview/students/accounts/activity) + Supervisor in AURORA —
   per screen: build `@/aurora/screens/<Name>.tsx`, repoint the page (keep **AdminGuard**), `git rm`
   the legacy screen, add CSS + an assertion, verify (typecheck + build + `aurora_assert.mjs` +
   PowerShell screenshot), commit + push.
5. THEN Phase 7 (login: swap to the Spark Eye logo + add the SNEC logo), Phase 8 (PAID Nano Banana
   imagery — CONFIRM credits first; `gemini-3-pro-image` via `GEMINI_API_KEY`), Phase 9 (hardening:
   remove `app/aurora-scratch/`, full a11y sweep, perf, final visual sweep, deploy verify).
   **NOTE the user's last steer:** when asked to prioritise, they invoked `/handoff` — so on resume,
   reconfirm whether to do Admin/Supervisor next or polish the demo path (Phase 7 + 9) first.
