# SESSION HANDOFF
> Black-box snapshot for a cross-account resume. Written 2026-06-14 (gamification/flashcard
> improvement plan — Phase 1 shipped). HEAD `083b808`.
> To resume in the new session: run `/handoff-resume` (or paste this whole file and say
> "resume from this handoff").

## 1. NEXT ACTION (read this first)
**Ask the user whether to start Phase 2** (or wait). The user approved a multi-phase
flashcard + gamification improvement plan and said "proceed"; I shipped **Phase 1**
(tasks #23) and then asked "continue into Phase 2 now, or pause to review the deploy
first?" — that question is OUTSTANDING. Do NOT start Phase 2 code until they answer.
If they say go: Phase 2 = task #24 (Flashcard depth — Due-today/SM-2 surfacing,
in-session retry for weak answers, choosable session length). Details in §6/§7.

Standing rule now in force (saved to memory `feedback_explain_to_users.md`): **add
in-UI explanations/help text wherever appropriate, at all times, without being asked.**

## 2. SESSION GOAL
Two intertwined goals on the EyeBot SNEC ophthalmology tutor (Next.js/React frontend +
FastAPI backend; part of the IELA 2026 award push):
(a) **Polish the flashcard learning loop** — compulsory typed answers, AI grading vs a
crafted KB-grounded model answer, lenient /100 scoring, AI-decided XP, encouraging coach.
(b) **Improve gamification** per an agreed plan (Phases 1–4). Phase 1 (correctness +
ranks + daily goal, hearts retired) is DONE & shipped. Phases 2–4 pending.
Earlier in the session I also did small UI fixes (check-in green/red answers; dashboard
Recent-activity moved beside Due-today). The big **de-AI overhaul is COMPLETE** (prior
handoff `9bb6713`) — do not redo it.

## 3. DECISIONS LOCKED (do NOT re-litigate)
- **Flashcards = compulsory answer → AI grade → reveal model answer.** The student MUST
  type an answer (≤300 chars, live counter); on submit the AI grades it **out of 100**,
  **very leniently** (reasonable attempt = 70–100), and that grade **decides the XP** won
  (5–35/card, floor 5). Model answer is revealed only AFTER grading, beside the student's
  answer + score + feedback. Grading failure still rewards + reveals (never trapped).
- **Self-rating REMOVED** (no more "How well did you recall this?" / Again-Hard-Good-Easy).
  Replaced by a single Next-card button.
- **Coach side panel** of encouraging, informal, lighthearted, context-aware bubbles.
- **Grading stays AI** (the only AI left in flashcards); everything else deterministic.
- Gamification plan approved. **Phase 1 = G1 (real session XP + backend single source of
  truth), G2 (daily XP goal ring), G4 (retire hearts), G6 (clinical rank titles +
  level-up).** Order/agency: "you decide".
- **NO G3** — the user explicitly said **check-in remains the SOLE streak trigger**; do
  NOT change streak logic (streak still only advances on the daily check-in, via
  `tools/profile/update_profile.py`).
- **Hearts: REMOVE** (user deferred to my recommendation; they were dead + punitive).
- **STANDING RULE (all future work): explain things to users in the UI** wherever helpful
  (tooltips, captions, microcopy). Saved as memory `feedback_explain_to_users.md`.
- Daily XP goal = **100/day** (~one session). Rank ladder: Trainee → Junior Practitioner
  → Practitioner → Senior Practitioner → Specialist → Senior Specialist → Consultant.

## 4. OPEN QUESTIONS (awaiting user)
- **Start Phase 2 now, or pause to review Phase 1 on the deploy first?** (THE blocking one.)
- **Phase 4 leaderboard: in or out?** Flagged as the one risky/demotivating mechanic —
  opt-in + supervisor-gated if yes. Deferred.
- (When Phase 3 lands) badge themes — clinical/role-aware names (e.g. "Tonometry Pro",
  "OCT Reader"). Not yet started.

## 5. ACTIVE SKILL / WORKFLOW
none. Plain build-and-commit loop. (`/handoff` + `/handoff-resume` in use for cross-account
continuity. Note: tried to invoke `superpowers:frontend-design` earlier — it does NOT exist
in this environment; don't retry.)

## 6. TASK LIST SNAPSHOT (rebuild verbatim)
- #10–#19 [completed] — AURORA redesign phases 0–9 (shipped long ago; do NOT redo).
- #20 [completed] — Cases → 50/role topic-sets, no-repeat, drop AI gen (151 cases).
- #21 [completed] — Flashcards → 30 sets/role (15 topics × easy/medium) + picker (300 cards).
- #22 [in_progress] — Verify + ship de-AI overhaul. (Effectively done/shipped; left
  in_progress historically. Can mark completed.)
- #23 [completed] — Gamification/flashcards **Phase 1** — correctness + ranks + daily goal.
- #24 [pending] — **Phase 2** — Flashcard depth: F1 surface SM-2 "N cards due today" on
  Dashboard + a Review mode that blends due + new cards across topics; F2 re-queue answers
  scoring <40 at end of session; F4 choosable session length (Quick 5 / Standard 10 / Deep
  20) + keyboard-first. All deterministic (grading stays the existing AI).
- #25 [pending] — **Phase 3** — Motivation: G5 fix `totalCards` increment + persist
  achievements server-side + clinical/role-aware badges + badge case on Progress; G7
  per-session XP breakdown + cap chat XP/day (anti-farm); F3 "Explain this" → opens Tutor
  pre-seeded with the question; weekly recap on Progress/Summary.
- #26 [pending] — **Phase 4 (optional)** — opt-in, supervisor-gated cohort leaderboard.

## 7. WORK PRODUCT SO FAR (this session — ALL COMMITTED, tree clean at 083b808)
Commits this session (newest first):
- `083b808` feat(gamification): daily XP goal ring on the dashboard hero (G2)
- `3235b7d` feat(gamification): real session XP, clinical rank titles, retire hearts (G1/G4/G6)
- `b03cf7a` feat(flashcards): AI grades /100 leniently and decides XP, with a coach panel
- `a9f91a9` feat(flashcards): cap recall answers at 300 chars with a live quota counter
- `244ddd3` feat(flashcards): require an answer, grade it, then reveal the model answer
- `c83cd64` feat(dashboard): move Recent activity beside Due today on the same row
- `18be33a` feat(checkin): show correct answer in green, wrong in red

**The agreed gamification/flashcard improvement plan (full text, for Phases 2–4):**
Flashcards — F1 activate SM-2 "Due today" (engine exists, underused), F2 in-session retry
for weak answers (<40), F3 "Explain this"→Tutor handoff, F4 session length + keyboard-first,
F5 optional hint (small XP trim). Gamification — G1✅, G2✅, G3 SKIPPED (check-in only),
G4✅, G5 clinical badges that actually fire + badge case, G6✅, G7 XP transparency +
anti-farm, G8 optional leaderboard.
**Known still-broken (targets for Phase 3):** `totalCards` is NEVER incremented anywhere →
achievements `first_session`/`cards_50`/`cards_100` can't fire; achievements are
localStorage-only (not persisted/visible); Tutor grants 10 XP/message uncapped (farmable);
`perfect_session` achievement is now obsolete (self-rating removed).

## 8. KEY FILES & REPO FACTS
Flashcards (the live work area):
- `frontend/src/aurora/screens/Flashcards.tsx` — compulsory answer, /100 grading via
  `useFlashcardCheck`, `xpForScore(score)` (=max(5,round(score/100*35))), coach bubbles
  (`coach` derived per render, keyed off `idx` so no flicker), `MAX_ANSWER_CHARS=300`,
  card-count pill (hearts removed), level-up `toast` (sonner) using `rankForLevel`.
  Writes `eyebot_session` {topic,cardsReviewed,avgScore,earnedXp} on finish for Summary.
- `frontend/src/aurora/screens/Summary.tsx` — reads real earnedXp from `eyebot_session`;
  total + level from backend (`useProgress`); shows rank; has explain microcopy.
- `frontend/src/aurora/screens/Dashboard.tsx` — hero readouts (Streak/<rank>/XP) with
  `hint` tooltips; `GoalRing` in hero `action`; reads `getDailyXp()` after mount.
- `frontend/src/aurora/components/GoalRing.tsx` — NEW SVG dial; `onDark` = white arc for
  the gradient hero, else gradient arc; caption spells out raw numbers.
- `frontend/src/aurora/components/GradientHero.tsx` — `Readout` now has optional `hint`
  (rendered as `title=` tooltip); `action` slot used for the ring.
- `frontend/src/lib/rank.ts` — NEW. `rankForLevel(level)` → {title,nextTitle,levelsToNext}.
- `frontend/src/lib/legacy/gamification.ts` — `XP_REWARDS`, `addXP` (now also calls
  `addDailyXp`), NEW `DAILY_XP_GOAL=100` + `getDailyXp()`/`addDailyXp()` (per-day
  localStorage key `eyebot_daily_xp_<toDateString>`). `XP_PER_LEVEL=500`,
  `calculateLevel`. `getStoredHearts/setStoredHearts/syncHeartsFromBackend` still EXIST but
  hearts are no longer surfaced (safe to leave).
- `frontend/src/aurora/AppShell.tsx` — removed `syncHeartsFromBackend` call (kept
  `syncStreakFromBackend`).
- `frontend/src/aurora/aurora.css` — added: `.aurora-deck-count-pill`, `.aurora-coach*`,
  `.aurora-feedback-xp`, `.aurora-recall-meta/-count/-hint`, `.aurora-your-answer`,
  `.aurora-ring*` (+ `.is-on-dark`), `.aurora-summary-xp-note/-rank`, hero `.aurora-hero-text
  { margin-right:auto }`. Removed dead `.aurora-rate*` rules. Green token `--g-green/
  --on-green/--on-green-2` added in `frontend/src/aurora/tokens.css` (used by check-in
  correct state + XP badge + ring-done + goal complete).
Backend:
- `tools/api/routers/student.py` — `/api/flashcards/check`: prompt rewritten for **0–100,
  very lenient, warm tone**; score clamped 0–100; SM-2 quality remapped `round(score/20)`;
  defensive truncate `student_answer[:600]`. `/api/flashcards/topics` + `/generate`
  (per-user no-repeat) unchanged. `/api/gamification/sync` returns {xp,hearts,level,streak}.
- `tools/profile/update_profile.py` — streak logic (check-in only — DO NOT touch per
  decision), xp/hearts daily reset. `tools/flashcards/sm2.py` — SM-2 (`next_review`,
  `due_date`); `flashcard_store.py` — `get_due_cards`, `insert_cards`,
  `get_served_static_fronts` (these power Phase 2 F1).
- `frontend/src/hooks/useProgress.ts` → GET `/api/progress` {xp,hearts,level,streak,
  weak_topics,topic_performance,sessions,...}. `useGamification.ts` → sync mutation.
Other: `frontend/src/screens/GuidedTour.tsx` EXISTS (legacy tour; mentions hearts — left
alone). The user earlier asked for a NEW first-3-visits product tour — I explored routes
but wrote NO tour code; it's still un-started (lower priority than the gamification plan).
Repo: branch `main`; Render auto-deploys on push. Stack: Next 16/React 19/Tailwind v4/
TanStack Query; FastAPI (MOCK_MODE works without Gemini). Memory dir at
`C:\Users\caleb\.claude\projects\C--Users-caleb-OneDrive-Desktop-SNEC-AI-CHATBOT\memory\`.

## 9. GOTCHAS & CONSTRAINTS
- **PowerShell 5.1 mangles `git commit -m` here-strings that contain double-quotes** — the
  `"` breaks native arg passing (commit fails with a bogus pathspec). Use single-quoted
  here-strings `@'...'@` AND avoid embedded `"` in the message body. End with the
  `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>` line.
- **Auto commit + push after each completed unit** (per-slice commits; multiple per phase
  is fine). **INLINE execution, no subagents.** Don't overwrite `workflows/` without permission.
- **Do NOT touch streak logic** (decision: check-in is the only streak trigger).
- **Grading stays AI**; everything else deterministic. Don't reintroduce AI generation.
- **Verify after edits:** `npm --prefix frontend run typecheck` (CSS-only changes still run
  it); if backend touched, `python -c "import tools.api.routers.student"` and
  `python -m pytest tests/api -q`. **2 admin tests fail pre-existingly** in
  `tests/api/test_admin_endpoints.py` — NOT yours, ignore. `tests/cases` = 10 pass.
- Hydration: read localStorage AFTER mount (useEffect) for new dashboard values to avoid
  SSR mismatch (did this for `dailyXp`).
- Never hardcode/log/commit secrets (.env has GEMINI_API_KEY + SUPABASE keys). Windows/CRLF
  warnings on commit are harmless.
- Medical accuracy critical for any content; ground in `workflows/ophthalmology_kb.md`.

## 10. RESUME CHECKLIST
1. Read this file fully + `CLAUDE.md` (WAT framework) + the memory `MEMORY.md` index
   (esp. `feedback_explain_to_users.md`, `feedback_git_workflow.md`, `feedback_design_standard.md`).
2. `git log --oneline -8` (confirm HEAD `083b808` or later) and `git status` (expect clean).
3. Rebuild the task list from §6 (#22 in_progress→can complete; #23 completed; #24–#26 pending).
4. Re-orient the user briefly (goal, Phase 1 shipped, NO G3, explain-to-users rule).
5. Execute NEXT ACTION (§1): ask whether to start **Phase 2** (#24) now or pause; on go,
   implement F1/F2/F4 deterministically, typecheck, commit+push per slice, applying the
   explain-to-users rule throughout.
