# SESSION HANDOFF
> Black-box snapshot for a cross-account resume. Written 2026-07-24 14:55 +0800.
> To resume in the new session: run `/handoff-resume` (or paste this whole file and say
> "resume from this handoff").

## 1. NEXT ACTION (read this first)
**The primary work is DONE and SHIPPED.** Admin console P1 ("Truth & Safety") — 12 tasks —
was implemented, three-stage-reviewed, rebased onto `origin/main`, and pushed:
`b0f6f67..bcd6e0e` (22 commits). All gates were green (backend 1037 passed, typecheck
clean, build 11 routes, aurora harness 53/53).

There is **nothing left to do on P1.** The next action is a DECISION for the user: proceed
to **P2** (real analytics depth) next, or stop here? If yes to P2, start the brainstorm →
writing-plans → subagent-driven-development loop for P2 (scope in §7). Also: a background
task `task_6225e6d6` ("Fix admin_student_insights rate-limit URL-keying gap") is running in
a SEPARATE local session — **that fix is already on `origin/main`** (commit `0183a19`, a
concurrent session did it identically), so when that task finishes, VERIFY against
origin/main and likely DISCARD its output as redundant to avoid a duplicate/conflicting
change.

## 2. SESSION GOAL
User's request: "the current admin feature is so low quality and bad, audit and plan before
making it world class." We audited the SNEC EyeBot staff admin/analytics console, found the
skeleton sound but the data-integrity/product layer broken (it displayed fabricated numbers
and rendered backend failures as zeros), and the user chose a **full rebuild across 5
phases**. We delivered **Phase 1 (Truth & Safety)** end-to-end. Phases 2–5 remain, specced
but not built.

## 3. DECISIONS LOCKED
- **"World class" direction = Full rebuild, all three** (data integrity + analytics depth +
  operations), decomposed into 5 phases (P1–P5). P1 done.
- **Migrations 010 + 011: already applied** (2026-07-14). User initially said "prep SQL,
  I'll apply" but they were already live — the dead OSCE panels were a plain code bug, not a
  blocked feature. No migration was needed for P1.
- **Tenancy = SNEC only for now.** No multi-institution/tenant boundary. This removed the
  bulk of P4 scope. Every staff member legitimately sees every student.
- **Design timing = visual/a11y rebuild LAST** (P5). P1–P4 land inside the current dark
  `.aurora-admin` shell; the visual + keyboard-accessibility rebuild comes at the end.
- **Execution = subagent-driven-development** (fresh implementer per task + spec review +
  code-quality review, loop until clean).
- User standing rules (CLAUDE.md / memory): after a completed task, **commit + push directly
  to `main`** (no feature branch, no asking) BUT verify green first; work in an **isolated
  worktree at `origin/main`** because the repo is edited by MULTIPLE concurrent sessions and
  `main` gets force-pushed — ALWAYS `git fetch` + check fast-forward before push.

## 4. OPEN QUESTIONS
- **Proceed to P2 now, or stop?** (Awaiting user.) If P2: scope already in §7.
- Whether to fold the P2 "`/token-summary` under-reports past 500 sessions" bug into P2 or
  defer further (flagged, low urgency).
None else outstanding.

## 5. ACTIVE SKILL / WORKFLOW
**None mid-flight.** The full brainstorming → writing-plans → subagent-driven-development
cycle for P1 completed. If continuing to P2, START FRESH with `superpowers:brainstorming`
(then `writing-plans`, then `subagent-driven-development`), same as P1.

## 6. TASK LIST SNAPSHOT
All 12 P1 tasks are COMPLETED (do NOT rebuild — P1 is shipped):
- #1 Structured case fields on activity feed — completed
- #2 Cohort band consumes structured fields — completed
- #3 Cohort summary returns topic counts — completed
- #4 Weakest-topic bars use real counts — completed
- #5 Windowed DB read helpers — completed
- #6 Activity-trend endpoint — completed
- #7 Frontend consumes trend endpoint — completed
- #8 Failures render as errors, not zeros — completed
- #9 Confirm before revoking an account — completed
- #10 Rate-limit admin write endpoints — completed
- #11 Allow-list the digest recipient — completed
- #12 Delete the dead admin surface — completed

## 7. WORK PRODUCT SO FAR
**Shipped to origin/main (bcd6e0e), all committed — nothing uncommitted from THIS session.**
Spec + plan live on origin/main:
- `docs/superpowers/specs/2026-07-24-admin-p1-truth-safety-design.md` — P1 design; §7 lists P2–P5 scope.
- `docs/superpowers/plans/2026-07-24-admin-p1-truth-safety.md` — the 12-task TDD plan (corrected to match shipped code).

What P1 delivered (all live): activity feed emits structured OSCE grade fields (revived 2
dead Tier-2 panels + deleted a regex that scraped "32/40" from a display string); cohort
`weakest_topics` carry real `{topic,count}` (bars were faked from list index); **getJSON now
THROWS → backend failures render as skeleton/error states, never "0"** (the worst defect);
new server-side `GET /api/admin/activity-trend` (old chart bucketed an 80-item-capped feed);
confirm-on-remove dialog; rate-limits on all 5 admin write endpoints; digest recipient
allow-list (was arbitrary-recipient data-exfil); ~500 lines dead code deleted
(AdminAccounts.tsx, react-router outlet shim); student-detail modal rewired onto the shared
`useStudentDetail` hook (with a note-draft-survives-poll regression test).

**P2–P5 remaining (from design doc §7):**
- **P2 — analytics depth:** real server-side aggregation, time-series trends, explainable
  at-risk, per-student mastery vs cohort, per-discipline pool filter (the "switch content
  pool" UI claim was REMOVED in P1 as false — no pool filtering exists yet), data-viz
  rebuild. Also fix `/api/admin/token-summary` silently under-reporting once a cohort > 500
  sessions (`db.get_all_sessions()` defaults `limit=500`).
- **P3 — operations:** chunked/resumable CSV import (currently synchronous: per-row bcrypt +
  email, unbounded `file.read()`, credentials lost on timeout), credential download, bulk
  actions (multi-select remove/promote/resend-invite), server-side roster query/pagination.
- **P4 — access & scale:** trainer/admin permission split (a trainer currently sees every
  student's full session history + token spend), replace poll-everything refresh (9 queries
  × 30s per tab over full-table-scan endpoints). NO tenant boundary (SNEC-only, locked).
- **P5 — design & a11y:** visual rebuild, keyboard-navigable tables (rows are `<div onClick>`,
  no role/tabindex), modal focus trap + restore, responsive.

## 8. KEY FILES & REPO FACTS
- Worktree used for ALL P1 work (clean, at origin/main): `C:\Users\caleb\AppData\Local\Temp\
  claude\C--Users-caleb-OneDrive-Desktop-SNEC-AI-CHATBOT\be1041d5-0502-423a-8bfe-b5304ac5deb5\
  scratchpad\admin-audit` — a TEMP dir; the git commits are durable on origin/main regardless.
  May be cleaned; recreate with `git worktree add <path> --detach origin/main` if needed.
- Admin surface (frontend): `frontend/src/aurora/screens/Admin{,Cohort,Roster,Provisioning,
  StudentDetail,Audit}.tsx`, `frontend/src/hooks/useAdmin.ts`, `frontend/src/screens/
  adminShared.tsx`, `frontend/src/aurora/components/admin/PanelState.tsx` (new in P1), charts
  in `frontend/src/aurora/components/charts/`. AdminAccounts.tsx was DELETED.
- Admin backend: `tools/api/routers/admin.py`, `tools/api/routers/supervisor.py`,
  `tools/supervisor/*.py`, `tools/shared/db.py` (added `get_sessions_since` /
  `get_case_progress_since`).
- Tests: `tests/api/test_admin_*.py`, `tests/supervisor/test_cohort_summary*.py`; frontend
  visual harness assertions in `frontend/tests/aurora_assert.mjs` (CI-gated).
- Build (junctioned node_modules → Turbopack rejects junctions): `npm --prefix frontend run
  typecheck` and `npm --prefix frontend run build:safe` (webpack). NEVER plain `npm run
  build`/`npm ci`/`npm install` in the worktree.
- Read CLAUDE.md (production invariants, WAT architecture) and memory
  `project_admin_p1_truth_safety.md` for the full gotcha list.

## 9. GOTCHAS & CONSTRAINTS
- **This handoff file was committed via the origin/main worktree, NOT the main checkout.**
  The user's LOCAL main checkout (`C:\Users\caleb\OneDrive\Desktop\SNEC_AI_CHATBOT`) is 22
  commits BEHIND origin/main (0 ahead — a clean ancestor). **Do NOT push from the local main
  checkout** — it would need a force and could disturb the concurrent background session
  (`task_6225e6d6`) sharing it. To sync the local checkout: `git pull --ff-only` there when
  safe (watch for uncommitted working-tree changes from other sessions). NOTE: the local-disk
  copy of THIS file may be one commit behind the origin/main copy — they are identical content.
- **slowapi rate-limit gotcha (baked into P1, don't regress):** slowapi defaults to
  `key_style="url"`, so a plain `@limiter.limit` on a `{param}`-templated route buckets
  per-URL — a loop over distinct params DODGES the cap. MUST use
  `@limiter.shared_limit("N/minute", scope="fixed_name")`. All 5 admin writes + `/insights`
  now use the right form.
- **Frontend `.mjs` harness fixtures are NOT type-checked** — a stale mock shape passes
  typecheck+build and only fails at render in the CI aurora harness. Reconcile mocks in BOTH
  `frontend/tests/aurora_assert.mjs` and `frontend/tests/_mocks.mjs` on ANY endpoint-shape
  change.
- **Harness server trap:** `scripts/start-harness.sh` leaves `node .next/standalone/
  server.js` on :3000 holding a `.next` lock → next build dies EBUSY. `start-harness.sh stop`
  is UNRELIABLE (Git-Bash) — kill the node PID directly via PowerShell. OneDrive CPU
  contention makes the harness intermittently flaky on early homepage steps; retry.
- **Live Gemini/image calls cost real money + prod quota** — tests/harness run keyless in
  MOCK_MODE; never fire a live AI call without explicit go-ahead.
- **main auto-deploys to Render prod on push** — CI's harness gate runs AFTER the deploy
  starts, so verify green LOCALLY before pushing.
- Don't create/overwrite workflow files without explicit permission. Don't re-litigate the
  §3 locked decisions.

## 10. RESUME CHECKLIST
1. Read this file fully (and `git log --oneline origin/main -25` to see the shipped stack
   `b0f6f67..bcd6e0e`).
2. Confirm P1 is live: `git fetch && git log --oneline origin/main | head`. Nothing to
   rebuild for P1.
3. Check the background task `task_6225e6d6` state; if it produced an /insights rate-limit
   change, verify against `0183a19` on origin/main and discard as redundant.
4. Ask the user: proceed to **P2** (analytics depth, §7) now, or stop? Do NOT re-open the §3
   locked decisions.
5. If P2: recreate the origin/main worktree if gone, then run
   `superpowers:brainstorming` → `superpowers:writing-plans` →
   `superpowers:subagent-driven-development`, exactly as P1 was executed.
