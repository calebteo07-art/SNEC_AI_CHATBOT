# SESSION HANDOFF
> Black-box snapshot for a cross-account resume. Written 2026-07-09 (resume session, Task 5 SHIPPED, mid-Task-6 prep).
> To resume in the new session: run `/handoff-resume` (or paste this whole file and say
> "resume from this handoff").

## 1. NEXT ACTION (read this first)
**Dispatch the Task 6 implementer subagent (model=sonnet).** Tasks 1–5 are DONE, committed,
and pushed (HEAD = `7ba2041`, tree clean). Task 6 = "Catalog expansion — way more, way
funnier + phrase coverage gate" (backend + frontend). Its FULL plan text is at
**lines 995–1211** of `docs/superpowers/plans/2026-07-08-selena-seamless-custom.md` (I have
read it in full this session and reproduce the essentials in §7). I also verified the
`portrait.py` phrase-map state (§7) — **the plan's Step 3 phrase additions are complete and
correct as written**; blush is already fully covered and background is not gated, so the
coverage gate will go green from the plan verbatim.

Per the active workflow: dispatch a fresh implementer (sonnet) with the full Task 6 text +
the §7 notes → run the two-stage review (spec sonnet → quality opus) → fix loop → I
(controller) run the warm-server harness assert + push green → mark Task 6 complete → Task 7.

**Harness/push caveat (same as Task 5):** implementer does code + `python -m pytest -q` +
`cd frontend && npm run typecheck && npm run build` + **LOCAL commit (no push)**. Then YOU
run the robust warm-server harness (see §9 recipe) and push only once green. Do NOT let the
implementer run `bash scripts/start-harness.sh aurora` (wedge risk).

No user question blocks Tasks 6–9. Only pending user gate = **PAID go-ahead for Tasks 10–11**.

## 2. SESSION GOAL
Execute the 11-task TDD plan `docs/superpowers/plans/2026-07-08-selena-seamless-custom.md`
via **superpowers:subagent-driven-development**. It replaces the user-rejected per-student
"custom Selena" avatar (flat vector stickers composited over a raster — "extremely ugly")
with ONE transparent AI-rendered whole-look portrait per student, shown+animated on every
surface, plus a much bigger/funnier option catalog and real AI option tiles in a "loadout"
Studio. Tasks 1–9 are code (auto-execute continuously); Tasks 10–11 are PAID live-Gemini and
are GATED on explicit user go-ahead.

## 3. DECISIONS LOCKED
- **Execution mode: Subagent-driven** (user picked "Subagent-driven (Recommended)"). Per task:
  implementer subagent → spec-compliance reviewer (sonnet) → code-quality reviewer (opus) →
  fix loops → verified commit on `main` → next task. Continuous, NO check-ins between tasks.
- **Model selection:** implementer=sonnet (standard multi-file), spec-reviewer=sonnet,
  quality-reviewer=opus; bump to opus only for architecture/judgment.
- Four locked design decisions from brainstorming:
  - (D-a) greeting-card animation = living CSS motion on ONE render per look (no 2nd pose frame).
  - (D-b) Studio option tiles = real AI renders (placeholders first, paid later).
  - (D-c) way more real AI tiles + "ridiculous and funny variety of every component".
  - (D-d) "loadout" hero — hero only ever shows real rendered art; picks dock as tiles; Save
    fuses a new render. The user-facing "✨ Fusing your look…" copy is LOCKED (don't reword);
    it already exists in SelenaStudio.tsx (~line 184).
- **Tasks 10–11 are PAID and require explicit user go-ahead** before any live Gemini call
  (~103-tile art batch ≈ $1–2.50, then an end-to-end look smoke). Never fire live gen otherwise.
- Auto-ship: after each green+verified task, stage ONLY that task's files, commit, push
  directly to `main` (no feature branch). Never push red.
- **Task 4 (settled):** `TILES_ROOT` in `tools/avatar/tiles.py` is `Path(__file__)`-anchored
  (absolute) — do NOT "simplify" it back to cwd-relative.
- **Task 5 (settled this session):** shipped with two code-review polish fixes folded in
  (see §7). The colour-axis tray chips deliberately render NO `<img>` (they have no tile
  files); they show emoji + label only. Don't "restore" an img there.

## 4. OPEN QUESTIONS
None outstanding for Tasks 6–9 — the plan is fully specified and Task 6's coverage gate was
pre-verified as satisfiable from the plan verbatim. The only pending user gate is the
**go-ahead for the PAID Tasks 10–11** (do not start them without it).

## 5. ACTIVE SKILL / WORKFLOW
**superpowers:subagent-driven-development** (mid-flight). Prompt templates live under
`C:\Users\caleb\.claude\plugins\cache\claude-plugins-official\superpowers\5.1.0\skills\subagent-driven-development\`
(`implementer-prompt.md`, `spec-reviewer-prompt.md`, `code-quality-reviewer-prompt.md`) and
`.../requesting-code-review/code-reviewer.md` (all four read this session).
**Current position: Task 5 COMPLETE (committed+reviewed+pushed `7ba2041`). Task 6 in_progress,
NO code written yet — next step is to dispatch the Task 6 implementer.** Remaining per-task
cycle for 6→9: implementer → spec review (sonnet) → quality review (opus) → fix loop →
verified commit → next. After Task 9: final whole-implementation code review →
superpowers:finishing-a-development-branch → update memory (`project_ricoe_v2` + a new memory
for this seamless-custom brief).

## 6. TASK LIST SNAPSHOT
Rebuild with TaskCreate/TaskUpdate exactly:
- #1 [completed] Task 1: Promote keying to tools/shared/keying.py
- #2 [completed] Task 2: Portrait v2 — green-screen render → transparent webp
- #3 [completed] Task 3: Selena v3 raster-only — delete compositor, migrate consumers
- #4 [completed] Task 4: Tile tooling — placeholders + tile-file mandate
- #5 [completed] Task 5: Studio loadout builder
- #6 [in_progress] Task 6: Catalog expansion + phrase coverage gate  ← NO code yet; plan read, portrait.py verified
- #7 [pending] Task 7: Greeting card living custom Selena + self-heal
- #8 [pending] Task 8: Leaderboard portraits (bulk, no N+1)
- #9 [pending] Task 9: Design locks + final verification sweep

## 7. WORK PRODUCT SO FAR

**Task 5 SHIPPED this session (one clean commit, pushed):**
- `7ba2041` feat(studio): loadout builder — pending picks dock as tile chips, living hero,
  fusing beat. Modified 3 files: `frontend/src/aurora/screens/SelenaStudio.tsx`,
  `frontend/src/aurora/studio.css`, `frontend/tests/aurora_assert.mjs`.
  - Added a `pending` memo (after the `dirty` memo) = `STEPS.filter(s => draft[s.axis] !==
    data.config[s.axis])`; renders `.studio-tray` of `.studio-tray-chip` `<li>`s under the
    hero + an always-on `.studio-explain` helper line. Added `data-alive` to `.studio-hero`
    (kept `data-float`) → CSS `studio-breathe` idle. Reduced-motion guards in BOTH
    `html[data-motion="reduce"]` and `@media (prefers-reduced-motion)` forms. New harness
    assert: `PASS: Selena Studio — loadout tray docks pending picks as tile chips`.
  - Review cycle: spec review (sonnet) = ✅ compliant (re-ran typecheck+build). Quality
    review (opus) = ✅ ready to merge, only Minors. Applied 2 Minors before push, then amended:
    (1) colour axes (bodyColor/irisColor/blush) render NO tray `<img>` (`{!COLOR_MAP[s.axis]
    && <img …>}`) — they have no tile files, so the old code 404'd-then-hid on the common
    path; (2) `aria-hidden` the chip emoji (`<span aria-hidden>{s.emoji}</span>`) to match
    the step-header convention. `#4` (always-on explain copy) intentionally kept per plan.
  - Verified green: typecheck + build (twice), and the warm-server harness assert exited 0
    with all 51 PASS lines including the new tray assert.

**Task 6 (NOT started) — plan is lines 995–1211. Essentials so you don't re-read:**
Files: modify `tools/avatar/parts.py` (AVATAR_AXES), `tools/avatar/portrait.py` (phrase
maps), `frontend/src/aurora/avatar/axes.generated.ts` (REGENERATED, don't hand-edit),
`frontend/src/aurora/avatar/manifest.ts` (+ colour hexes), `frontend/src/aurora/avatar/backdrops.ts`
(+ gradients); create `frontend/public/avatar/tiles/**` placeholders for new ids; create
`tests/avatar/test_prompt_coverage.py`.
- **Step 1 (TDD):** create `tests/avatar/test_prompt_coverage.py` with
  `test_every_prompt_axis_id_has_a_bespoke_phrase()` (iterates `PROMPT_MAPS.items()`,
  asserts every non-"none" id in `AVATAR_AXES[axis]` is in the mapping) and
  `test_expansion_landed()` (asserts `trafficCone`/`dinoOnesie`/`bobaTea`/`dealWithIt`/
  `aurora` landed). Run → FAIL first.
- **Step 2:** expand the 7 grown prop axes + colours/backgrounds in `AVATAR_AXES` (full lists
  at plan lines 1041–1069). `DEFAULT_AVATAR` and `CONFIG_VERSION` untouched. Existing ids
  keep order; new ids append. Also rewrite the parts.py module docstring (D10/D11 sprite
  story superseded).
- **Step 3:** extend the phrase-map dict literals in `portrait.py` (plan gives the exact
  additions at lines 1078–1161 as `_BODY.update({...})` etc — the note at line 1163 says
  extend the literals directly since the maps are dict literals today, matching file style).
- **Step 4:** `python tools/avatar/export_axes.py` (regenerates axes.generated.ts) +
  `python tools/avatar/make_tile_placeholders.py` (keyless PIL placeholders for new tile
  ids). Then append the new colour hexes to `manifest.ts` (BODY/IRIS/BG_COLORS, plan
  1176–1182) and new gradients to `backdrops.ts` (plan 1187–1191).
- **Step 5:** `python -m pytest -q` + `cd frontend && npm run typecheck && npm run build` → PASS.
- **Step 6:** harness (I run it) → commit these files: `tools/avatar/parts.py
  tools/avatar/portrait.py frontend/src/aurora/avatar/axes.generated.ts
  frontend/src/aurora/avatar/manifest.ts frontend/src/aurora/avatar/backdrops.ts
  frontend/public/avatar/tiles tests/avatar/test_prompt_coverage.py` with msg
  `feat(avatar): catalog doubles+ — 46 ridiculous new options with bespoke prompt phrases + coverage gate`.

**CRITICAL Task-6 verification I did this session (saves a debugging loop):**
`portrait.py` PROMPT_MAPS (lines 108–112) gates exactly 10 axes: bodyColor, irisColor,
eyeShape, mouth, lashes, **blush**, glasses, topper, accessory, outfit — **NOT background**.
I read every phrase dict (lines 56–103): **`_BLUSH` (78–83) already contains ALL 12 expanded
blush ids** (rose, coral, peach, plum, berry, sky, mint, gold, grape, teal, stars, freckles),
so the plan's Step 3 correctly OMITS a `_BLUSH.update()`. Background ids need no phrases (not
gated). Cross-checked every other axis: the plan's Step 3 additions exactly cover each axis's
NEW ids. ⇒ **Following Step 3 verbatim makes the coverage gate green — no gap.** (If the gate
still reports a missing id, add a phrase in the same over-the-top voice — but it shouldn't.)

## 8. KEY FILES & REPO FACTS
- Plan (source of truth, full TDD code per task): `docs/superpowers/plans/2026-07-08-selena-seamless-custom.md`
  (1535 lines). Task 5 = 904–991 (DONE). Task 6 = 995–1211 (READ this session). Task 7 =
  1214–~1400 (start read to ~1273; READ the rest before dispatching). Tasks 8–9 = ~1400–1535
  (NOT yet read this session — read before dispatching each).
- Spec: `docs/superpowers/specs/2026-07-07-selena-seamless-custom-design.md`.
- `tools/avatar/parts.py` — AVATAR_AXES / DEFAULT_AVATAR / CONFIG_VERSION (Task 6 expands the
  7 prop axes + colours + backgrounds).
- `tools/avatar/portrait.py` — phrase maps `_BODY.._OUTFIT` + `PROMPT_MAPS` (10 axes) +
  `phrase_for` + `config_to_prompt`. Task 6 extends the dict literals.
- `tools/avatar/export_axes.py` — regenerates `frontend/src/aurora/avatar/axes.generated.ts`.
- `tools/avatar/make_tile_placeholders.py` — keyless PIL placeholder tiles (Task 4).
- `tools/avatar/generate_tiles.py` — PAID tile generator (MOCK_MODE-refusing) — Task 10/11 only.
- `frontend/src/aurora/avatar/{manifest.ts,backdrops.ts,tiles.ts}` — colour hexes / backdrop
  gradients / `tileSrc` URL builder (`/avatar/tiles/${axis}/${id}.webp`).
- `frontend/src/aurora/screens/SelenaStudio.tsx` — Task 5 target (shipped). `COLOR_MAP`
  (~line 43) distinguishes colour axes.
- Gates: backend `python -m pytest -q` (MOCK_MODE auto-on, keyless — NEVER set
  GEMINI_API_KEY); frontend `cd frontend && npm run typecheck && npm run build`; harness
  robust gate = warm-server assert (§9). Pytest was 601 green at Task 4; Task 5 frontend-only
  (still 601); Task 6 adds test_prompt_coverage.py (+2) → expect ~603.
- CLAUDE.md + memory `MEMORY.md` govern: WAT architecture, single-worker Render (never block
  the event loop), fail-closed config, JWT identity, PowerShell-tool-vs-Bash-tool discipline
  (bash_guard hook), DB migrations only via /db-migrate, never delete Dockerfile.

## 9. GOTCHAS & CONSTRAINTS
- **WARM-SERVER HARNESS RECIPE (this is the robust gate — worked perfectly this session):**
  1. `SKIP_BUILD=1 bash "C:\Users\caleb\OneDrive\Desktop\SNEC_AI_CHATBOT\scripts\start-harness.sh" serve`
     — reuses the implementer's fresh build, copies `.next/static`+`public` into
     `.next/standalone`, starts `node .next/standalone/server.js` on :3000, warms routes
     (incl. /studio), leaves server running, returns promptly. (Only use SKIP_BUILD=1 if a
     build was JUST done; else drop it.)
  2. `node "C:\Users\caleb\OneDrive\Desktop\SNEC_AI_CHATBOT\frontend\tests\aurora_assert.mjs" http://127.0.0.1:3000; echo "EXIT=$?"`
     — expect EXIT=0 with ~51 PASS lines.
  3. `bash "…\scripts\start-harness.sh" stop` to tear down.
  Do NOT run the full `bash scripts/start-harness.sh aurora` (build→assert→teardown in one) —
  it can pass every assert then HANG on teardown for tens of minutes, and piping through
  `| tail` buffers output so you can't tell. If a run wedges, kill ONLY the start-harness bash
  PID + playwright chromium (match `ms-playwright`/`_npx`) — NEVER the user's `chrome.exe`.
  A stale `node .next/standalone/server.js` can hold a Windows build lock (EBUSY) and break
  `npm run build` — kill it first.
- **SHARED CWD / bash_guard hook trap:** the Bash tool and PowerShell tool share ONE persisted
  working directory. Never `cd` into a subdir inside a Bash compound command — it moves the
  shared cwd and (a) breaks `.claude/hooks/bash_guard.py` (resolved relative to cwd → blocks
  every subsequent Bash call) and (b) breaks PowerShell relative git pathspecs. Use absolute
  paths; for git use `git -C "C:\Users\caleb\OneDrive\Desktop\SNEC_AI_CHATBOT" …` (cwd-
  independent). Recovery if wedged: `Set-Location "C:\Users\caleb\OneDrive\Desktop\SNEC_AI_CHATBOT"`
  in the PowerShell tool. (The start-harness.sh script uses `(cd … && …)` subshells, which do
  NOT change the parent cwd — invoking it by absolute path is safe.)
- **Never push red.** For Task 6 the gates are pytest + typecheck + build + the aurora harness
  assert. Verify all green BEFORE pushing to `main` (auto-deploys to Render prod).
- **PAID gating:** Tasks 10–11 and any `generate_tiles.py --generate` / live image call require
  explicit user go-ahead. Placeholders (`make_tile_placeholders.py`) + tests are keyless.
- **Two-stage review order is mandatory:** spec compliance (sonnet) MUST be ✅ before code
  quality (opus). Reviewer finds issues → implementer fixes → re-review. Apply receiving-code-
  review judgment (verify each finding on merits; don't reflexively accept or dismiss).
- Auto-commit+push to `main` after each green task; stage ONLY that task's files. Tree was
  clean at handoff (HEAD `7ba2041`).
- Windows/PowerShell box: PowerShell cmdlets → PowerShell tool; POSIX/git/npm/bash-harness →
  Bash tool. bash_guard PreToolUse hook enforces this.
- Don't reword the LOCKED "✨ Fusing your look…" copy. Don't re-litigate §3 decisions.

## 10. RESUME CHECKLIST
1. Read this file top to bottom. CLAUDE.md + MEMORY.md load automatically.
2. Rebuild the task list (§6) with TaskCreate/TaskUpdate (mark #1–5 completed, #6 in_progress).
3. Re-invoke the active skill: superpowers:subagent-driven-development (starting Task 6).
4. `git status` (expect clean, HEAD `7ba2041`) and optionally `python -m pytest -q` (expect 601).
5. Dispatch the Task 6 implementer subagent (sonnet) with the full Task 6 text (plan 995–1211)
   + the §7 CRITICAL verification note. Implementer does code + pytest + typecheck + build +
   LOCAL commit (no push). Then YOU run the §9 warm-server harness, push green. Then spec
   review (sonnet) → quality review (opus) → fix loop → mark #6 complete.
6. Continue Tasks 7→9 via the same cycle. Read Task 7 to its end and Tasks 8–9 from the plan
   BEFORE dispatching each. Do NOT start Tasks 10–11 without explicit user go-ahead.
7. After Task 9: final whole-implementation code review → superpowers:finishing-a-development-branch
   → update memory (`project_ricoe_v2` + a new memory for this seamless-custom brief).
