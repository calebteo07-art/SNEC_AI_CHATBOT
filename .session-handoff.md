# SESSION HANDOFF
> Black-box snapshot for a cross-account resume. Written 2026-07-09 (resume session #2:
> Task 6 CODE-COMPLETE + both reviews ✅ + 1 Minor fix applied; commit `dd99251` is LOCAL-ONLY
> — the controller harness gate + push are the ONLY things left on Task 6).
> To resume in the new session: run `/handoff-resume` (or paste this whole file and say
> "resume from this handoff").

## 1. NEXT ACTION (read this first)
**Run the Task-6 warm-server harness assert, and if green, `git push origin main`, then mark
Task 6 complete and start Task 7.** Task 6 is fully implemented, spec-reviewed (✅), and
quality-reviewed (✅ ready-to-merge, only Minors), and its one actionable Minor was applied.
It is committed **LOCALLY as `dd99251`** but **NOT pushed** — because pushing a frontend
change to `main` auto-deploys Render prod and CLAUDE.md mandates the aurora harness be green
first. That harness is the LAST unrun gate. Exact commands (from repo root
`C:\Users\caleb\OneDrive\Desktop\SNEC_AI_CHATBOT`, Bash tool):

```bash
# 1) serve the standalone build on :3000 (SKIP_BUILD=1 is VALID here — see note below)
SKIP_BUILD=1 bash "C:\Users\caleb\OneDrive\Desktop\SNEC_AI_CHATBOT\scripts\start-harness.sh" serve
# 2) assert against the warm server (expect EXIT=0, ~51 PASS lines)
node "C:\Users\caleb\OneDrive\Desktop\SNEC_AI_CHATBOT\frontend\tests\aurora_assert.mjs" http://127.0.0.1:3000; echo "EXIT=$?"
# 3) tear down
bash "C:\Users\caleb\OneDrive\Desktop\SNEC_AI_CHATBOT\scripts\start-harness.sh" stop
```
If EXIT=0: `git -C "C:\Users\caleb\OneDrive\Desktop\SNEC_AI_CHATBOT" push origin main`
(this pushes `dd99251` **and** the handoff commit on top of it — fine). Then TaskUpdate #6 →
completed, and dispatch the Task 7 implementer (sonnet).

**SKIP_BUILD=1 is safe here:** the implementer already ran a successful `npm run build` in
this session AFTER generating all new tiles/axes. My only post-build changes were (a) a
single COMMENT line in the generated `axes.generated.ts` (header text — zero bundle impact;
the AVATAR_AXES *data* was already the expanded version in the implementer's build) and (b)
`tools/avatar/export_axes.py` (backend Python, not in the frontend bundle). So the on-disk
`.next` build already reflects correct runtime behavior. If you'd rather be 100% safe, drop
`SKIP_BUILD=1` for a fresh build (costs a few min; watch for the EBUSY note in §9). I checked
for a stale `node .next/standalone/server.js` right before this handoff — **none was
running** (the implementer had killed the one leftover). No user question blocks this.

## 2. SESSION GOAL
Execute the 11-task TDD plan `docs/superpowers/plans/2026-07-08-selena-seamless-custom.md`
via **superpowers:subagent-driven-development**. It replaces the user-rejected per-student
"custom Selena" (flat vector stickers composited over a raster — "extremely ugly") with ONE
transparent AI-rendered whole-look portrait per student, shown + CSS-animated on every
surface, plus a much bigger/funnier option catalog and real AI option tiles in a "loadout"
Studio. Tasks 1–9 are code (auto-execute continuously, no check-ins); Tasks 10–11 are PAID
live-Gemini and are GATED on explicit user go-ahead.

## 3. DECISIONS LOCKED
- **Execution mode: Subagent-driven** (user picked "Subagent-driven (Recommended)"). Per task:
  implementer subagent → spec-compliance reviewer (sonnet) → code-quality reviewer (opus) →
  fix loop → verified LOCAL commit → CONTROLLER runs harness → push to `main` → next task.
  Continuous, NO check-ins between tasks.
- **Model selection:** implementer=sonnet, spec-reviewer=sonnet, quality-reviewer=opus; bump
  to opus only for architecture/judgment.
- Four locked design decisions from brainstorming:
  - (D-a) greeting-card animation = living CSS motion on ONE render per look (no 2nd pose frame).
  - (D-b) Studio option tiles = real AI renders (placeholders first, paid later).
  - (D-c) way more real AI tiles + "ridiculous and funny variety of every component".
  - (D-d) "loadout" hero — hero only ever shows real rendered art; picks dock as tiles; Save
    fuses a new render. The user-facing "✨ Fusing your look…" copy is LOCKED (don't reword);
    it already exists in SelenaStudio.tsx (~line 184).
- **Tasks 10–11 are PAID and require explicit user go-ahead** before any live Gemini call
  (~103-tile art batch ≈ $1–2.50, then an end-to-end look smoke). Never fire live gen otherwise.
- Auto-ship: after each green+verified task, stage ONLY that task's files, commit, push
  directly to `main` (no feature branch). Never push red — frontend changes need the aurora
  harness green FIRST.
- **Task 4 (settled):** `TILES_ROOT` in `tools/avatar/tiles.py` is `Path(__file__)`-anchored
  (absolute) — do NOT "simplify" it back to cwd-relative.
- **Task 5 (settled prior session):** colour-axis tray chips deliberately render NO `<img>`
  (they have no tile files); emoji + label only. Don't "restore" an img there.
- **Task 6 code-review dispositions (settled THIS session — do not re-litigate):** quality
  reviewer (opus) returned Ready-to-merge=Yes with 4 Minors. I applied **#1** (the generator
  `export_axes.py` was still hard-coding the stale "sprite parts (RICOE D9/D10)" header into
  the regenerated `axes.generated.ts` — the exact story Task 6 deleted from `parts.py`'s
  docstring; fixed both the header string at export_axes.py:33 and the docstring at line 5,
  then regenerated). I deliberately **SKIPPED #2/#3/#4** with reasons: #2 (make the coverage
  gate assert `set(PROMPT_MAPS)==set(PORTRAIT_AXES)`) = optional hardening, plan specified the
  test verbatim and spec review confirmed it — YAGNI; #3 (rainyWindow/candy render as flat
  fills not gradients) = EXACTLY per plan (plan added only 4 gradients, said the rest fall
  back); #4 (`_EYE["rainbow"]` phrase reads iris-ish) = verbatim locked plan copy, harmless.

## 4. OPEN QUESTIONS
None for Tasks 6–9 — the plan is fully specified. The only pending user gate is the
**go-ahead for the PAID Tasks 10–11** (do not start them without it).

## 5. ACTIVE SKILL / WORKFLOW
**superpowers:subagent-driven-development** (mid-flight). Prompt templates live under
`C:\Users\caleb\.claude\plugins\cache\claude-plugins-official\superpowers\5.1.0\skills\subagent-driven-development\`
(`implementer-prompt.md`, `spec-reviewer-prompt.md`, `code-quality-reviewer-prompt.md`) and
`.../requesting-code-review/code-reviewer.md` (all four read this session).
**Current position: Task 6 implemented + spec-review ✅ + quality-review ✅ + Minor #1 applied,
committed LOCALLY as `dd99251` (NOT pushed). The ONLY remaining Task-6 steps are: controller
runs the aurora harness → push → mark #6 complete.** Then per-task cycle for 7→9: read the
task's full plan text → dispatch implementer (sonnet, LOCAL commit only, no push, no full
harness) → spec review (sonnet) → quality review (opus) → fix loop → controller harness →
push green → next. After Task 9: final whole-implementation code review →
superpowers:finishing-a-development-branch → update memory (`project_ricoe_v2` + a new memory
for this seamless-custom brief).

## 6. TASK LIST SNAPSHOT
Rebuild with TaskCreate/TaskUpdate exactly (statuses verbatim):
- #1 [completed] Task 1: Promote keying to tools/shared/keying.py
- #2 [completed] Task 2: Portrait v2 — green-screen render → transparent webp
- #3 [completed] Task 3: Selena v3 raster-only — delete compositor, migrate consumers
- #4 [completed] Task 4: Tile tooling — placeholders + tile-file mandate
- #5 [completed] Task 5: Studio loadout builder
- #6 [in_progress] Task 6: Catalog expansion + phrase coverage gate  ← code done + both reviews ✅ + Minor #1 applied; committed LOCAL `dd99251`; ONLY harness + push remain
- #7 [pending] Task 7: Greeting card living custom Selena + self-heal
- #8 [pending] Task 8: Leaderboard portraits (bulk, no N+1)
- #9 [pending] Task 9: Design locks + final verification sweep

## 7. WORK PRODUCT SO FAR (this session)

**Task 6 — IMPLEMENTED, REVIEWED, COMMITTED LOCALLY (commit `dd99251`, NOT pushed).**
Commit message: `feat(avatar): catalog doubles+ — 46 ridiculous new options with bespoke
prompt phrases + coverage gate`. 60 files changed (+142/−34): 53 new placeholder `.webp`
tiles + 7 source files. HEAD chain: `dd99251` → parent `c3052d6` (prior handoff snapshot) →
`7ba2041` (Task 5). Tree was CLEAN right before this handoff was written.

What landed (all verified by an independent spec reviewer reading the actual diff):
- `tools/avatar/parts.py`: `AVATAR_AXES` expanded to the plan's exact lists (existing ids
  keep order, new appended). Final counts: bodyColor(24), irisColor(15), eyeShape(12),
  lashes(6), mouth(14), blush(13 UNCHANGED), glasses(16), topper(24), accessory(16),
  outfit(20), background(19). `DEFAULT_AVATAR` + `CONFIG_VERSION` UNTOUCHED. Module docstring
  rewritten: the stale "layered sprite compositor / D10-D11 part-sprite library" story is
  gone, replaced by the one-render-portrait + static-Studio-tiles reality; kept "Selena is
  Iris — one eye, no hair" + "single source of truth / fail-closed" framing.
- `tools/avatar/portrait.py`: extended the 9 phrase dict LITERALS (`_BODY,_IRIS,_EYE,_MOUTH,
  _LASHES,_GLASSES,_TOPPER,_ACCESSORY,_OUTFIT`) in-place (NO `.update()` calls) so every
  non-"none" id on all 10 gated axes has a bespoke maximalist phrase. `_BLUSH` untouched
  (already complete). `background` intentionally NOT in `PROMPT_MAPS` (not gated). No
  duplicate keys (AST-scanned by reviewer); no wrong-axis phrases.
- `tests/avatar/test_prompt_coverage.py` (NEW, 22 lines): `test_every_prompt_axis_id_has_a_
  bespoke_phrase()` + `test_expansion_landed()`. Written failing-first (both failed pre-impl),
  now pass.
- `frontend/src/aurora/avatar/axes.generated.ts`: regenerated via `export_axes.py` (mirrors
  new axes; parity test `tests/avatar/test_axes_parity.py` byte-compares it — green).
- `frontend/src/aurora/avatar/manifest.ts`: `BODY_COLORS += gold/silver/midnight/watermelon`,
  `IRIS_COLORS += lava/ice/rainbow`, `BG_COLORS += aurora/lavaLamp/arcade/rainyWindow/candy/
  sakura` (these are exhaustive `Record<Union,string>` → a missing hex is a compile error,
  which is why typecheck guards them).
- `frontend/src/aurora/avatar/backdrops.ts`: `GRADIENTS` (a `Partial` map) += aurora/lavaLamp/
  arcade/sakura (rainyWindow/candy intentionally fall back to their solid `BG_COLORS`).
- `frontend/public/avatar/tiles/**`: 53 new keyless PIL placeholder tiles for the new ids on
  the 7 tile axes (eyeShape/lashes/mouth/glasses/topper/accessory/outfit). `tests/avatar/
  test_tiles.py::test_every_tile_id_has_a_committed_file` green. (Colour + background axes are
  correctly NOT in `TILE_AXES`, so they need no tiles.)
- **My controller fix (Minor #1), folded into `dd99251` via `git commit --amend`:**
  `tools/avatar/export_axes.py` — header string at line 33 changed to
  `// Selena is Iris: a one-eyed mascot; these ids drive her portrait render + Studio option
  tiles.` and docstring line 5 changed from "…to build the sprite manifest + option pickers…"
  to "…to build the Studio option pickers + tiles…". Regenerated `axes.generated.ts`
  (`--check` now `[OK]`).

**Gates already GREEN:**
- `python -m pytest -q` = **603 passed** (implementer ran full suite). After my regen I re-ran
  `python -m pytest -q tests/avatar/` = **30 passed** (includes the parity + coverage + tile
  gates; the amend only touched a comment + backend generator, so 603 still holds).
- Frontend `npm run typecheck` = clean (ran twice — implementer + after my regen).
- Frontend `npm run build` = succeeded (implementer, in this session, after generating tiles).
- **NOT yet run by controller: the aurora warm-server harness assert.** ← the one remaining gate.

**Reviews (both this session):**
- Spec compliance (sonnet): ✅ Spec compliant — independently verified axis lists exact & in
  order, `_BLUSH` byte-identical to parent, no `.update()`, coverage complete, generated TS
  mirrors, manifest/backdrops exact, tiles present, 603 pytest, typecheck clean, commit staged
  only mandated paths & not pushed.
- Code quality (opus): ✅ Ready-to-merge=Yes. Praised layered drift guards (parity test +
  exhaustive Record typing + double phrase/tile gates), no dup keys, accurate docstring. 4
  Minors → see §3 for my dispositions (applied #1, skipped #2/#3/#4).

## 8. KEY FILES & REPO FACTS
- Plan (source of truth, full TDD code per task): `docs/superpowers/plans/2026-07-08-selena-seamless-custom.md`
  (1535 lines). Task 6 = **lines 995–1211 (DONE)**. **Task 7 = 1214–~1400 (prior session read
  only to ~1273 — READ THE REST before dispatching).** Tasks 8–9 = ~1400–1535 (NOT read yet —
  read before dispatching each).
- Spec: `docs/superpowers/specs/2026-07-07-selena-seamless-custom-design.md`.
- `tools/avatar/parts.py` — AVATAR_AXES / DEFAULT_AVATAR / CONFIG_VERSION (single source of truth).
- `tools/avatar/portrait.py` — phrase maps `_BODY.._OUTFIT` + `PROMPT_MAPS` (gates 10 axes:
  bodyColor, irisColor, eyeShape, mouth, lashes, blush, glasses, topper, accessory, outfit —
  NOT background) + `phrase_for` + `config_to_prompt` (uses `_humanize` silent fallback for
  unmapped ids — that's what the coverage gate prevents).
- `tools/avatar/export_axes.py` — regenerates `axes.generated.ts`; supports `--check` (exit 1
  if stale). Parity guard: `tests/avatar/test_axes_parity.py`.
- `tools/avatar/make_tile_placeholders.py` — keyless PIL placeholder tiles. `tools/avatar/
  generate_tiles.py` — PAID tile generator (MOCK_MODE-refusing) — Tasks 10/11 ONLY.
- `tools/avatar/tiles.py` — `TILES_ROOT` (abs, `__file__`-anchored), `TILE_AXES`, tile lookups.
- `frontend/src/aurora/avatar/{manifest.ts,backdrops.ts,tiles.ts,axes.generated.ts}` — colour
  hexes / backdrop gradients / `tileSrc` URL builder (`/avatar/tiles/${axis}/${id}.webp`) /
  generated axis mirror.
- `frontend/src/aurora/screens/SelenaStudio.tsx` — Task 5 target (shipped `7ba2041`).
- Gates: backend `python -m pytest -q` (MOCK_MODE auto-on, keyless — NEVER set
  GEMINI_API_KEY); frontend `cd frontend && npm run typecheck && npm run build`; robust harness
  gate = warm-server assert (§9). Suite is **603 green** at Task 6.
- `scripts/start-harness.sh` — READ this session. `serve` subcommand: builds (unless
  SKIP_BUILD=1) → copies `.next/static`+`public` into `.next/standalone` → starts
  `node .next/standalone/server.js` on :3000 → warms routes (incl. /studio) → leaves server
  up. `stop` kills the pidfile'd server. It reuses a server already answering on :3000.
- CLAUDE.md + memory `MEMORY.md` govern: WAT architecture, single-worker Render (never block
  the event loop), fail-closed config, JWT identity, PowerShell-tool-vs-Bash-tool discipline
  (bash_guard hook), DB migrations only via /db-migrate, never delete Dockerfile.

## 9. GOTCHAS & CONSTRAINTS
- **DO NOT push `dd99251` until the aurora harness is green.** It's a frontend change; `main`
  auto-deploys Render prod; CLAUDE.md /ship-check mandates a behavioral harness verify for
  user-facing changes. All other gates (603 pytest, typecheck, build, both reviews) are green
  — the harness is the last one.
- **WARM-SERVER HARNESS RECIPE (robust gate — worked perfectly last session):** run `serve`
  (SKIP_BUILD=1 valid here per §1), then `node frontend/tests/aurora_assert.mjs
  http://127.0.0.1:3000` (expect EXIT=0, ~51 PASS), then `stop`. **Do NOT run the full
  `bash scripts/start-harness.sh aurora`** (build→assert→teardown in one) — it can pass every
  assert then HANG on teardown for tens of minutes. If a run wedges, kill ONLY the
  start-harness bash PID + playwright chromium (match `ms-playwright`/`_npx`) — NEVER the
  user's `chrome.exe`. A stale `node .next/standalone/server.js` can hold a Windows build lock
  (EBUSY) and break `npm run build` — kill it first (the implementer hit + fixed exactly this
  EBUSY once this session; none is running now).
- **HARNESS FLAKE (memory, confirmed):** full `start-harness.sh aurora` can flake on the FIRST
  cold nav (`waitForURL /cases 6000ms`) on a busy machine — NOT a bug. The warm-server recipe
  above (assert vs an already-warm server) is the reliable gate.
- **SHARED CWD / bash_guard trap:** the Bash tool and PowerShell tool share ONE persisted cwd.
  Never `cd` into a subdir inside a Bash compound command (moves shared cwd → breaks
  `.claude/hooks/bash_guard.py` and PowerShell relative git pathspecs). Use absolute paths;
  for git use `git -C "C:\Users\caleb\OneDrive\Desktop\SNEC_AI_CHATBOT" …`. The frontend build
  uses `(cd frontend && …)` subshells, which do NOT change the parent cwd. Recovery if wedged:
  `Set-Location "C:\Users\caleb\OneDrive\Desktop\SNEC_AI_CHATBOT"` in the PowerShell tool.
- **PAID gating:** Tasks 10–11 and any `generate_tiles.py --generate` / live image call require
  explicit user go-ahead. Placeholders (`make_tile_placeholders.py`) + tests are keyless.
- **Two-stage review order is mandatory:** spec compliance (sonnet) MUST be ✅ before code
  quality (opus). Apply receiving-code-review judgment (verify each finding on merits; don't
  reflexively accept or dismiss) — see the Task-6 dispositions in §3 as the pattern.
- **Implementer must NOT run the full harness and must NOT push** — LOCAL commit only; the
  controller runs the warm-server harness and pushes. Stage ONLY that task's files.
- Windows/PowerShell box: PowerShell cmdlets → PowerShell tool; POSIX/git/npm/bash-harness →
  Bash tool. bash_guard PreToolUse hook enforces this.
- Don't reword the LOCKED "✨ Fusing your look…" copy. Don't re-litigate §3 decisions.
- **This handoff commit is committed but may be LOCAL-ONLY** (see the closing note the agent
  gave the user) — if `git log origin/main..main` shows commits ahead, they are `dd99251`
  (Task 6, awaiting harness) + this handoff commit; both push together once the harness is green.

## 10. RESUME CHECKLIST
1. Read this file top to bottom. CLAUDE.md + MEMORY.md load automatically.
2. Rebuild the task list (§6) with TaskCreate/TaskUpdate (mark #1–5 completed, #6 in_progress).
3. Re-invoke the active skill: superpowers:subagent-driven-development (finishing Task 6).
4. `git -C "C:\Users\caleb\OneDrive\Desktop\SNEC_AI_CHATBOT" status` (expect clean, HEAD =
   handoff commit on top of `dd99251`) and `git log --oneline -4` to confirm the chain.
   Optionally `python -m pytest -q` (expect 603).
5. **Execute the NEXT ACTION (§1): run the warm-server harness assert for Task 6.** If EXIT=0,
   `git push origin main` (pushes `dd99251` + handoff commit), TaskUpdate #6 → completed.
6. Continue Tasks 7→9 via the same cycle (implementer sonnet → spec sonnet → quality opus →
   fix loop → controller harness → push). READ Task 7's full plan text (1214–~1400) and Tasks
   8–9 (~1400–1535) from the plan BEFORE dispatching each. Do NOT start PAID Tasks 10–11
   without explicit user go-ahead.
7. After Task 9: final whole-implementation code review → superpowers:finishing-a-development-branch
   → update memory (`project_ricoe_v2` + a new memory for this seamless-custom brief).
