# SESSION HANDOFF
> Black-box snapshot for a cross-account resume. Written 2026-06-16.
> To resume in the new session: run `/handoff-resume` (or paste this whole file and say
> "resume from this handoff").

## 1. NEXT ACTION (read this first)
Ask the user: **"Want me to write the frontend plan now?"** — that question is on the table
and unanswered. If yes (or if the user says "continue"), the next action is to invoke the
**superpowers:writing-plans** skill and write a SECOND implementation plan at
`docs/superpowers/plans/2026-06-16-osce-station-frontend.md` that rebuilds the virtual-patient
activity screen `frontend/src/aurora/screens/CaseSession.tsx` into the approved colourful,
animated, light-mode "Guided OSCE Station" UI, consuming the new `/station` and `/observe`
endpoints. Then execute it via **superpowers:subagent-driven-development** (the user already
chose subagent-driven execution for this project).

## 2. SESSION GOAL
Turn the virtual-patient simulation into a **Guided OSCE Station**: (a) a checklist beside the
sim that **auto-ticks** as the student conducts the consult (AI examiner + deterministic ticks
from performed exam actions); (b) a 3-phase clinical flow — QnA/process/followup expressed in
clinical terms; (c) an interactive **examination "reveal"** mechanic for the process phase;
(d) **encouraging, in-depth, OSCE-anchored AI grading** at the end; (e) a **colourful, animated,
light-mode redesign** of the activity page matching the patient-selection page. Must follow the
real OSCE checklists + materials in the database. **Backend is DONE and merged to `main`. The
frontend redesign is the remaining half.**

## 3. DECISIONS LOCKED (do NOT re-litigate)
- **Interaction model = "Guided OSCE Station" (Option A)** — visible 3-phase rail; in the
  Examination phase the composer grows a tray of clinical *actions*; clicking one performs it,
  reveals the real finding from the case, and auto-ticks the matching step. Student can also just type.
- **Phase names (from the DB category vocabulary):** ① Preparation & Identification ② Clinical
  Assessment ③ Documentation & Follow-up. (User accepted; said "follow the database".)
- **Checklist content = the FULL real steps from Supabase** (20 checklists, 472 steps), grouped
  into the 3 phases; render only phases that actually contain steps (don't fabricate empty ones).
- **Auto-tick = LIVE, per student turn** — AI "examiner" (`/observe`) + deterministic action ticks;
  manual click-to-toggle kept as fallback. Technique sub-points (explain/3 readings/average) stay
  conversational, so clicking an action alone does NOT max the score.
- **Wording:** the checklist label is **"OSCE checklist · auto-tracked · N steps"** and auto-detected
  steps carry a subtle **"✦ auto"** marker. (User explicitly wanted the word "auto"; rejected
  "ticks itself" and "live / marks as you go".)
- **Demographics = Option 1** — fixed values backfilled into all case files by a script (NOT
  invented by the prompt). DONE.
- **Visual direction = colourful, animated, LIGHT** — approved mockup `light-station-v3`: living
  gradient-mesh canvas (NOT flat white), gradient-ring glass cards, per-phase colour panels
  (Prep=blue #4285F4, Clinical=purple #9B72CB, Docs=rose #D96570), spinning conic aurora rim on the
  patient avatar, vivid gradient chat bubbles, gradient-green reveal card with shimmer sweep,
  full-colour phase rail pills. User feedback arc: v1 "too plain/white" → v2 added motion → v3
  "nice continue" (approved). Motion must be **CSS-only**.
- **Integration:** backend was merged directly to `main` (Option 1 of finishing-a-dev-branch) and
  pushed. Frontend will presumably also go to `main` (user works directly on main).

## 4. OPEN QUESTIONS
- **"Write the frontend plan now?"** — the only open question. Options already on the table:
  (a) write the frontend plan now, (b) defer. Default expectation: write it now.
- Background-task chip `task_756af170` ("Fix failing profile update tests") is pending the user's
  click — they can start it or dismiss it. Not blocking.

## 5. ACTIVE SKILL / WORKFLOW
None mid-flight. The chain just completed: superpowers:brainstorming → writing-plans →
subagent-driven-development → finishing-a-development-branch (Option 1: merged to main). The
**next** skill to invoke is **superpowers:writing-plans** (for the frontend), then
**superpowers:subagent-driven-development** to execute it.

## 6. TASK LIST SNAPSHOT
`TaskList` is EMPTY (the TaskCreate/TaskUpdate system was not used this session; plan execution
was tracked narratively). The only tracked item is the background-task CHIP (separate system):
- `task_756af170` — "Fix failing profile update tests" — status: pending (awaiting user click).
  (6 pre-existing failures in `tests/profile/test_update_profile.py`, e.g. `KeyError: 'session_count'`,
  confirmed present on main before this work — unrelated to OSCE station.)

## 7. WORK PRODUCT SO FAR
- **Spec (committed):** `docs/superpowers/specs/2026-06-16-virtual-patient-osce-station-design.md` —
  full design incl. §11 (frontend), §6 (exam reveal), §7 (auto-tick), §8 (grading). READ THIS for
  the frontend.
- **Backend plan (committed, fully executed):** `docs/superpowers/plans/2026-06-16-osce-station-backend.md`.
- **Frontend plan: NOT yet written** — this is the next deliverable.
- **Visual mockups (on local disk, GITIGNORED under `.superpowers/brainstorm/573-1781571859/content/`):**
  `interaction-model.html`, `station-design.html`, `light-station.html`, `light-station-v2.html`,
  **`light-station-v3.html` (the approved reference — open it if resuming on the same machine)**.
  These will NOT survive a different-machine resume; the v3 design is described in §3 above and in
  spec §11. The visual-companion server was already STOPPED.
- **Backend shipped to main (commit `98d3395`)** — these modules exist and are tested (39/39 in
  `tests/cases/`):
  - `tools/cases/resolve_checklist.py` — `resolve_procedure_name(case)->(name,how)`, `match_procedure(text)`, `build_rubric_checklist(case)`. 139/151 cases map to a real checklist; 12 Ishihara/Amsler use rubric fallback.
  - `tools/cases/phase_split.py` — `assign_phases(steps)->[1/2/3]`, `group_by_phase(steps)->[{phase,name,steps}]`, `PHASE_NAMES`.
  - `tools/cases/examination_actions.py` — `build_actions(examination_findings, steps)->[{key,label,reveal_text,satisfies_steps}]`.
  - `tools/cases/observe_steps.py` — `observe(checklist_steps, messages, already_ticked)->[int]`; returns [] on mock/quota/error.
  - `tools/cases/seed_demographics.py` — one-time tool, already run; all 151 `cases/*.json` patients now have `nric`/`date_of_birth`/`address`/`contact_number`.
  - `tools/api/routers/cases.py` — NEW `GET /api/cases/{id}/station` (auth-required) and `POST /api/cases/{id}/observe`. Submit response gained `per_phase`; debrief reworded to lead with "What you did really well" / "Where to grow next time".
  - `tools/api/shared.py` — `PATIENT_SYSTEM` gained a rule: give recorded NRIC/DOB/address only when asked to verify identity.

### Exact endpoint payload shapes the frontend will consume
- `GET /api/cases/{id}/station` →
  `{ case:{case_id,title,difficulty,topic,estimated_minutes,patient:{name,age,presenting_complaint}},
     checklist:{ procedure_name, phases:[{phase:int, name:str, steps:[{step_number,action,critical,category,notes}]}],
                 total_steps, critical_count, source:"checklist"|"rubric" },
     examination_actions:[{ key, label, reveal_text, satisfies_steps:[int] }] }`
- `POST /api/cases/{id}/observe` body `{ messages:[{role,content}], already_ticked:[int] }` → `{ newly_satisfied:[int] }`
- `POST /api/cases/{id}/submit` (unchanged contract + new field) → `{ result:{...4 domains...}, cards, mock_mode,
     debrief, checklist_comparison:[{step_number,action,critical,performed,clinical_note}], per_phase:[{phase,name,done,total}] }`

## 8. KEY FILES & REPO FACTS (for the frontend work)
- **REBUILD:** `frontend/src/aurora/screens/CaseSession.tsx` — currently a DARK two-pane sim with a
  MANUALLY-clicked checklist (`tickedSteps`, `toggleStep`). It fetches `/api/cases/{id}` and
  `/api/cases/{id}/checklist`, streams `/api/cases/{id}/chat` (SSE), submits to `/api/cases/{id}/submit`.
  Keep the SSE + submit logic; switch checklist fetch to `/station`; add `/observe` polling after each
  student turn; add examination-tray action handling + reveal cards; replace dark styling with the
  light colourful v3 design. Preserve `sessionStorage` handoff key `eyebot_case_handoff` and existing test hooks.
- **THEME REFERENCE:** `frontend/src/aurora/screens/Cases.tsx` (the light patient-selection page to match).
- **TOKENS:** `frontend/src/aurora/tokens.css` — `--paper #F6F7F9`, `--canvas #EEF0F8`, `--surface #fff`,
  `--ink #1F1F1F`, `--ink-2 #5F6368`, gemini gradient (`--g-blue #4285F4`, `--g-purple #9B72CB`,
  `--g-rose #D96570`), `--g-green #34A853`, `--wash-lavender`. Use these.
- **CSS TO REPLACE:** `frontend/src/aurora/aurora.css` — the `.aurora-session-*` block is roughly
  **lines 856–930** (verify with grep `^\.aurora-session`). Replace with new `.aurora-station-*` classes.
- **MOTION:** `frontend/src/aurora/motion.css` is the CSS-only motion engine. `MotionProvider` is NOT
  mounted, so GSAP fx wrappers (SplitText/Magnetic) CRASH — use CSS animations + existing
  Reveal/RouteReveal/useCountUp only.
- **Plate/eye imagery:** `@/aurora/media` (`PLATE`) — use for the patient avatar/hero instead of the
  placeholder glyph in the mockup.
- DB facts: Supabase has 20 checklists / 472 steps; step categories drive phases; source of truth is
  `tools/kb/search.get_checklist_by_name`.

## 9. GOTCHAS & CONSTRAINTS
- **User standing rules (CLAUDE.md + memory):** auto stage+commit+push after meaningful work;
  do NOT create/overwrite files in `workflows/` without explicit permission (the
  `workflows/case_simulation.md` update is DEFERRED, needs the user's OK); `.tmp/` is disposable.
- **Render prod = ONE uvicorn worker** — never block the async event loop; all AI calls via
  `asyncio.to_thread` + timeouts. `/observe` adds one cheap Gemini call per student turn; it is
  resilient (returns [] on quota/error) — keep it that way; debounce on the frontend.
- **Motion = CSS-only** (see §8). Honor `prefers-reduced-motion` / `html[data-motion="reduce"]`.
- **Engineering bar:** user wants Stanford-caliber, custom, polished frontend (memory:
  feedback_design_standard, feedback_engineer_persona). Add in-UI help/explanations where useful
  (memory: feedback_explain_to_users).
- **Pre-existing uncommitted file:** `frontend/next-env.d.ts` shows as modified — it's Next.js
  auto-generated, present at session start, NOT this session's work. Leave it / don't build on it.
- **Pre-existing test failures:** 6 in `tests/profile/test_update_profile.py` fail on main (unrelated);
  do not treat as a regression. Validate cases with `python -m pytest tests/cases/ -q` (use
  `python -m pytest`, not bare `pytest`; Windows shell + Bash tool both available; save files UTF-8).
- **Gemini model note:** Gemini 3.x model IDs are real (post knowledge-cutoff); don't "correct" them.
- Backend endpoints are ADDITIVE — the old `/checklist` still exists for back-compat (smoke test relies on it).

## 10. RESUME CHECKLIST
1. Read this file fully.
2. Read the spec `docs/superpowers/specs/2026-06-16-virtual-patient-osce-station-design.md` (esp. §6, §7, §8, §11).
3. Skim the backend plan + the new modules in `tools/cases/` and the `/station` + `/observe` endpoints in `tools/api/routers/cases.py` to confirm the payload shapes in §7 above.
4. If on the same machine, open `.superpowers/brainstorm/573-1781571859/content/light-station-v3.html` to see the approved visual; otherwise rely on §3 + spec §11.
5. Confirm with the user: "Resuming the OSCE Station — backend is shipped to main; ready to write & execute the FRONTEND plan (light colourful animated CaseSession). Go?"
6. Execute NEXT ACTION (§1): invoke superpowers:writing-plans → write `docs/superpowers/plans/2026-06-16-osce-station-frontend.md` → then superpowers:subagent-driven-development to build it. Commit + push as you go.
7. Optionally surface the `task_756af170` profile-tests chip if the user wants it handled.
