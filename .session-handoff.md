# SESSION HANDOFF
> Black-box snapshot for a cross-account resume. Written 2026-07-06 (RICOE v2 · Selena 3D-portrait workstream).
> To resume in the new session: run `/handoff-resume` (or paste this whole file and say
> "resume from this handoff").

## 1. NEXT ACTION (read this first)
Build **3D-portrait Task 2 — generate + store** (per the plan `docs/superpowers/plans/2026-07-06-ricoe-v2-selena-3d-portrait-on-save.md`).
Concretely, in `tools/avatar/portrait.py` add `render_portrait(config)->bytes` (call `gemini-3.1-flash-image`
reusing the image client from `tools/avatar/generate_sprites.py`, prompt = `config_to_prompt`, anchored to
`frontend/public/brand/iris.png`) and `store_portrait(hash, bytes)->url`; add `upload_avatar(...)` to
`tools/shared/supabase_client.py` (reuse the `upload_kb_image` pattern → public bucket `selena-avatars`);
author migration `007_avatar_images.sql` (cache table). **Scaffold-first / MOCK-safe: never fire live gen in
tests** — the live call only runs on the real cache-missed save path (Task 3). The pure core (Task 1) is DONE.

**TWO OUT-OF-BAND SETUP ITEMS the user must do before Task 2/3 land in prod** (code degrades gracefully to
SVG-only until then, but portraits won't generate/persist without them):
  (a) create a **PUBLIC** Supabase Storage bucket named **`selena-avatars`** (Dashboard → Storage → New bucket → public);
  (b) apply **migration `007_avatar_images.sql`** via `/db-migrate` (same paste-into-SQL-editor flow as 006).
Flag both to the user when Task 2 code is ready. Per CLAUDE.md's git exception you may still ship the gated
code first — but say so plainly and coordinate so `main` never boots broken.

The user's last substantive instruction was **"continue"** (autonomous cadence — keep shipping, don't ask).

## 2. SESSION GOAL
RICOE v2 world-class rebuild, currently in the **Selena avatar** workstream. Deliver a per-student,
customizable, on-brand **Selena** (= the one-eyed Iris mascot) with a fun/gamified builder AND a real 3D
portrait. Backend + registry + SVG compositor + gamified Studio are SHIPPED; the current build wires a
**real 3D Iris PNG generated per saved config and cached** (Nano-Banana `gemini-3.1-flash-image`), shown
everywhere while the instant SVG stays as the free live-edit preview.

## 3. DECISIONS LOCKED (settled — do NOT relitigate)
- **D9 — SELENA *IS* IRIS** (remember forever): the one-eyed soft-3D homepage mascot
  (`frontend/public/brand/iris.png`, `GreetingHero.tsx` `.hm-iris`). One big customizable eye, NO hair,
  smooth blob body, tiny arms, floor shadow. Every custom Selena is an Iris variant — NOT a human.
- **D10 — [SUPERSEDED by D12]** render was to be a curated 3D sprite library composited at runtime.
- **D11 — SCAFFOLD-FIRST**: build with free placeholders first; fire PAID Nano-Banana only on greenlight.
  **Greenlight GIVEN 2026-07-06** — pilot done, generation validated (`1d1dc13`, 3 images in `.tmp/selena-pilot/`).
- **D12 — real 3D = PER-CONFIG GENERATE-ON-SAVE, CACHED** (user-decided 2026-07-06; supersedes D10):
  the image model bakes a FULL look into ONE transparent PNG (pilot proved parts don't composite). On Save,
  studio config → `gemini-3.1-flash-image` render (anchored to `iris.png`) → cached by config-hash in
  Supabase → shown everywhere. SVG `<Selena>` stays as the free live-edit preview. The `background` axis is a
  **CSS backdrop** behind the transparent PNG — EXCLUDED from the character prompt AND the cache hash, so
  backdrop-only changes are free (already implemented in `portrait.py`).
- **Model choice**: use **`gemini-3.1-flash-image`** — the pro image model (`gemini-3-pro-image`) 504s under
  load. Dedicated image client needs `HttpOptions(timeout=240_000)` + 503/504 retry (already in generate_sprites.py).
- **Onboarding ref = Bitmoji builder** (inspiration only, DO NOT COPY): big live preview on top, ONE category
  per page, option grid + colour-swatch column, category nav, Save; gamified (surprise-me). Already built as Selena Studio.
- D1–D8 still hold (rebuild-in-place; ivory&ink flashcards + moving-Gemini accent; leaderboard everyone +
  hide-toggle, XP-only rank; uniforms excluded until asked). Foundation 1 (design tokens) shipped `63cfed4`.

## 4. OPEN QUESTIONS / COORDINATION
- **Migration `006_avatar.sql` APPLIED 2026-07-06** (`77b1c55`); PUT `/api/avatar` persists. Done.
- Paid gen greenlit + validated (pilot `1d1dc13`).
- **BEFORE 3D-portrait Task 2/3 reach prod:** user must (a) create PUBLIC Supabase bucket `selena-avatars`;
  (b) apply migration `007_avatar_images.sql` via `/db-migrate`. No other open decisions — the productization
  question is resolved (D12).

## 5. ACTIVE SKILL / WORKFLOW
Mid RICOE v2 execution via the superpowers subagent-driven-development style, but frontend/verify tasks run
**INLINE** (subagents hang on `next build`). Active plan: `docs/superpowers/plans/2026-07-06-ricoe-v2-selena-3d-portrait-on-save.md`.
Remaining plan steps: **Task 2 (generate+store+migration 007) ← NEXT**, Task 3 (non-blocking endpoints),
Task 4 (frontend swap + CSS backdrop). No brainstorming needed — plan is authored & approved.

## 6. TASK LIST SNAPSHOT
The in-tool TaskList is EMPTY this session — I've tracked tasks manually here. Rebuild from this list if desired:
```
#1  completed  Task 1 — parts registry + validate_config
#2  completed  Task 2 — GET/PUT /api/avatar
#3  completed  Task 3 — migration 006 + gate + ship
#4  completed  Author plan 2b (sprite compositor + Bitmoji onboarding)
#5  completed  Build <Selena> SVG compositor + manifest (plan 2b Tasks 1+2: axis mirror+parity, compositor)
#6  completed  Selena Studio — one-per-page gamified onboarding/edit flow (plan 2b Task 3) — 576fb52
#7  completed  Behavioral verify — aurora harness studio check (select→repaint→Save round-trip) — 1cf42f4
#8  completed  Nano-Banana generator tool + 3-image pilot validated — 1d1dc13 (flash-image is the batch model)
#9  completed  3D-portrait plan authored + Task 1 (config→prompt + config→hash core, 10 tests) — 2329f55
#10 pending    3D-portrait Task 2 — render_portrait/store_portrait + upload_avatar + migration 007 — NEXT
#11 pending    3D-portrait Task 3 — POST/GET portrait endpoints (BackgroundTasks→to_thread, non-blocking)
#12 pending    3D-portrait Task 4 — frontend swap (SVG preview → 3D PNG when ready) + CSS backdrop
#13 pending    [superseded] plan 2b Task 4 (SVG-on-home) — folded into 3D-portrait Task 4
```

## 7. WORK PRODUCT SO FAR (all committed + pushed to `main`; working tree CLEAN)
- `ce2f3a2`→`b358750` Plan 2 (registry, GET/PUT, typed body+audit, migration 006) — fully shipped, reviewed.
- `ca22daf` registry REWORKED to Iris eye-mascot axes (`tools/avatar/parts.py`: bodyColor(20), irisColor(incl
  galaxy), eyeShape, lashes, mouth, blush(incl stars/freckles), glasses(incl heart/goggles), topper(incl
  crown/horns/flame), accessory, outfit(collar/lanyard/labcoat/cape), background(incl galaxy/confetti);
  CONFIG_VERSION 1→2; default = homepage Iris). Tests updated.
- `ba12819` plan 2b doc. `3aa439b` plan 2b Task 1: `tools/avatar/export_axes.py` +
  `frontend/src/aurora/avatar/axes.generated.ts` + `tests/avatar/test_axes_parity.py` (drift guard).
- `433d840` plan 2b Task 2: `frontend/src/aurora/avatar/{manifest.ts,renderSelena.ts,Selena.tsx}` — the
  `<Selena>` layered SVG compositor (one-eyed Iris, soft-3D placeholder parts). Parity is a COMPILE-TIME guard
  (shape registries typed `Record<IdUnion,...>` → `npm run typecheck` fails on any unhandled id).
- `576fb52` plan 2b Task 3 — **Selena Studio**: `frontend/src/hooks/useAvatar.ts` (useAvatar/useSaveAvatar on
  new `["avatar"]` query key + `AVATAR_COMBOS`), `frontend/src/aurora/screens/SelenaStudio.tsx` +
  `frontend/src/aurora/studio.css`, `/studio` route (CheckInGuard, ssr:false), "Customize Selena" entry in
  Profile. ONE axis/page; colour axes = swatches, others = live mini-`<Selena>` tiles; per-step + all-axis
  "Surprise me"; honest "one of N looks" stat; CSS-only save celebration.
- `1cf42f4` behavioral verify — `frontend/tests/aurora_assert.mjs` drives `/studio` vs a mocked `/api/avatar`:
  select repaints the pinned `<Selena>` (detect via `#8FD8D2` aqua hex — renderSelena uses per-call gradient
  uids so raw-SVG equality is a false positive), shape steps render mini-`<Selena>` tiles, Save round-trips to
  PUT (captures `savedAvatar.bodyColor==="aqua"`), one h1, no 390px overflow. `scripts/start-harness.sh` warms `/studio`.
- `1d1dc13` **Nano-Banana generator** `tools/avatar/generate_sprites.py` — `_image_client()`
  (`HttpOptions(timeout=240_000)`), `generate_one(...)` with 504/503 retry, `STYLE` contract, `PILOT_LOOKS`,
  CLI `--estimate|--pilot|--model pro|flash|--only`, refuses live path in MOCK_MODE, outputs to `.tmp/selena-pilot/`.
  Run as `python -m tools.avatar.generate_sprites`. Pilot = 3 gorgeous transparent Iris variants; finding:
  pro 504s under load → flash-image; model bakes FULL looks, not composable parts.
- `2329f55` **3D-portrait Task 1** — `tools/avatar/portrait.py` (`PORTRAIT_AXES` = all axes minus background;
  `config_hash` = sha256[:16] over normalized portrait axes, order/extra-key/background-invariant;
  `config_to_prompt` = Iris `_CONTRACT` + per-axis phrasing dicts `_BODY/_IRIS/_EYE/_MOUTH/_LASHES/_BLUSH/
  _GLASSES/_TOPPER/_ACCESSORY/_OUTFIT`, skips `none`, excludes background) + `tests/avatar/test_portrait.py`
  (10 tests). Full suite **528 green**.
- `77b1c55` migration 006 ledgered APPLIED (`tools/db/migrations/APPLIED.md`).
- Memory updated: `reference_selena_image_gen.md` (CREATED — model choice, tool usage, "bakes FULL looks");
  `project_ricoe_v2.md` + `MEMORY.md` (D9–D12, Studio, pilot). Handoffs `028beb7`/`b63da7d`/`837bcb0`.

## 8. KEY FILES & REPO FACTS
- `tools/avatar/portrait.py` — Task-1 pure core (edit here for Task 2 render/store).
- `tools/avatar/generate_sprites.py` — paid generator; reuse its `_image_client()` + retry for `render_portrait`.
- `tools/avatar/parts.py` — registry, single source of truth (`AVATAR_AXES`, `DEFAULT_AVATAR`, `CONFIG_VERSION`).
- `tools/api/routers/avatar.py` — GET/PUT `/api/avatar` (persists via `tools.shared.db.update_profile(sub,
  avatar_config=…)`, NOT `tools/profile/update_profile.py` which lacks that kwarg). Task 3 adds POST + extends GET.
- `tools/shared/supabase_client.py` — add `upload_avatar` (reuse `upload_kb_image` pattern). Verify exact name before use.
- `tools/db/migrations/` — 006 applied; add `007_avatar_images.sql` here. `APPLIED.md` is the ledger.
  Lint: `tools/db/lint_migration.py`. NO `ADD CONSTRAINT IF NOT EXISTS` / `CREATE POLICY IF NOT EXISTS` (42601).
- Frontend: `frontend/src/hooks/useAvatar.ts` (Task 4 exposes portrait_url/status here),
  `frontend/src/aurora/screens/SelenaStudio.tsx` (Save triggers POST + poll),
  `frontend/src/aurora/avatar/{Selena.tsx,renderSelena.ts,axes.generated.ts}` (axes.generated.ts is GENERATED —
  never hand-edit; regen via `python tools/avatar/export_axes.py`), `GreetingHero.tsx` (`.hm-iris` home mascot).
- Plans: `docs/superpowers/plans/2026-07-06-ricoe-v2-selena-3d-portrait-on-save.md` (active),
  `.../2026-07-05-ricoe-v2-foundation-2b-selena-compositor-onboarding.md`. Spec: `docs/superpowers/specs/2026-07-05-ricoe-v2-design.md`.

## 9. GOTCHAS & CONSTRAINTS
- **NEVER fire live Nano-Banana/Gemini image gen in tests** (MOCK/keyless). Live gen only on the real
  cache-missed save path. One flash-image ≈ 1–2¢, then free forever (cache). Rate-limit the POST.
- **Never block the event loop** — prod is ONE uvicorn worker on Render free. Generation (~15–30s) MUST run
  via FastAPI `BackgroundTasks` → `asyncio.to_thread` (NOT Celery — not deployed on free tier). Wrap every
  blocking call (Gemini, Supabase sync, bcrypt) in `asyncio.to_thread` + timeout.
- **Identity from JWT** `current_user["sub"]`, never the request body. New endpoints on the shared `limiter`.
- **Secrets only in env / Render dashboard** — never commit `.env`/`credentials.json`/`token.json`. **Never
  delete the `Dockerfile`** (the live service builds it; deleting broke prod 2026-06-26).
- **Migrations**: no `IF NOT EXISTS` on constraints/policies; never paste a file *path* into the SQL editor;
  use `/db-migrate`; ledger in `APPLIED.md`.
- **Ship discipline**: do not push red — run `python -m pytest -q` (528) + `cd frontend && npm run typecheck &&
  npm run build`. Auto-commit+push to `main` after each task; trailer
  `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`. Stage only task-relevant files.
- **Commit messages with quotes/apostrophes**: PowerShell here-strings AND git `-m` mangle them → write the
  message to a scratchpad `.txt` and `git commit -F <file>`.
- **Shell**: PowerShell cmdlets in the PowerShell tool; POSIX in the Bash tool. Bash cwd can get stuck in
  `frontend/` (breaks `.claude/hooks/bash_guard.py`) — prefer git/gate via PowerShell or reset cwd. Run module
  scripts as `python -m tools.avatar.<x>` (bare path → `ModuleNotFoundError: No module named 'tools'`).
- **PERSIST_SCHEMA_VERSION** (`frontend/src/.../queryClient.ts`): bump on ANY persisted-query shape change.
  useAvatar added a NEW key `["avatar"]` (no bump needed); Task 4 adding portrait fields to it also needs none
  unless an EXISTING persisted key's shape changes — check before shipping.

## 10. RESUME CHECKLIST
1. Read this file + skim memory `project_ricoe_v2.md` (D1–D12) + `reference_selena_image_gen.md` + the active
   plan `docs/superpowers/plans/2026-07-06-ricoe-v2-selena-3d-portrait-on-save.md`.
2. One-line confirm to the user: "Resuming RICOE v2 — Selena backend/Studio/pilot shipped; building 3D-portrait
   Task 2 (generate+store+migration 007), gated behind the `selena-avatars` bucket + migration 007." Cadence =
   keep-shipping autonomously (user said "continue").
3. Rebuild the task list from §6 if using the TaskList tool (it's empty otherwise).
4. Execute **Task 2** (render_portrait/store_portrait + upload_avatar + migration 007), MOCK-safe, TDD, commit.
   Then Task 3 (endpoints), Task 4 (frontend swap) — each committed, each verified, INLINE frontend verify.
5. **Flag the two out-of-band items** to the user (PUBLIC `selena-avatars` bucket + apply migration 007) so
   `main` never boots broken; land gated code with a plain heads-up per the CLAUDE.md git exception.
