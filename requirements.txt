# EyeBot — Python Dependencies
# Generated by Agent 3: Dependency Installer
# Install with: pip install -r requirements.txt

# Core AI
google-genai>=2.0.0
httpx>=0.28.1

# Google Integration
gspread>=6.2.1
google-auth-oauthlib>=1.4.0

# Image Processing
Pillow>=10.0.0

# API Server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
slowapi>=0.1.9
python-multipart>=0.0.9

# Case Validation
pydantic>=2.6.0

# PDF Generation (Phase 2)
reportlab>=4.0.0

# Knowledge Base — PDF ingestion + vector DB
supabase>=2.31.0
pymupdf>=1.28.2
json-repair>=0.63.3

# Authentication
bcrypt>=5.0.0
python-jose[cryptography]>=3.3.0

# Async task queue
celery[redis]>=5.3.0
redis>=8.1.0

# Observability — error tracking stays DORMANT unless SENTRY_DSN is set
sentry-sdk>=2.0.0

# Environment
python-dotenv>=1.0.0

# NOTE: test/CI tooling (pytest, pip-audit, pip-tools) lives in
# requirements-dev.txt so production installs stay lean. For fully reproducible,
# hash-pinned builds, generate requirements.lock on Python 3.12 via
# scripts/lock-deps.sh and point render-build.sh at it.
