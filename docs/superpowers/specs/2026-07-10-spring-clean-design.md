# Spring Cleaning — Whole-Project Cleanup (2026-07-10)

**Status:** in progress
**Source:** 44-agent read-only audit (9 dimensions, adversarial verification of risky deletions).
**Approved scope (user, 2026-07-10):** Full safe sweep now · Retire the manifest-media vertical · Refactor-and-remove the fx motion cluster.

Production repo, ships straight to `main` → auto-deploys to Render. Every phase ends with the
relevant verify gate green (`pytest -q`, `frontend typecheck + build`, aurora harness) before commit.

## Guardrails (non-negotiable)

1. **Do not touch in-flight work.** The tree carries a concurrent session's Ghibli-flashcards changes:
   `frontend/src/aurora/components/flashcards/Payoff.tsx`, `frontend/src/aurora/aurora.css`,
   `docs/design-locks.md`, `tools/media/generate_flashcards_topics.py`, and the untracked
   `docs/superpowers/specs/2026-07-10-flashcards-ghibli-topics-white-reveal-design.md`. Never stage these.
2. **Stage only this task's files** per commit (tree carries unrelated dirty files).
3. **Preserve intentional keeps:** `iris.png`, default `SelenaLogo`/`rest` pose, `flashcards-scene.png`
   ("cutie"), provenance scripts (`strip_logo_bg.py`, `migrate_flashcards_from_sheets.py`), `Dockerfile`,
   `CLAUDE.md`, `ARCHITECTURE.md`, `SECURITY.md`, `design-locks.md`, `APPLIED.md`.
4. **Coupled deletions go together** (see phases) or typecheck breaks.
5. **Verify green before every push.** Never ship red.

## Phases (each = one or more logical commits, verified green)

### P1 — Git & disk hygiene (no tracked-code change)
- `git worktree remove` the 5 abandoned `.claude/worktrees/agent-*` + `musing-booth` + `relaxed-merkle`; `rm -rf` the orphan `agent-af69ed…`.
- Delete 14 merged local branches + 4 `worktree-agent-*` branches (`-d`, self-guarding).
- Clear `.tmp/` contents (regenerable AI intermediates, gitignored).
- `git gc` (repack; no history change).
- `.gitignore`: untrack `frontend/next-env.d.ts` + add ignore; delete untracked scratch `frontend/tests/_shot_tmp.mjs`.
- **Decide:** local branch `claude/musing-booth-d6a761` has 2 unmerged test-hardening commits → cherry-pick onto main, then drop.
- **Commit:** `.gitignore` + `next-env.d.ts` untrack (+ cherry-picked tests if kept).

### P2 — Frontend easy dead code (~1,300 LOC) + coupled anatomy assets
Delete (all verify=safe): `screens/GuidedTour.tsx`, `aurora/components/PlateWell.tsx`, `aurora/components/Sparkline.tsx`,
`hooks/useInViewport.ts`, `screens/ExerciseSplit.tsx`, `screens/SkeletonLoader.tsx`, `screens/transition/`,
`lib/legacy/{curriculum,topicIcons,trackColors,springs}.*` (trio+springs together),
`fx/canvas/fluid/`, `fx/cursor/Magnetic.tsx`, `fx/text/SplitText.tsx`, `fx/gsapSetup.ts` (+ trim their `fx/index.ts` barrel lines).
Coupled: 7 `frontend/public/anatomy/*.png` + `anatomy/README.md` (referenced only by the deleted `trackColors.ts`).
Tidy orphaned CSS the deletions create where trivially safe (leave `@keyframes aurora-plate-in` — still used by AtlasMap).
- **Gate:** `npm run typecheck && npm run build` + aurora harness. **Commit.**

### P3 — Backend dead code + doc accuracy
- Remove `tools/kb/search.py::search_checklist()` (+ drop from module docstring), `tools/cases/rubric_prompts.py::build_eval_prompt()`.
- Fix stale `search.py` module docstring (RAG removed from chat path).
- **Gate:** `pytest -q`. **Commit.**

### P4 — Stale-doc archive
- `git rm .session-handoff.md` (work shipped; regenerated by /handoff).
- Move to `docs/superpowers/specs/archive/` + `docs/superpowers/plans/archive/`: `ricoe-roadmap.md` (→ docs/archive/) and the superseded flashcards lineage (aperture, 06-18 redesign, stepped-selection, vivid-calm, warm-cream, 06-29 reveal-rework) + the 2026-06-03 duolingo pair. Keep the current AURORA + ricoe-v2 + charge-flip-reveal + in-flight ghibli specs.
- **Commit.**

### P5 — Clear-cut dependency removal
- `requirements.txt`: drop `tiktoken`, `google-api-python-client`, `google-auth-httplib2`.
- (`three` + `@react-three/*` handled in P7 after fx removal.)
- **Gate:** `pytest -q` (import smoke). **Commit.**

### P6 — Test wiring (coverage improvement, not deletion)
- Add `greeting_assert.mjs` + `leaderboard_logic.mjs` (type-stripped node harnesses) to the CI frontend job.
- Simplify the dead skip-guard in `tests/flashcards/test_foundations_taxonomy.py`.
- **Gate:** run both harnesses + `pytest -q`. **Commit.**

### P7 — Retire the manifest-media vertical (behavior change; coordinated)
Frontend: delete `fx/media/HiggsfieldLoop.tsx`, `fx/media/AccentSvg.tsx`, `lib/media/{client,types}.ts`, dead `PLATE` keys in `aurora/media.ts`.
Assets: 4 `*-loop-00.mp4`+posters, 6 plate rasters, 16 gen/seed accent SVGs, `public/media/manifest.json` (keep `greeting-selena.*`, `case-session-photo-00`, `eye-atlas-plate-00`, `cutie`, `iris`).
Backend: remove `/api/media` router + its mount in `server.py`; the `media_refresh` Celery task + its `celery_app` include/route; the AdminOverview "Refresh media library" button + `refreshMedia()`; generator tools `tools/media/{build_manifest,generate_accents,seed_accents,ingest_loops}.py`; Dockerfile media-copy/chown lines.
Workflow: update/retire `workflows/media_generation.md` accordingly (⚠ workflow edit — authorized by this cleanup's scope).
- **Gate:** `pytest -q` + `typecheck + build` + aurora harness + behavioral check (admin screen loads, no dead button). **Commit.**

### P8 — fx motion cluster refactor + removal (behavior change)
- Refactor `fx/TransitionProvider.tsx`: drop `useFx` (source reduced-motion from `@/aurora/motion`) and drop the `useAudio`/`play("whoosh")` wiring. Keep `useWipeNavigate` working (OnboardingScreen depends on it).
- Delete `fx/MotionProvider.tsx`, `fx/ScrollProvider.tsx`, `fx/ticker.ts`, `fx/canvas/fluidBus.ts`, `fx/liquid/`, `fx/audio/`; prune all now-dead `fx/index.ts` re-exports. Keep `confetti.ts`, `Reveal.tsx`/`RouteReveal`, `TransitionProvider`.
- Remove `three`, `@react-three/fiber`, `@react-three/drei`, `@types/three` from `package.json`.
- **Gate:** `typecheck + build` + aurora harness + behavioral (wipe-nav still works, no console errors). **Commit.**

### Deferred (explicitly out of this sweep)
- **`.git` history rewrite** (~90 MB `chinita/proposal/marketing` blobs) — high-risk force-push on an auto-deploy repo; needs a planned maintenance window + mirror backup.
- **Dependabot / remote-branch triage** (13 bot branches, 9 merged remotes, `production-hardening`) — GitHub-side, needs per-PR CI + Render-env confirmation.
- **Media re-encode** (PNG→WebP, mp4 compression) — optional asset optimization, separate pass.
- **`components/ui/button.tsx`** shadcn scaffold + its 2 exclusive deps — kept (repo intentionally supports shadcn); removable on request.

## Acceptance criteria
- After each phase: its verify gate is green; only this task's files are staged.
- Final tree: no dangling imports, `pytest -q` + `frontend typecheck + build` + aurora harness all green, in-flight Ghibli files untouched.
- Net: ~534 MB disk reclaimed, ~2,100+ LOC dead code removed, manifest-media + fx dead subsystems gone, 3+ deps dropped, docs archived.
